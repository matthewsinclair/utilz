---
issue: 0050
title: Two of hoist-rebase.sh's postconditions cannot pass: the code they name moved and the checks did not follow
status: CLOSED
severity: low
created: 2026-09-22
closed: 2026-09-22
---

`hoist-rebase.sh` (ST0010's attachment) checks after a merge that Utilz's own crate-side work survived. Two of its checks name a file the code has left, so a run on the live tree reports two losses that are not losses. Measured 2026-09-22 by running the script's postcondition section alone against a copy of the files it reads.

- `post "test/acceptance.sh" "PREZ_TEST_BROWSER" 5 "AC18(b) browser override"`: the live count is 4 and the floor is 5. Issue 0040 (`ef32951`, 19 Sep) moved `chrome()` and its override into `test/lib/harness.sh`, which holds 7. `acceptance.sh` held 11 before that move.
- `post "src/theme.rs" "Origin::SearchPath" 2 "AC14 Origin enum"`: the live count is 1 and the floor is 2. ST0017 WP-01 (`c5afc33`, 9 Sep) moved the theme resolver into `crates/artifact/src/theme.rs`, which holds 7 occurrences and the `pub enum Origin` itself. `src/theme.rs` held 3 before that move.

Nothing has been lost and both moves were deliberate. The damage is to the instrument: the script's whole job is to print `LOST` when a clean-looking merge has dropped Utilz-side work, and a script that prints it twice on an untouched tree teaches its reader that the word means nothing.

THE FIX: each check names the file the code lives in now, with its floor unchanged, 5 and 2 against live counts of 7 and 7.

NOT CHECKED, and recorded rather than assumed: the script's merge half still assumes the crate's pre-workspace layout, which ST0017 replaced with a Cargo workspace. Whether a rebase from a new `_tools` pin is still something Utilz wants is hv's call. This issue is about the postconditions alone.

---

_Generated by Intent v3.2.0 from the issue canon. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

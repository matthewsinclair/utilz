---
issue: 0035
title: prez AT09's estate tripwire does not match Gtools, so a prez source naming its consumer passes
status: CLOSED
severity: low
created: 2026-09-14
closed: 2026-09-14
reporter: Matthew Sinclair
---

## Summary

AT09's estate tripwire (`opt/prez/crate/test/acceptance.sh:940`) greps every `src/` in prez's workspace for `(/Users/|Dropbox|[Gg]eodica)`, and `intent/restart.md:140`'s by-hand check uses the same pattern. hv's standing ruling, as `intent/restart.md:21` records it, covers Geodica "or its `_tools` (which moved to `~/Devel/prj/Gtools`)", but neither pattern matches Gtools. So a prez source that names its consumer, in code or in a comment, passes the check that exists to refuse it. The imports check beside it (`:942`) already names gtools, so the gap is in the names check only.

## Measured 2026-09-14

A scratch `src/` holding one file whose comment reads `// the Gtools shim sets PREZ_THEME_PATH before it runs prez`:

- today's pattern: 0 hits, so the leak passes;
- with `[Gg]tools` added: 1 hit;
- today's workspace `src/` with `[Gg]tools` added: 0 hits, so the fix turns nothing red today.

The pattern dates from fdf161a (2026-08-29), when AT09 first read comments.

## Fix

`[Gg]tools` joins AT09's names pattern in `opt/prez/crate/test/acceptance.sh`, with a comment naming this issue, and `intent/restart.md:140`'s by-hand check, so the two keep one pattern over one population. vc cleared the edit before its ST0020 verification finished, because vc's runs were in a scratch worktree.

Measured in the real harness, under bash 3.2.57 with a private target: AT09 with a probe file in `crates/artifact/src` whose comment names Gtools FAILS its names check (1, wanted 0), and with the probe removed it PASSES (33 `.rs` files walked, names 0, imports 0, clippy 0).

---

_Generated by Intent v3.0.3 from `the issue canon`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

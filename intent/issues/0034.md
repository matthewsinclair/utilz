---
issue: 0034
title: todo done --prune reports 'pruned 0 item(s)' whatever it archived, because write_file recounts the buckets after DONE is cleared
status: CLOSED
severity: low
created: 2026-09-14
closed: 2026-09-14
reporter: Matthew Sinclair
---

## Summary

`utilz todo done --prune` reports the wrong number. It archives every DONE item and then says `todo: pruned 0 item(s) to <history>`, whatever it pruned. `cmd_prune` clears the DONE bucket, calls `write_file`, and only then reports `$C`. `write_file` recounts the buckets with `_counts`, so `C` is already 0 when the message reads it. The line has read this way since the utility was added (`03ccded`, 2026-07-03, ST0008), and no test asserts the count.

Found by vc on 2026-09-14 while demonstrating issue 0033: two items were pruned, the archive held both, and the message said `pruned 0 item(s)`.

## Fix

Take the count before the bucket is cleared, and report that. A bats test, red first, prunes two DONE items and requires the message to name 2.

## Severity

Low. The archive is right and the message is wrong. A report of zero reads as nothing having happened, which is the one thing a prune report exists to rule out.

## Resolution

Fixed by vc at `a6c7c62`, filed at `43e9487`. `cmd_prune` takes `local pruned=$C` after the archive is written and before DONE is cleared, and the report reads `pruned`. `C` is still the DONE count there: nothing between `_counts` and that line assigns it or recounts (`_resolve_history` and `_prepend_file` do neither), and nothing after it touches `pruned`. `cmd_flush` is unchanged: its only count is its confirmation prompt, taken before its clear, and a grep for `item(s)` finds exactly those two reports in the script. No version bump: todo's 2.0.0 is already in this release, through issue 0033.

Reviewed by cc against vc's four review points, each checked at the source, and re-run by cc (logs in cc's scratchpad):

- Red first, independently: `a6c7c62`'s `todo.bats` against its parent's script, in a scratch worktree, with bats under `/bin/bash` 3.2.57. 1 of 42 failed, "prune reports how many items it archived (issue 0034)", on its own assertion at `todo.bats:268` (`red-0034-cc.log`).
- Green at `e1149ff`: 42 of 42 under 3.2.57 (`green-0034-cc.log`). ShellCheck and `bash -n` are clean on `opt/todo/todo`.
- Through the dispatcher, in a scratch directory: three items added and two done, then `done --prune` reported `pruned 2 item(s)`, and the archive held both with their ids. A second prune said `nothing to prune`.

Closed by cc, the reviewer, not the fixer.

---

_Generated by Intent v3.0.3 from `the issue canon`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

{
  "schema": "intent/issue@3.0",
  "number": 27,
  "slug": "prez-at09-checks-only-the-root-package-s-src-so",
  "title": "prez AT09 checks only the root package's src/, so the workspace members' sources are unguarded",
  "status": "open",
  "severity": "low",
  "created": "2026-09-14",
  "reporter": "Matthew Sinclair",
  "body": "## Summary\n\nAT09 -- \"the source is standalone\" -- runs four checks over `\"$CRATE/src\"` only (`opt/prez/crate/test/acceptance.sh:893-908`): estate paths or names, estate imports, files containing tabs, and lines at an odd indent. Since ST0017 the crate is a workspace, and `crates/artifact/src` (the shared theme, inlining and base64 code) and `crates/showreel/src` (the showreel pipeline) are compiled into the binaries prez ships, yet sit outside all four checks. The estate grep in `intent/restart.md`'s fresh-checkout checklist has the same population. Found by vc on 2026-09-14 in the pre-release check.\n\n## Measured 2026-09-14\n\nAll four AT09 checks, run by hand over each source directory:\n\n| Directory             | `.rs` files | estate | imports | tabs | odd indent |\n| --------------------- | ----------- | ------ | ------- | ---- | ---------- |\n| `src`                 | 13          | 0      | 0       | 0    | 0          |\n| `crates/artifact/src` | 4           | 0      | 0       | 0    | 0          |\n| `crates/showreel/src` | 16          | 0      | 0       | 0    | 0          |\n\nNothing is hiding today. The gap is the guard's, and it fails green: an estate path added under `crates/showreel/src` passes AT09.\n\n## Why\n\nThe same shape as issue 0023's walk, which the shim's own comment names: a population written out by hand is correct on the day it is written, and `crates/` arrived without anyone extending it.\n\n## Severity\n\nLow. Clean today; the risk is a future leak into shipped code that no check reads.\n\n## Fix direction\n\nDerive AT09's population from the tree -- every `src/` directory under the crate, root and members, pruning `target/` -- rather than naming one, and point `restart.md`'s estate grep at the same population. Evidence for the Done, run once rather than committed: a scratch copy with a planted estate path under `crates/showreel/src` goes red at AT09 on the fix and stays green on HEAD.\n"
}

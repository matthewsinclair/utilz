{
  "id": "01M23X339212C4E2RKY7TVTZMX",
  "ts": "2026-09-09T20:17:32.450Z",
  "principal": "local",
  "project_id": "072a1cea-7b04-4cf9-aa51-0995f6d9d7a0",
  "op": "ac.edit",
  "subject": {
    "type": "ac",
    "id": "ST0017/AC-2.1"
  },
  "payload": {
    "text": "The harness derives its population label from what actually ran, never from a flag someone passed, and refuses a verdict without one. GRADED IN THREE LEGS OF DIFFERENT QUALITY, because the row read as one claim and is not -- snorkeltoast's split, verified by vc at the source, and the row stays UNSATISFIED on the middle one. LEG 1, ARTIFACT-LEVEL POPULATION: DERIVED AND RAN. capture() writes artifact_sha256 and population_source into manifest.json and compare prints the POPULATION block, so what is graded is identified by what was actually photographed rather than by an assertion. LEG 2, PRODUCER-LEVEL POPULATION: A FLAGGED GUESS, AND THIS IS WHAT HOLDS THE ROW OPEN. The run's label reads \"adjacency (UNVERIFIED)\" because no artifact stamps its own builder, so the producer is inferred to be the compiler that sat BESIDE the harness at capture time. compare says so loudly and marks the verdict PROVISIONAL, which is the honest form and not the required one: THE ROW ASKS FOR A LABEL DERIVED FROM WHAT RAN, AND ADJACENCY IS NOT THAT. It is not a flag someone passed either, so the row is half-met rather than violated. LEG 3, THE REFUSAL: PRESENT, TERMINAL, AND NEVER FIRED -- and it is a SOUND detector zero rather than a tautology, which is the distinction this thread keeps having to make. Read at showreel-harness:1150, the first die in cmd_compare: \"the {name} capture carries no artifact identity\", reached before STRUCTURE and before everything else, ending in sys.exit(1). It cannot fire on a manifest capture() wrote, because capture() always writes the identity -- but a hand-edited or partially-written manifest is a real input class and the branch is reachable from it. So it COULD fire, which is what makes the zero worth something; contrast AC-2.17's UNDECIDED branch, which no input could reach. DISCHARGED WHEN LEG 2 BECOMES A DERIVATION, WHICH IS WP-03'S TO GIVE: once the Rust build stamps its own identity into the artifact, the producer stops being the thing that was standing nearby and becomes the thing that is recorded. This is a real WP-02-on-WP-03 dependency and is stated here so it is not rediscovered as a surprise.\n\"STAMPS ITS OWN IDENTITY\" IS UNDERSPECIFIED AND THE HARNESS ALREADY SPECIFIES IT. Measured by vc 2026-09-09 20:17Z, before the stamp is written rather than after the one-shot compare is spent. `producer_stamp` reads `<meta name=\"showreel-producer\" content=\"...\">` (showreel-harness:226, STAMP_RE at :222). NOTHING ON EITHER SIDE WRITES IT: four greps -- the reference player.html, the reference compiler, the pulled template, the Rust src/ -- all zero, and the same instrument returned a real hit for `producer` at showreel:1006, so the zero is a sound detector zero rather than a tautology. THAT :1006 HIT IS ALSO THE TRAP: `\"producer\": cfg.get(\"producer\", \"\")` is the REEL'S CREDIT LINE, a different thing under the same word, and the pulled template renders it as `add(\"Producer\", REEL.producer)` -- so the artifact already carries \"Producer\" and a value in a sense that has nothing to do with who built it.\nTHE CONTENT IS SPECIFIED BY THE INSTRUMENT'S OWN COMMENT AND BY NO ROW (showreel-harness:218-223): an identifier covering the pixel-affecting rules -- long edge, resampling filter, quality, and the alpha rule -- \"because once ST0017 C2 retires the alpha asymmetry, a Rust build over Python masters is a pipeline whose halves disagree about policy by construction, and `rust` alone cannot say that.\" SO A STAMP READING content=\"rust\" MEETS THIS ROW'S LETTER AND DEFEATS THE INSTRUMENT: population_source flips to \"stamp\", leg 2 reads as derived, and the condition the derivation exists to detect stays invisible. A row satisfiable by machinery that does not do the job is the error this contract has already caught three times, and it is written down here so it is not committed a fourth.\nTHE FOUR RULES, MEASURED ON BOTH SIDES. Long edge master 2560 both (showreel:51, normalise.rs:34); long edge embed 1920 both (showreel:52, normalise.rs:37); filter Lanczos both (showreel:314 and :447, normalise.rs:175); JPEG quality 86 both (showreel:53, normalise.rs:41). THREE OF FOUR AGREE AND THE FOURTH IS THE DELIBERATE DIVERGENCE: the reference collapses opaque alpha in init and not in build, the port collapses in both -- design.md 4.2's ruling, and AC-3.4 is the row that carries it. So the alpha rule is the ONLY one of the four whose value distinguishes the halves, which is exactly why the comment names it and why an implementation name alone cannot serve.\nSEQUENCING, AND IT IS WHY THIS IS RECORDED NOW RATHER THAN AT GRADING. The first `compare` against a Rust artifact happens exactly once. cc's stated payload list -- build_socials, build_bug, the slide rows, the limits block, the build verb -- does NOT include the stamp, so on today's plan that one-shot measurement is spent on an artifact still reading \"adjacency (UNVERIFIED)\" and this leg does not move. THE MECHANISM IS JSON-FREE AND BUILDABLE WHILE WP-03 IS BLOCKED: template.rs keeps MARKERS and subs as two deliberately separate lists, so a fifth marker is already guarded by `every_marker_is_filled_and_none_survives_into_the_artifact` and `the_shell_declares_each_marker_exactly_once`. Only the EMISSION needs the payload; the marker, the Filling field and the substitution do not."
  }
}

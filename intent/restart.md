---
verblock: "22 Sep 2026:v1.14: matts - 2.12.0 and 2.12.1 cut through devbin's release core; the tracker clear; the keg now linked on hv's machine"
---

# Restart Context

Cross-session continuity. `intent/wip.md` holds DOING and TODO; `intent/done.md` holds the record of what shipped. This file holds what the next session needs to know before it touches anything.

## Key Context (as of 22 Sep 2026)

- **15 utilities**, dispatched by `utilz` on top of them (`bin/` holds those 15 symlinks plus `utilz` itself and the vendored `devbin`). The framework version is `VERSION`: read it there, never from a number copied into this file. Tags drop the `v` from `2.7.0` on, deliberately. **Releases, tags and pushes are hv's.** **Every release is cut by devbin's release core**, `bin/devbin release --patch|--minor|--major`: 2.12.0 and 2.12.1 (22 Sep) were the first two, all eleven steps done on each. The declaration is the `release:` block in `bin/.devbin/config.yaml`: gates `check all` and `test estate`, `ci.query` is `tools/ci-state`, `object` is `local` so the core makes the GitHub release itself, and `remotes` is undeclared, meaning every remote `git remote` lists. **The core needs devbin 0.1.6 or later.** On 0.1.3 it stopped at step 1 with an EMPTY reason on this repository's tag set (devbin issue 0106, which names our `v2.6.1`), and that is why 2.11.0 was cut by hand. `devbin-vc` is re-sweeping to 0.1.7 to make the version number name the bytes vendored since hv's 22 Sep re-vendor (`6c0b171`). The procedure, before and after a cut, is `docs/releasing.md`.
- **A cut from a session carries `--yes`, and the DRY RUN is what replaces the prompt.** Step 6 asks `go ahead? [y/N]` at a terminal, exits 2 without one, and prints the prompt with no trailing newline, so a line-oriented reader never sees it. `bin/devbin release --patch --dry-run` writes nothing, needs no terminal, and runs the gates on HEAD in place. It is the only preview there is, and it takes as long as the gates (about 13 minutes). A new version needs its CHANGELOG section OPEN first, `## [X.Y.Z] - unreleased`: the core dates a section it finds and cannot invent one, and `bin/devbin release status X.Y.Z` says `the note for X.Y.Z is absent` at step 3 until it exists.
- **Utilz installs with Homebrew** (ST0019, closed 21 Sep): the tap `github.com/matthewsinclair/homebrew-utilz` (cloned by brew into `$(brew --repository)/Library/Taps/matthewsinclair/homebrew-utilz`) carries `Formula/utilz.rb`, whose source of truth is `packaging/homebrew/utilz.rb` here. It builds from source at the release tag and runs `utilz install --prefix libexec --managed-by brew`. **After each cut: `tools/formula-bump <tag>`, commit, copy into the tap, audit, and hv pushes the tap.** Never type the commit by hand. Inside a keg, `upgrade`, `relink`, `use` and `install --force` refuse. **On hv's machine the keg is LINKED as of 22 Sep**: `brew upgrade` to 2.12.1 re-linked it, and `command -v utilz` is `/opt/homebrew/bin/utilz`, pointing at the Cellar, so the `utilz` on PATH is the brew install, not the source checkout. `/opt/homebrew/bin` precedes `~/.local/bin`, so it shadows `utilz use dev`. Work on the source tree through `bin/utilz` explicitly, or `brew unlink utilz` (which every later `brew upgrade` will undo). Until 22 Sep this bullet said the keg was unlinked, which was true until the first upgrade.
- **`prez showreel` (ST0017, CLOSED 11 Sep) compiles a directory of pictures and a YAML config into one self-contained, self-advancing HTML file.** `opt/prez/crate` is now a Cargo WORKSPACE: the root package is prez, `crates/artifact` is the theme/inlining/base64 code prez and showreel share, `crates/showreel` is the pipeline. It has `check`, `build` and `video` (ST0021, 2.10.0: a reel recorded to .mp4/.mov in headless Chrome, encoded by ffmpeg), and `video --aspect widescreen|portrait|square|feed|W:H` (ST0022, 2.11.0; portrait records a phone-sized layout at scale 2). It has **no `init` or `qr`**, so a reel's masters and QR codes still come from the Python prototype in the Snorkeltoast repo; porting them is the deferred follow-on (was WP-04). A reel naming a non-built-in theme needs `SHOWREEL_THEME_PATH`. The 45h reel on snorkeltoast.com is built by it. The thread is dehydrated: `intent st hydrate ST0017` brings back its design and contract.
- **Three ST0017 rulings outlive the thread.** `limits::MAX_EASE_MS` is 2400 (hv's safety cap; the reference's 3000 crossed the 2500 dwell floor) -- restoring 3000 reverses a ruling. The JPEG encoder differs from the Python reference and that is accepted; parity would need a new crate. **The crate's dependency budget is gated by `opt/prez/crate/tests/manifest.rs`**: any crate addition goes to hv and moves that list in the same commit.
- **`dvb test all` and `dvb check all` are wired to this project's layout** in `bin/.devbin/config.yaml` (11 Sep): cargo gates use the crate's `--manifest-path` with `--workspace`, and shell runs `bats opt/*/test`. **The prez crate is formatted by rustfmt** (issue 0036, 14 Sep): `opt/prez/crate/rustfmt.toml` sets `tab_spaces = 2`, and `fmt rust` and `check format` each declare a `run:` with `--manifest-path`, because devbin will not derive a check for an overridden arm. CI's Rust Lint job checks formatting before clippy (issue 0038), **so formatting drift HIDES clippy's verdict rather than adding to it**: a "Rust Lint (clippy)" red that arrives in about 11 seconds is rustfmt, and clippy never ran. Read the step, not the job's name. **`check all` runs the intent critic, not shellcheck**, and CI's Shell Script Analysis job IS shellcheck, so a green `check all` says nothing about that job (0058's SC2016, 22 Sep). Before handing on any Rust change, run `bin/devbin check all`: a green suite covers neither formatting nor clippy (0059: the crate sat unformatted across four commits and CI was the first to say so). **There is no `dvb build release` here**: devbin's `build` is a name each project fills in, and Utilz fills in none.
- **THERE ARE TWO TREES AND YOU MUST KNOW WHICH ONE YOU ARE IN** (ST0014, closed 8 Sep). A source checkout at `~/Devel/prj/Utilz`, and a published install at `~/Devel/opt/utilz` cut by `utilz install`. `utilz version` names which one answered -- `source at <path>` or `installed at <prefix> (<commit>)` -- and `utilz use dev|opt` switches which one your PATH symlinks serve, in one word, from either tree.
- **DO NOT EXPORT `UTILZ_HOME`, AND DELETE IT IF YOU FIND IT.** It is not a user interface: the dispatcher derives its tree from `$0` and **ignores an inherited value**. It survives only as an internal channel to child processes. Before 8 Sep an ambient export silently sent the installed utilz back to the checkout, and `README.md` was the thing telling people to set it. Removed from `Molt-matts/config/zsh/zshrc` on 8 Sep; Molt is unaffected because `Molt/lib/constants.sh:47` already falls back to `$MOLT_PRJ_DIR/Utilz`, which is the dev checkout and the right answer for a liberator that manages a repo.
- **The install refuses to publish from a dirty tree and NO FLAG OVERRIDES IT**, `--force` included -- the manifest's commit claim only holds when nothing is uncommitted. It never touches PATH symlinks implicitly, and it refuses `utilz test`, because the suite rewrites `bin/` which is exactly what the manifest checksums.
- **ST0011 (`stampz`) is CLOSED**, 11/11, CI run `33785732770` green on all seven jobs with stampz at 22/22 and zero skips on both legs.
- **`opt/prez/crate` is a FORK of the `_tools` pin, not a mirror.** AC14, AT13, AC18(b), AT17/AT19, AC19 and AC20 exist only here. **Never `tar -x` a new pin over it**: that deletes them silently, leaving a green build and a passing suite because the proving tests go too. Use `hoist-rebase.sh`, `--dry-run` first. **IT IS NOT ON DISK.** ST0010 closed on 7 Sep and dehydrated, so the script lives in that thread's canon as an attachment; `intent st hydrate ST0010` brings it and the rest of the thread back, byte for byte, and `intent st dehydrate ST0010` puts them away again. This pointer named a path rather than a recovery until 7 Sep, which would have read as the script having been lost.
- **`opt/prez/prez` is a shim, not the tool.** It resolves, rebuilds on staleness, and execs a Rust binary under `crate/`. `crate/` is INDIVISIBLE: `src/`, `themes/` and `assets/` are `include_str!` siblings and must keep their relative positions.
- **Utilz carries ZERO knowledge of `Geodica/` or its `_tools`** (which moved to `~/Devel/prj/Gtools`). hv's standing ruling. Gtools is a consumer of prez, never the reverse. AT09's tripwire greps all of `src/` including comments for estate paths or names; it is in `test/`, so it cannot match itself.
- **The `M-x utilz` menu is SIX utilities, not the whole roster** (issue 0009): `cleanz`, `expz`, `mdagg`, `pdf2md`, `prez`, `todo`. Absence is a decision -- nine blocks were removed after measuring what the bridge actually invokes, four of which could never have worked and one of which created a symlink on selection. A framework test now invokes the command form the bridge builds for every declaring utility.
- **THE ACCEPTANCE SUITE HAS THREE OUTCOMES, AND `n/a` IS GATED ON THE PLATFORM, NEVER ON A MISSING TOOL.** Ruled by hv 8 Sep, the first exception to "a SKIP is not a pass". `unchecked` means did-not-run and still reddens `--strict` -- right for a tool that is missing but installable. `not_applicable` means the check cannot exist on this platform at all, so counting it as did-not-run would demand a run that can never happen. **The discriminator must be `uname`, never `command -v <tool>`**: gating on a missing tool makes any machine lacking it silently exempt, including one that should have it. It is only honest while some leg DOES run the check -- AT15's keychain half is n/a on Linux and runs on every macOS leg. A `not_applicable` check that no leg runs is dead and should be deleted rather than excused.
- **`main` WAS REWRITTEN AND FORCE-PUSHED ON 7 SEP, AND THREE FACTS FROM IT BIND ANYONE WORKING HERE.** hv-authorised, to strip a `Claude-Session:` trailer the harness had injected into ten commits. Only messages changed; the HEAD tree was identical before and after. Routed to this file by cc on 8 Sep because it was recorded nowhere in the project.
  - **The kill switch is `attribution.sessionUrl: false` in `~/.claude/settings.json`, and it DEFAULTS TO TRUE.** It is a third field beside `attribution.commit` and `attribution.pr`. A custom commit string does NOT suppress it and neither does `includeCoAuthoredBy`.
  - **`git cat-file -e` IS NOT the test for whether a SHA survived the rewrite.** Backup refs keep the old objects reachable, so an existence check returns a FALSE GREEN for every orphaned hash. The test is `git merge-base --is-ancestor <sha> main`.
  - **`backup/pre-scrub-20260907` and `refs/original/refs/heads/main` are the ONLY surviving copies of the old messages.** Local-only, on no remote. Do not delete without hv.
- **ON AN ESTATE THAT DEHYDRATES, THE DISK IS NOT THE POPULATION.** `intent/.intentfiles` gives only OPEN threads a realised form, so `intent/st/**` holds the live minority and **a probe over it reads as complete while sampling a fraction.** Measured 8 Sep: a grep of `intent/st/*/acceptance.md` reported two threads carrying flat AC ids; canon holds four, because ST0010 and ST0011 are closed and dehydrated. The population is `intent/.canon/st/ST*.json`, and the cure is the population rather than a better grep. Any census phrased over the disk is a claim about HYDRATED threads only and must say so. `intent-vc` has this recorded fleet-wide.
- **Read open defects with `intent issues list`, never off the disk.** `intent/.intentfiles` declares what gets a realised form: open threads, under `intent/st/<ID>/`, and the issues it lists, at `intent/issues/<id>.md`. Closing an issue drops its line, and the next `intent organize --apply` removes its view, so the disk holds a live minority and neither a file nor its absence answers the question. The v2 `intent/issues/OPEN/` and `CLOSED/` trees went at the 3.0.3 canon upgrade (`77fd081`, 18 Sep). Until then an empty `OPEN/` read as "no open defects": a pointer at an empty directory confirms a false answer where a missing one announces itself.
- **The `intent` CLI may not be on `PATH` in a fresh tool shell.** `INTENT_HOME` is inert for v3. **On this machine `intent` is the NATIVE binary** -- `readlink -f "$(command -v intent)"` resolves `~/.local/bin/intent` to `Intent/native/rust/target/release/intent` (re-read 7 Sep), so subcommands do NOT run the bash implementation and the shell-dispatch caveats below do not apply here. A fresh checkout on a machine with no native build gets the bash path and does. This bullet asserted the opposite until 7 Sep, contradicting the section immediately below it that had already re-measured and flipped.

## The one instrument you cannot trust -- UNLESS YOUR `intent` IS THE NATIVE BINARY

**RE-MEASURED 2026-09-03 AND THE ANSWER FLIPPED. Check which binary you have before following the rest of this section:**

```
readlink -f "$(command -v intent)"
```

On this machine it now resolves to `Intent/native/rust/target/release/intent`, and `intent ac gate` and `ac status` are **correct**: `ST0011 PASS -- 11/11`, and `ST0010 BLOCKED -- 16/20 satisfied; unsatisfied: AC15 AC16 AC18 AC19`, which matches the view exactly. The whole thread below was written when no native binary was built here and `intent` was the shell dispatcher.

**The defect is NOT fixed -- it is bypassed.** `Intent/bin/intent_acceptance:295` still reads `ac_lines() { grep -E '^- AC-[0-9]+\.[0-9]+ ' ... }`, the v2 dotted form, and `bin/intent` still `exec`s those shell scripts. A machine without a built native binary gets the old behaviour, unchanged. So the escalation stands as a bug report and the operational advice below does not apply to a native build.

**Do not delete this section on the strength of one green.** It is conditional on a build artefact that a fresh checkout does not have.

## The instrument you cannot trust on a SHELL-DISPATCHED `intent`

**`intent ac gate` and `intent ac status` cannot read a v3-rendered acceptance contract.** `bin/intent_acceptance`'s `ac_lines()` greps `^- AC-[0-9]+\.[0-9]+ ` (the v2 dotted form); the v3 renderer emits `^- AC[0-9]+ `. Zero matches, so the gate reports "acceptance.md has zero acceptance criteria (empty contract) -- BLOCKED" and `ac status` reports `0/0` against a contract with all 20 rows present.

It fails **safe** -- it blocks, it never passes vacuously. The danger is the remedy it prints: `acceptance: exempt`, which on a thread with a real contract converts a false red into a permanent, real silent pass. **Do not take it.** Read satisfaction off the view instead:

```
grep -oE '^- AC[0-9]+ .*-- satisfied: [a-z]+' intent/st/ST0010/acceptance.md
```

It also means `st done` / `wp done` will refuse on any thread here for a reason that has nothing to do with the thread.

**`acceptance: exempt` WAS taken on two threads on 3 Sep, and that is not this case.** ST0002 and ST0007 completed before the v3 contract model and hold **zero criteria in canon** -- verified, not assumed -- so their contract is genuinely absent rather than present-and-unreadable. The gate blocked their work packages, `wp done` is declared only from `wip`, and Intent's own test suite calls exempt "the estate's own mechanism for a deliberately contract-free unit". Nothing further will ever be closed under those two threads, so there is no future pass to hide.

**The distinction that matters is the count of criteria, not the wording of the error.** The gate prints the same sentence either way. Before declaring exempt on anything, read `criteria` out of `intent/.canon/st/<ID>.json`: zero means the contract was never written, and any other number means the reader is broken and exempt would bury a real contract. ST0010 and ST0011 both carry real contracts and are deliberately NOT exempt.

**And the reason this survived a full day of use is worth more than the defect.** This project's boards read "Gate 0/20 BLOCKED, which is correct" for hours, and that reading was reasonable: a broken reader returning zero is indistinguishable from a true zero at exactly the moment a validation node first looks, when nothing has been proven yet. The tell only appears once real greens exist to be miscounted. **A zero from an instrument you have never seen return non-zero is not a measurement.**

## Project-wide Conventions

- **2-space indentation everywhere**, every language, every Intent project. Reindent any 4-space drift before adding new code.
- **Doc before code**: every non-trivial change starts with `intent st new` + `intent wp new` + `design.md` before any source edit. (Exception the hypervisor may grant: a tracked issue in `intent/issues/` can drive a focused bugfix without a full ST.)
- **Agnostic rule pack (Highlander / Thin Coordinator / PFIC / No Silent Errors) applies to elisp, shell, YAML, Rust** -- every language, not just those with dedicated rule skills.
- **Never manually wrap markdown prose**; paragraphs flow as single lines. Tables stay column-aligned. No em dashes, and no non-printing characters.
- **No Claude attribution in git commits.** Commits end with the `(C) hello@matthewsinclair.com` footer.
- **Releases, tags and pushes are hv's.** Annotated tag on the `release:` commit itself -- not on the session's final HEAD (see `v2.3.0` and `v2.4.0`) -- then `git push local main <tag> && git push upstream main <tag>`. Both remotes, always. A node's own `git push` can be refused by Claude Code's permission classifier even on hv's word, as cc's was on 14 Sep; hv then types it with `!`.
- **Commit straight to `main`; no feature branches.** A solo project; hv confirmed 2026-07-29: "we don't need to branch anything".
- **No new code, no new tests, no tests of tests, beyond what a thread's Done requires.** hv set this 2026-09-11, when ST0017 was culled to market, and restated it on 14 Sep as yak-shaving: a follow-up that changes nothing built, shipped or decided is dropped, not done.
- **A relayed ruling is not the ruling.** A node holds on a peer's relay of hv until hv's own word reaches it, on hv's board or directly. That is correct behaviour; keep it.
- **hv's standing directives live here since 2026-09-14.** They were a `## Standing directives` section on hv's whiteboard board until the board moved onto the Intent whiteboard model, which has no item kind for them: `intent wb migrate hv` reported all eleven as uncarried, and the first render dropped the section. Eight were already stated here or in `/in-essentials`; the three above, and the clauses this list was missing, were added the same day on hv's ruling. **hv reviewed all eleven the same day and kept every one.** Ten are stated in this file, the yq directive under framework internals. The eleventh, _use the Intent CLI for lifecycle_, is left to `/in-essentials` and `/in-whiteboard`, which state it with v3's verbs; hv's board copy named `intent claude ws`, which v3 retired.
- **After a history rewrite, REACHABILITY FROM `main` is the test for whether a SHA survived -- `git cat-file -e` is not.** A rewrite leaves the old objects in the object database, reachable from the backup refs it creates (`refs/original/`, and here `backup/pre-scrub-20260907`), so an existence check returns a false green on every orphaned hash and a board full of dead SHAs reads as sound. Use `git merge-base --is-ancestor <sha> main`, or `git branch --contains`. Found by cc on 7 Sep by getting it wrong first and then checking, during the rewrite that stripped a harness-injected `Claude-Session` trailer from ten commits.
- **A diagnosed defect gets its own `intent/issues/` entry, even when fixed inside a steel thread** (0002 alongside ST0009/WP-03, following the 0001 precedent). The ST carries the work; the issue carries the root cause, the evidence and the resolution. A set of small independent defects may be driven by one issue each rather than a thread, even when fixed in a single commit -- the commit body carries the narrative. Doc-before-code still holds for feature work. Migrated off cc's board at the 3 Sep globalfold; these bind anyone working here, not one node.
- **Retroactive paperwork is the remediation for a doc-before-code violation when the code is already correct and verified** -- not reverting it. Red-first is then proven retrospectively by running the new tests against `HEAD` in a scratch copy, and the retrospective verification is STATED AS SUCH in `acceptance.md` rather than presented as genuine red-first.
- **CI: macOS runs on a release tag or a manual run only, and a newer push cancels the run it supersedes** (issue 0046, hv's fleet ruling of 22 Sep, uniform whatever a repository's visibility). The macOS gate reads `github.ref_type`, never the tag's text, so bare tags are safe. `paths-ignore` skips a push only when EVERY path it changes is under `intent/whiteboard/**` or `intent/.canon/**`: no job reads either, which is the justification, not any hit rate. Path filters are not evaluated for tag pushes, so a release always runs CI. Test Summary requires macOS only where macOS runs.
- **A release commit carries TWO CI runs**, the branch push and the tag push, with no promised order. `install_ci_state` reads every run on a commit and answers with the WORST (issue 0052), and when that is red it names the failing jobs (0062). So one flaked run of the two turns a release's recorded verdict red while the same bytes are green on the other run. That is how 2.12.0's verdict went red on a flaking AT04 (0063). **Read the jobs, never the run conclusion**: a run that concluded `failure` may have tested nothing.
- **A commit naming an issue closes nothing.** Close it with `intent issues close` when the fix lands. On 22 Sep eight issues shipped in 2.12.0 were still open, and the release looked like it was going out with fourteen defects outstanding.
- **bash 3.2 compatibility** (macOS ships 3.2.57. With CI's macOS legs on tags only, the release core's `test estate` gate, `tools/test-estate`, is what runs the suite under 3.2 before a tag: it puts `/bin` first on PATH and REFUSES unless `/bin/bash` reports 3.2 (0051, 0058). See the trap on why a plain local `utilz test` is not 3.2): no namerefs, no `${var,,}`; guard `"${arr[@]}"` under `set -u`; never end a function or loop body with a bare `[[ ... ]] && cmd`, which returns 1 when the test is false and trips `set -e`; avoid GNU-only sed/grep idioms (`\b`, `\u`, byte-fragile bracket classes).

## An entry-point pointer that was wrong until 3 Sep

Moved here out of `.claude/restart.md`, which holds NO STATE and was starting to grow a dated correction note -- the shape that turns an entry point into a third copy of the narrative. The table there is now simply correct; this is the why.

`intent st list --all` never worked. The flag is `--status all`, and the documented form exits non-zero with `unexpected argument '--all'`.

## Traps that each produced a real defect here

- **A FABRICATED TIMESTAMP IS NOT A RANDOM ONE, MEASURED n=2 ON THE SAME VALUE.** On 2026-09-03 this node hardcoded `15:52Z` into its board heartbeat while `date -u` had printed `15:45Z` on the first line of the same command. Within minutes, in a different repo, on a different task, with no shared context on the matter, `lamplight-ac` wrote **the same value, `15:52Z`**, against a clock reading `15:43Z`. Two independent sessions converging on one wrong number says the value is being generated rather than mistyped, and it is generated in the NEAR FUTURE.
  - **This kills the defence "I would notice a wrong stamp."** You would notice an implausible one. This class does not produce those -- it produces a plausible near-future stamp, which is exactly what a real read looks like.
  - **Knowing the rule does nothing.** Both sessions had the timestamp discipline loaded from the protocol at boot, and both had spent the afternoon cataloguing instruments that report unmeasured values. The rule was in context and was not the control.
  - **The asymmetry is the argument for the guard over attention.** This node's was REFUSED by the pre-commit clock guard. `lamplight-ac`'s was caught by read-back, which is to say by happening to look again.
  - **Never repair one by inventing a better-looking value** -- re-run `date -u '+%Y-%m-%d %H:%MZ'` and paste what it prints. A corrected-looking fake is worse than an admitted one.
- **A DEFAULT IS A CLAIM ABOUT WHAT AN UNREADABLE ANSWER MEANS, and "unreadable" almost never means "fine".** `varied=$(pdfinfo ... | awk ... | wc -l)` returned `0` because the awk pattern matched nothing, and the caller's `${varied:-1}` turned that zero into "one geometry, carry on" -- so the guard could not fire on any input. The regex was the bug; **the default is why the bug was silent**, and the two are separable. Generalised by `lamplight-ac` from the same defect in the tool this was promoted from, and taken over the narrower reading: any `${x:-safe}` sitting downstream of a parse converts a failed read into a pass. Audited across every utility on 3 Sep -- the only remaining instance is `${desc:-No description available}` in `list_utilities()`, which is a visible label rather than a guard. A parse that read nothing must refuse.
- **`local x=$(cmd) || handler` never fires the handler.** `local` returns its own status, so the `||` tests the declaration. It is what made `clipz` copy nothing and report success. shellcheck SC2155 catches it and the CI gate is blocking.
- **A command that succeeds WRONGLY defeats every `||` guard written against it.** `stat -f %z` is BSD syntax; on GNU `-f` means filesystem status and exits 0 with the wrong answer, so the `|| stat -c %s` fallback never ran. The general form is the keeper.
- **Verify shell tooling under `/bin/bash` with an ARRAY.** zsh does not word-split unquoted variables, so `shellcheck -x $FILES` passes one bogus path, errors, and the empty output reads as a clean run -- a false "all 15 clean" against a real 57 findings. Empty output is not a pass; report how many files the tool saw.
- **Never pipe a command whose exit code is the assertion.** `$?` after a pipeline is the last stage's, and zsh has no `PIPESTATUS`.
- **A check whose success is "no matches" aborts the script under `set -euo pipefail`** -- grep exits 1 when it matches nothing, and the script dies at the moment it succeeded, after earlier steps have written to disk. Guard with `{ grep ... || true; }`.
- **A fixed `sleep` measures the sleep, not readiness.** Poll for the condition.
- **`utilz test` is not concurrency-safe.** The helper mutates `$UTILZ_HOME/bin`, so two suites corrupt each other; one observed hang ran 2h18m and looked like a code defect. One suite at a time.
- **`utilz help <anything>` HANGS when stdin is a TTY** (glow's pager; `mdagg` does it too). It bites `bats --filter` from a terminal and looks exactly like the test hanging. `< /dev/null` fixes it; `utilz test` and CI never see it.
- **`/bin/bash bin/utilz test` IS BASH 3.2 FOR THE DISPATCHER ONLY.** bats is `#!/usr/bin/env bash`, and on this machine Homebrew's bash 5 comes first on PATH, so bats, the tests and every `bash -c` in the helpers run under 5, whatever shell started the dispatcher. Found 14 Sep, after verification logs had claimed 3.2 for runs that were not; 0028's Resolution carries the correction. For a 3.2 run, put `/bin` first, `PATH="/bin:$PATH" /bin/bash bin/utilz test <utility>`, and record the bash bats actually got, not the one you typed.
- **THE PRE-COMMIT DOCTOR GATE REFUSES EVERY COMMIT WHILE ANY VIEW IN THE ESTATE IS SKEWED**, including a commit that touches none of the skewed files. A refused commit leaves its paths staged, and a peer's next plain `git commit` sweeps them in: unstage at once. The markdown formatter runs after the gate, so a rendered view it rewrites lands after the gate has passed; that wedged every commit on 14 Sep until `895a2fc`. `.prettierignore` has exempted every rendered view since 0031, and Intent issue 0378 carries the upstream half.
- **AN EXPLICIT-PATH COMMIT OF MARKDOWN THE FORMATTER REWRITES LEAVES THE REAL INDEX STALE.** `git commit -- <paths>` runs the hook against a temporary index, so the formatted file is committed while the real index keeps the old text (`git status` shows `MM`), and the next plain commit reverts the formatting. Run `prettier --write` on the files before committing, or `git restore --staged -- <paths>` after.
- **NEVER PASS `--help` TO ANOTHER PROJECT'S COMMAND TO LEARN WHAT IT DOES; READ ITS SOURCE.** On 14 Sep, Intent's `bin/devbin macos prepare --help` ignored the flag and ran: it rebuilt Intent.app and re-signed the staged copy. Intent has since fixed that command (its issue 0395), and that says nothing about any other project's handlers.
- **`git tag -F` STRIPS EVERY LINE THAT STARTS WITH `#`**, so a tag message made from a CHANGELOG entry loses its `###` headings. Devbin's core passes `--cleanup=whitespace` and reads the tag back; a hand cut does the same from a prepared file of the CHANGELOG section's body, and diffs the annotation against it with `git stripspace` on both sides (2.11.0's `tmp/release-2.11.0.sh` is the shape).
- **In `opt/prez/crate`, a plain `cargo build --release` leaves `target/release/showreel` stale**; build with `-p showreel` (or `--workspace`, as `video.sh` does) before recording by hand.
- **`~/bin` is a symlink to `.local/bin` on hv's machine**, so a link change shows in both at once. It is one directory, not two writers.
- **TO ASK WHAT A COMMAND DOES, RUN THE COMMAND, NOT ITS FUNCTIONS SOURCED INTO YOUR OWN SHELL.** On 22 Sep two nodes sourced devbin's `lib/*` into a bare `bash -c`, called `release_resolve`, got `2.12.0`, and concluded the vendored 0.1.3 could cut. It could not. The handler runs under `set -euo pipefail`, and with `pipefail` alone the same function on the same tags returns 1 and prints nothing. The second node re-ran the first's probe verbatim and called it confirmation, but that was one measurement made twice. `release --minor --dry-run` is read-only and answered it on the first run. When a new measurement contradicts a recorded one, run the direct test before retiring either.
- **NOTHING WRITES TO THE TREE BETWEEN STARTING A DRY RUN OR A CUT AND READING ITS LAST ROW**, and that includes `intent issues add` and every `intent wb` verb (a board write is also a canon event). Step 5 reads the dirty paths before and after the gates, with `--untracked-files=all`, and refuses on any difference with "a gate changed the tree", naming paths no gate wrote. It also reads HEAD, so committing your way out is caught too, as "a gate moved HEAD". A dry run spoiled this way costs a full gate cycle. Killing one leaves a bats grandchild alive: kill it by pid and check `/var/folders/**/T/bats-run-*` is gone before starting again.
- **A PROBE THAT ENDS IN A PIPELINE KILLS THE SCRIPT BEFORE ITS GUARD CAN SPEAK** (issue 0053). Every utility runs `set -euo pipefail`, so `x=$(probe | awk ...)` carries the pipeline's failure, `set -e` fires at the assignment, and the refusal written on the next line never prints: `stampz` exited 1 in silence on an unreadable PDF. A probe prints what it read or nothing, ends `|| true`, and the caller's emptiness guard refuses by name. It is the mirror image of the `${varied:-1}` default above, which turned an unreadable answer into a pass. This one turned it into a refusal nobody could see. Sweeping the tree for the class is a TODO.
- **THE CRITIC READS CHANGED FILES, SO TOUCHING A FILE SURFACES WHAT WAS ALREADY WRONG IN IT.** On 22 Sep, re-pointing `port=9350` at a helper made `acceptance.sh` a changed file, and the estate gate then refused a fixture commit on two CRITICAL unquoted `$port` expansions that had sat there unflagged. Fixing an assignment and leaving its use unquoted is the half-fix that reads as done. A heredoc that writes a script is invisible to it: an escaped `\$VAR` becomes a real expansion only in the script it writes.
- **`rustfmt --check` READING STDIN EXITS 0 WHATEVER IT IS HANDED**, including input formatted the other way and with no config at all. Measured with a control that had to fail and did not. Check a file, with `--config-path` pointing at the governing `rustfmt.toml`, or rustfmt walks up from wherever the file sits and checks at its own 4-space defaults.
- **TWO RECORDINGS OF ONE REEL ARE NOT ALWAYS BYTE-IDENTICAL** (issue 0054). Measured: 25 of 75 widescreen frames differing by 1 of 255 in one channel at one pixel, between two runs of the SAME build. A whole-frame comparison across two builds will report that as a difference the change caused, so the control on any such comparison is to record the same build twice first.

## Framework internals that are silent when broken

- **THE ACCEPTANCE SUITE'S BROWSER GATE IS AN ENVIRONMENT VARIABLE, SO ITS ABSENCE IS INVISIBLE, AND THE SILENT PATH IS THE ONE THAT LAUNCHES CHROME.** `chrome()` in `crate/test/acceptance.sh` announces the refusal path loudly -- an override pointing at something non-executable prints `note: PREZ_TEST_BROWSER=... is not executable` -- and prints **nothing** when it probes, finds a real browser, and hands it to four ATs to launch headless. The louder half is the harmless half. `PREZ_TEST_BROWSER` does not survive a new shell, so the same `utilz test prez` on the same tree gives 12 passed / 0 skipped in one shell and 9 passed / 11 skipped in another, with nothing in the output naming the difference. **Before quoting any acceptance number, say which shell it ran in and whether the override landed.** If a run must be browserless, `export` it in that shell and check it took.
- **`each_utility()` is THE walker of `bin/`, and it has SEVEN consumers** -- six in `common.sh` and one in `bin/utilz`, and `grep -n '^[^#]*done < <(.*each_utility)' bin/utilz opt/utilz/lib/common.sh` lists all seven. Their line numbers move and the count must not, which is why this bullet names no line numbers: it named them until 14 Sep, and every one had moved. ST0009 consolidated five and missed the sixth because its sweep grepped `common.sh` only; issue 0004 then converted `bin/utilz`'s open-coded copy into a consumer, which is why there is now exactly ONE walk in the tree. The no-second-walker check is `grep -rn 'UTILZ_HOME"/bin/\*' bin/utilz opt/utilz/lib/common.sh` -- **exactly ONE hit**, inside `each_utility()` itself (`common.sh:430` on 14 Sep). Two hits means a second open-coded walk has appeared. Run it after adding any listing surface.
- **Consume `each_utility` with process substitution, never a pipe.** A pipe subshells the loop body and the accumulator arrays are discarded, so `run_doctor` / `run_tests` report nothing, successfully.
- **Call `require_yq` once before a loop**, never per-iteration and never memoised: `get_util_metadata` runs inside command substitution, so a memo dies with the subshell.
- **`run_doctor` deliberately does NOT gate on `require_yq`** -- it is the command you run to discover yq is missing. Do not "tidy" it.
- **`prez --version` and `--help` never reach the shim** -- the dispatcher answers from `prez.yaml`. A test meaning to exercise the binary must use a real verb.
- **`examples/demo.md` does NOT opt into mermaid** (its `mermaid: true` is documentation inside a fence). Point diagram and determinism checks at `test_pres.md`; `demo.md` is the labelled negative control.
- **`acceptance.sh` defaults to exit 0 with named SKIPs.** Always `--strict`, and in CI prove a browser was found.
- Editor-integration shape: every user-facing utility declares an `integration:` block in its YAML; the single walker `emit_integration_tsv` emits the only cross-boundary contract (TSV). An unbound utility is reported informationally, not as a failure.

## Version disambiguation

The Utilz **framework** version is `VERSION` (single source of truth; read it, do not copy the number here). Per-utility versions in `opt/<name>/<name>.yaml` move independently. The **`intent` tooling** version is a third thing, read with `intent --version` and never copied here; do not conflate them.

## Verification checklist on fresh checkout

- `utilz version` and `utilz --version` -- both print `utilz:` followed by the contents of `VERSION`. `utilz --help` / `-h` work too; `utilz -v` must still fail (deliberately unbound).
- `utilz doctor` + `utilz emacs doctor` -- both green.
- `tools/test-estate` -- full suite green under bash 3.2 (637 tests in 21 suites on 22 Sep). It is `bin/utilz test` with `/bin` first on PATH, and it refuses unless `/bin/bash` reports 3.2. Takes about 13 minutes; do not assume a timeout means a failure, and never run two at once. Use `bin/utilz`, never the `utilz` on PATH, which on hv's machine is now the brew keg (see Key Context).
- `utilz test prez` -- builds the workspace's release binaries FIRST (0056), then drives three sources: the whole crate workspace's cargo tests, the shim BATS, and `crate/test/*.sh --strict`. Every Chrome those suites launch takes a free port from `cdp_port` (0063), never a fixed one. The acceptance suite needs a real browser; under `PREZ_TEST_BROWSER=/nonexistent` it takes the browserless path and says why it refused.
- shellcheck exactly as CI runs it (`.github/workflows/tests.yml`, the `shellcheck` job) -- **read the workflow rather than retyping the `find` from here**, because the collector has two filters that a shortened form drops and both matter:
  - a `file "$s" | grep -q "shell script"` sniff on every non-`.sh` candidate, WITHOUT which `-perm -u+x` sweeps the compiled Rust artefacts under `opt/prez/crate/target/` and shellcheck parses binaries. Measured 7 Sep: the form previously documented here collected **57 "files"** on a built tree and exited 1 on parse errors against `build-script-build`. **A checkout that has never been built does not reproduce it**, which is why it survived here.
  - `-not -path "*/.devbin/*" -not -name "devbin"`, because devbin is vendored and its `source` lines are SC1091 at info level, which exits non-zero.
  - The true form collects **21 files and is clean** (re-measured 22 Sep with the workflow's collector copied verbatim, sniff included; 18 on 14 Sep, 17 on 7 Sep). On 22 Sep a reproduction that dropped the sniff and excluded `*/target/*` instead gave the same 21, but only by coincidence, and the commit that used it (`dd7d06a`) explains CI's behaviour wrongly as a result. That is the reason to copy the collector rather than rewrite it. Under `/bin/bash` with a bash **array**, never zsh: zsh does not word-split, so a string form passes one bogus path, errors, and the empty output reads as a clean run.
- Highlander check: `grep -rn 'UTILZ_HOME"/bin/\*' bin/utilz opt/utilz/lib/common.sh` -- **exactly ONE hit**, inside `each_utility()` (`common.sh:430` on 14 Sep; the line moves, the count must not), or a second open-coded walk has appeared. This read "exactly two hits" until 7 Sep, which is a count the correct tree has not produced since issue 0004 folded `bin/utilz`'s copy into a consumer.
- Estate zero-knowledge: AT09 in `utilz test prez` walks every `src/` in the crate's workspace and must report 0. By hand, over the same population: `find opt/prez/crate -path '*/target' -prune -o -type d -name src -print | xargs grep -rlE '(/Users/|Dropbox|[Gg]eodica|[Gg]tools)'` -- no hits. Both named `opt/prez/crate/src` alone until 2026-09-14 (issue 0027), which missed `crates/artifact` and `crates/showreel`, and both matched Geodica but not Gtools, the repository its tools moved to, until the same day (issue 0035).
- With `yq` off `PATH`: `utilz list` must fail loudly with a single install hint, and `utilz doctor` must still complete and name `yq` as missing.

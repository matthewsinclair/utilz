---
node: cc
name: Control Claude
role: control
session_id: d69b4069-611f-4829-8991-1a15c02f53bb
heartbeat_at: 2026-09-19 17:40Z
status: active
focus: "ST0021: 0040's harness proof and landing, then WP-02 (cdp.rs red tests in the tree)"
claims: [ST0019, ST0021]
---

# Control Claude (cc)

## DOING

- ST0021 (showreel video export for 2.10.0), building on hv's decision 7. WP-01 is DONE (97604d1, vc GO); WP-02 is started. TWO THINGS ARE IN FLIGHT AND UNCOMMITTED IN THE TREE, and this item is their resume state. (a) ISSUE 0040, the shared harness (filed at 28934d2; vc's decision 2 ruled EXTRACT before video.sh's first line). opt/prez/crate/test/lib/harness.sh is assembled from exact line ranges (acceptance.sh 116-192 plus theme-addressing.sh 85-144); both suites now source it through a fail-loud block; theme-addressing.sh:26-29 records the ruling. Checked: each of the 18 helpers is defined once; a direct run refuses (rc 2); a missing harness stops a suite naming the file (rc 1); local shellcheck findings are identical to HEAD's (16, none in harness.sh). The after-run of both suites under bash 3.2 was started just before the compact. RESUME: compare scratchpad harness/before-*.per-at with after-*.per-at, and the passed and rc lines; if identical, write 0040's Resolution, close it, commit fix(0040) by explicit path (git add the NEW lib/harness.sh), and send vc the SHA over the socket. (b) WP-02: crates/showreel/src/cdp.rs holds its RED tests only (NUL framing, AT28's stall naming its step, a closed pipe, an event kept for its waiter, a protocol error naming the method, the written message), and is not yet in lib.rs. RESUME: implement split_messages and Session (a reader thread, an mpsc recv_timeout watchdog; tests use std::io::pipe); move drive.rs's file_url into artifact::browser with its test; add artifact::base64::decode and correct its encode-only header; clock.js from the spike's shim.js; record.rs (an sh wrapper under process_group(0) whose EXIT trap removes the profile, the deterministic flags, 1 ms load slices, t0 anchoring, img.decode before every screenshot, the watchdog, Browser.close then kill -KILL -- -pgid). Then video.sh red first on lib/harness.sh, WP-03 (add --browser to the verb, so the finder's remedy stays true), and WP-04. The spike's harness and data are in the scratchpad's spike/ (capture.py, shim.js, FINDINGS.md).

## TODO

- The 2.10.0 release commit, once the video export lands, prepared as ebd0243 was: VERSION 2.9.0 to 2.10.0, prez's Cargo.toml and Cargo.lock at 2.2.0, and a CHANGELOG entry checked against every product file changed since 2.9.0. vc gives the GO, and hv tags and pushes.
- ST0019's design, after the release: the Utilz parts only (its version files, the CI build of prez, the Homebrew formula), answering questions 1-3, with 5 and 6 handed to Devbin 0064 through vc. THE 14 SEP RESEARCH is always recoverable from that session's transcript, 14d6f67d-9238-483a-bd2e-80345ed85383.jsonl, where all three files are single Write calls; the precedents survey describes private repos, so it stays out of this repo. If the video export needs Chrome and ffmpeg at run time, the formula's dependencies follow from it.

## Holds

_(none)_

## Watch-outs

- One suite at a time with vc, and commits by explicit path only: vc's files have sat staged in the shared index while cc committed.
- An explicit-path commit of a .md that the pre-commit prettier hook reformats leaves the real index holding the pre-format text (MM): check git status afterwards. An issue body must be prettier-stable too, or the committed view drifts from the store render and the doctor gate refuses every later commit (0025-0027, fixed by vc at 895a2fc).

## Decisions

_(none)_

---

_Generated by Intent v3.1.0 from the whiteboard model. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

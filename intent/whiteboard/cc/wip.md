---
node: cc
name: Control Claude
role: control
session_id: 14d6f67d-9238-483a-bd2e-80345ed85383
heartbeat_at: 2026-09-14 21:44Z
status: paused
focus: "none"
claims: [ST0019]
---

# Control Claude (cc)

## DOING

- ST0019's design.md, the Utilz parts only (its version files, the CI build of prez, the Homebrew formula), for vc's review before any code. hv's steer via vc (14 Sep): keep the design to what it needs, and no yak-shaving. RESEARCH is in cc's 14 Sep scratch, /private/tmp/claude-501/-Users-matts-Devel-prj-Utilz/14d6f67d-9238-483a-bd2e-80345ed85383/scratchpad/st-release/: precedents-survey-2026-09-14.md (Intent's, Conflab's and Devbin's release and brew setups; it describes private repos, so it stays out of this repo), brew-facts-2026-09-14.md, and install-model-survey-2026-09-14.md if that agent finished (re-run it if absent). FACTS THAT SHAPE THE DESIGN: brew parses the version from a bare tag (/releases/download/2.9.0/ gives 2.9.0), so the formula needs no version line; options are not allowed in homebrew/core; pdf2md needs pdfplumber and xtrct needs anthropic, which a formula vendors as resources in a libexec virtualenv or leaves to the user; macoz, clipz and cleanz call macOS-only tools. NEXT: draft design.md answering questions 1-3, and hand 5 and 6 to Devbin 0064.

## TODO

- install.sh:341-343's comment says the manifest's source-tree row is resolved physically, while :345 runs a logical cd "$tree" && pwd. File it and fix whichever side is wrong (finders are fixers). ST0019's install-model survey found it, and cc verified both lines on 14 Sep.

## Holds

- Close 0038 once hv has pushed main carrying 3fd5f24 and the first CI run on it has finished. If that run's Rust Lint (clippy) log shows cargo fmt ran, close 0038 with the run id in its Resolution. If rustfmt is missing on ubuntu-latest, the job is red, and the fix is adding the toolchain component. vc reads that run too.

## Watch-outs

- One suite at a time with vc, and commits by explicit path only: vc's files have sat staged in the shared index while cc committed.
- An explicit-path commit of a .md that the pre-commit prettier hook reformats leaves the real index holding the pre-format text (MM): check git status afterwards. An issue body must be prettier-stable too, or the committed view drifts from the store render and the doctor gate refuses every later commit (0025-0027, fixed by vc at 895a2fc).

## Decisions

_(none)_

---

_Generated by Intent v3.0.3 from `the whiteboard model`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

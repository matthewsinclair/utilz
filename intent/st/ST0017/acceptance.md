---
st_id: ST0017
title: Add 'showreel' to 'prez'
---

# ST0017: Add 'showreel' to 'prez' -- Acceptance

> **THIS FILE IS A GENERATED VIEW, AND A ROW AUTHORED HERE IS DISCARDED BY THE NEXT SYNC.** The acceptance contract is canon in the thread model; this file renders it. Acceptance Criteria (AC) are the ratified completeness boundary; Acceptance Tests (AT) are the small red-to-green tests that prove them.
>
> Done = every AC is covered by a GREEN AT, or (for a non-test AC) its named evidence is satisfied, AND the AC set is the ratified full boundary. Done is read from this map, never from a hand-ticked box.
>
> Test-backed satisfaction is COMPUTED from covering green ATs and never stored -- storing it would be double truth. An AC has four states, not two: beyond satisfied and unsatisfied, a requirement can be **descoped** to a named thread or **withdrawn** with its reason on the record. Both are non-blocking and both are reported separately, so a thread that descoped half its contract looks like one.

## Acceptance Criteria

### WP-01 -- Workspace + artifact crate: the C3 consolidation, zero behaviour change to prez (status: Done)

- AC-1.1 (non-test) Theme resolution and base64 have exactly ONE implementation, linked by both binaries. Data-URI inlining is NOT in this set: the diff established it has no shared surface. -- evidence: Verified by vc 2026-09-09 at e0dd357: prez links the shared crate -- main.rs:49 'pub use artifact::Failure', main.rs:52 'pub(crate) use artifact::base64', theme.rs:3 records the resolver lives in artifact::theme. One implementation, both binaries. -- satisfied: yes
- AC-1.10 (non-test) The workspace root pins resolver = 3 explicitly and sets [profile.dev] debug = line-tables-only. -- evidence: Verified by vc: crate/Cargo.toml :72 resolver = 3 with lamplight-vc's reasoning at the key, and :79-80 [profile.dev] debug = line-tables-only. -- satisfied: yes
- AC-1.11 (non-test) EVERY workspace member with a tests/ directory declares autotests = false, exactly one [[test]] unless a declared exception carries its reason, and is covered by the orphan guard. Checked live over all members, never recorded once for the first crate. -- evidence: CENSUS ZERO WITH ITS CENSUS PRINTED, verified live by vc 2026-09-09 over all three members rather than recorded once for the first crate, which is the failure the row names. Population: the workspace root package prez plus members = ["crates/artifact", "crates/showreel"], three crates. Members carrying a tests/ directory: ZERO -- `find opt/prez/crate -type d -name tests -not -path '*/target/*'` returns 0. Declarations of autotests or [[test]] in any of the three manifests: ZERO. The orphan guard's own answer, run rather than quoted: `bin/devbin check autotests` reports "3 crate(s), none with a tests/ directory -- nothing to check". So the row's universal quantifier is vacuously true AND its real requirement is met: every member was checked, the number checked is stated, and the guard covers the same three. This is a census zero and not a detector zero -- nothing was there to count, the count is printed, and the row does not depend on anything having fired. TN001 exposure is therefore zero and measured rather than assumed; the honest cost is that showreel being lib+bin gives two unittest targets with the bin holding none. -- satisfied: yes
- AC-1.12 (non-test) Every gate this estate runs is enumerated before a change is called green, and each reports its own population including on passes. A gate that never activates has no voice and is indistinguishable from one that ran and found nothing. -- evidence: cc enumerated every gate CI runs and drove all seven: cargo test debug 12+133, cargo test --release 12+133 (CI's actual arm), clippy --all-targets -D warnings 0, cargo build --release clean, shellcheck 18 files, utilz test prez all 4 suites, devbin check autotests 2 crates. The row caught its own author twice on the day it was minted -- cc called WP-01 green on cargo test alone, and vc called it verified on clippy plus cargo build, two gates of four. -- satisfied: yes
- AC-1.13 (non-test) The shellcheck collector excludes target/ by its own rule, not by CI job ordering. Measured 2026-09-09 by cc: run locally after a build it collects 69 'files' against 18 real ones, picking up compiled binaries. It is correct in CI only because the shellcheck job never builds -- a gate whose correctness depends on another job's behaviour rather than on its own exclusions. -- WITHDRAWN: WITHDRAWN 2026-09-09. The row's load-bearing claim is REFUTED BY MEASUREMENT and the refutation is cc's, against cc's own row. The row said the collector is correct in CI only because the shellcheck job never builds, so its correctness depended on another job's behaviour. It does not. Measured by cc against a fully built tree (target/ at 1.2 GB, 125 executables) and verified independently by vc at the source: the collector's find emits everything under target/, and the case default runs `file "$s" | grep -q "shell script"` and rejects every Mach-O binary, so FILES ends at 18 either way. The gate is correct locally after a build, which is precisely the premise this row asserted fails. THE ORIGINAL 69 WAS find'S RAW EMISSION READ AS THE GATE'S POPULATION -- stage one of a three-stage pipeline measured, and the claim made about stage three. Same shape as the 6.29 vc relayed to hv, and cc named it as such unprompted. WHAT SURVIVES DOES NOT JUSTIFY A ROW. The *.sh branch adds with no type check, so a .sh dropped under target/ by a build script would enter the array; measured population of .sh files under target/ today is ZERO, and nothing in this estate writes one. The remainder is cost -- 143 stats and 125 file shell-outs -- and this was never a performance row. The collector also already carries the count control this thread argues for: FILES empty exits 1 with "the collector is broken, failing loudly". So the row as written is a control for a risk nobody has, and satisfying it would mean fixing something measured to work -- hv's test applied to a row of vc's own, after hv cut two of vc's yesterday for exactly this. cc leans weakly to taking the one line anyway and says the find is one they are not otherwise touching; that makes it a special trip for an empty population, and it settles the call. TAKING THE LINE REMAINS FINE AND IS NOT REQUIRED: if cc is in that file for another reason, a -not -path plus a guard on the *.sh branch makes an emergent correctness a stated one, ungated. REINSTATE IF the population stops being empty -- a build script that writes a .sh under target/ makes this a live defect again, and `intent ac reinstate` is the verb.
- AC-1.14 (non-test) cargo test --release is driven as a distinct arm from the debug one, because CI runs release and a debug-only green is a different measurement. Both arms stated with their counts. -- evidence: BOTH ARMS DRIVEN, and cross-checked by two instruments that do not share a mechanism. cc drove cargo test --workspace --no-fail-fast in release and in debug, exit 0 both, and attributed the split from the run's own Running headers rather than from per-crate memory. Release and debug are IDENTICAL: artifact lib 16, prez main.rs 133, showreel lib 17, showreel main.rs 0, doc-tests artifact 0, doc-tests showreel 0 -- total 166, 0 failed, 0 ignored, across six targets of which three carry tests. prez's 133 sit in main.rs because prez is bin-only with no lib target; showreel's bin at 0 is the deliberate lib+bin split. VC'S INDEPENDENT CHECK USED A DIFFERENT INSTRUMENT ON PURPOSE -- cc read cargo's output, vc counted `#[test]` attributes in the workspace source -- and it agrees at 166 with the same per-crate split: opt/prez/crate/src 133, crates/artifact/src 16, crates/showreel/src 17. Two nodes agreeing on one instrument wastes one of them; these are two instruments, and the source count also bounds the ignored figure, since 166 attributes producing 166 run tests leaves no room for a skip. The partition sums: 16+133+17+0+0+0 = 166. The distinction the row exists for holds -- CI runs release, and the arms were driven separately rather than one inferred from the other, which is what makes "identical" a finding instead of an assumption. -- satisfied: yes
- AC-1.15 (non-test) An injection must PROVE IT APPLIED before its result is read, and the applying step refuses rather than warns. Binds every red-proof in this thread, not just WP-01. Measured 2026-09-09: cc's first R1 injection went through python3 -c in double quotes, the shell ate the backslash-n in the anchor, the pattern matched nothing -- and the suite then ran and printed 'ok. 12 passed; 0 failed', which is BYTE-IDENTICAL to a control that fired and passed. Only an incidental count==1 assertion separated them. Third instance of the shape today: snorkeltoast's first injection landed inside a fenced example, cc's roster reorder could not reach the code, and this one did not apply at all. The rule is not 'controls need red-proving' -- it is that THE RED-PROOF ITSELF NEEDS A CONTROL, because a proof that did not run and a proof that passed have the same output.
AUDITED BY vc 2026-09-09 17:16Z AND NOT SATISFIED, WITH THE CLEAREST VIOLATION BEING vc'S OWN, MADE TODAY, AFTER THIS ROW WAS MINTED. The discharge condition is the row's SECOND clause and it is the concrete one: the applying step REFUSES rather than warns. A step that prints its check and continues is a warn, and a warn is what this row exists to refuse. Population -- every red-proof in this thread, six of them, in three homes. REFUSES: snorkeltoast's PORT_EXPECTATIONS shape check, which rejects a row claiming `observed` while citing nothing and was itself red-proved by injection after its first version was not; and vc's board edits, which exit non-zero when a replacement matches other than once. STRUCTURALLY IMMUNE: snorkeltoast's `cmd_redproof`, whose two injections are CONSTRUCTED -- an image darkened a level, a slide removed -- rather than pattern-matched, so they cannot silently no-op, and whose precondition refuses with "no COVERED image slide to injure". DOES NOT REFUSE: vc's AC-1.2 red control as first run, which printed "injection applied? removed 1 line(s), expected 1" and carried on regardless of the answer -- re-run at 17:16Z through an `apply_or_refuse` step, with the refusal itself red-proved by a no-op injection that exits 1. AND TWO THAT CANNOT REFUSE BECAUSE THERE IS NOTHING LEFT TO RUN: cc's records at `limits.rs` (two of seven tests fail on the injected reference implementation) and `segment.rs` (three of five, with two greps at zero), which are COMMENTS describing a hand-run act. Both claims are almost certainly true and neither is falsifiable from the artefact -- this is PROSE DOES NOT FAIL arriving at red-proofs, which is the one place the thread had assumed it could not. THE ROW IS NOT DISCHARGED BY AN AUDIT, BECAUSE A CENSUS OVER A GROWING POPULATION DECAYS: WP-03's build path will produce more injections than the thread has made so far. DISCHARGED WHEN the next red-proofs this thread produces apply through a step that refuses, and cc states the form their two comment-recorded injections took -- if those steps refused, the record should say so, and if they printed, that is a third instance rather than an accusation. NOT PROPOSED: a committed injection harness. That is machinery for a discipline problem over a small population of future injections, by two nodes who have now both been burned, and hv has cut three items of vc's for exactly that shape. -- evidence: Verified by vc 2026-09-09 18:26Z. THE CONDITION THIS ROW SET WAS FORWARD-LOOKING RATHER THAN A CENSUS, AND THAT CHOICE IS WHAT MADE IT GRADEABLE: a census taken at 17:18Z would have read compliant and been wrong within the hour.

THE POPULATION, EIGHT RED-PROOFS IN THREE HOMES, EACH READ RATHER THAN COUNTED. STRUCTURAL, in the repository and executable by anyone: snorkeltoast's cmd_redproof, whose injections are CONSTRUCTED -- an image darkened one level, a slide removed -- so they cannot silently no-op; and their six table injections at bc6c3fe, which run on every selftest against a deep copy, with THE APPLIER ITSELF RED-PROVED in three directions (an injection mutating nothing, a shape check that stops detecting, and a table already failing beforehand -- that third one is what stops six greens reporting a working check when what they observed was a check already broken). REFUSING APPLIER WITH RE-APPLIABLE TEXT: cc's C2 at 62b644d, three injections recorded as LITERAL DIFFS with the applier's form named once -- `assert s.count(old) == 1, "INJECTION DID NOT APPLY"` -- and the explicit note that the `grep -c` printed afterwards is a READING and does not gate. vc TESTED THE RE-APPLIABILITY RATHER THAN ASSUMING IT: both quoted anchors occur exactly once in normalise.rs, so the assert would pass and a reader can re-run the injection and obtain their own number. CORRECTED AFTER VIOLATING IT: vc's AC-1.2 control, which printed its check and carried on, re-run through an apply_or_refuse step whose refusal is itself red-proved by a no-op injection. HONESTLY MARKED AS NON-COMPLIANT RATHER THAN REPAIRED: cc's limits.rs and segment.rs records, where cc went and looked instead of answering from memory and reported "IT PRINTED. NEITHER ONE REFUSED, AND I CANNOT SHOW OTHERWISE FROM THE ARTEFACT" -- segment.rs settling its own case, because it reports TWO counts and reporting a value is what a reading looks like while a step that refuses needs neither written down.

WHY RE-APPLIABLE TEXT DISCHARGES THIS ROW AND A REFUSING APPLIER ALONE DOES NOT. The row's own sentence is that a proof that did not run and a proof that passed HAVE THE SAME OUTPUT. A literal injection in the record breaks exactly that: the two stop having the same output the moment anyone re-applies it, because one of them yields a different number. The refusing applier protects the figure at the moment it is taken; the recorded text lets any later reader take it again. The second is the stronger property and it is the one that has landed.

THE RESIDUAL, WHICH IS cc'S OWN AND IS RECORDED RATHER THAN SMOOTHED. The applier lives in a shell command and not in the tree, so "the applier refused" remains cc's report in exactly the way limits.rs was. cc named this themselves and called the forward half two-thirds closed rather than closed. vc rules it discharged notwithstanding, because the row's property is the indistinguishability of a proof that ran from one that did not, and re-appliable text removes it without anyone having to believe the applier. A row is not made stronger by requiring a second mechanism for a property the first already secures.

THE BEST INSTANCE IN THE THREAD, AND IT IS THE ARGUMENT FOR THE ROW RATHER THAN AGAINST cc. "Proved applied before its result was read" appears in 1d5f320, written roughly three hours after cc identified that exact phrase as the tell of a reading and corrected two files for carrying it. cc knew the rule, had just applied it to their own work, and wrote the phrase again in the next commit. KNOWING A RULE IS MEASURABLY NOT THE SAME AS BEING PROTECTED BY IT. cc also volunteered that their claim "the injection text is in the commit message verbatim" was false -- it was a description, not the text, and nobody could have re-applied any of the four -- after vc grepped all four messages for `assert`, `s.count(`, "INJECTION DID NOT" and `grep -c` and found nothing.

AND ONE RED-PROOF REFUTED ITS OWN AUTHOR'S COMMENT, WHICH THIS THREAD HAD NOT SEEN BEFORE. C2's injection 2 reordered resize before collapse to test an order cc's doc comment had argued was load-bearing across three paragraphs. NOT ONE TEST MOVED. A fully-opaque alpha channel is CONSTANT, a Lanczos kernel sums to one, and a constant channel resamples to itself, so there was never anything for the resize to break; the same argument disposes of the orientation half, since a transpose commutes with a uniform scale. Both claims corrected to what was measured and the order kept for COST -- three channels resampled instead of four -- stated as cost rather than correctness. That is the row working on prose no test could ever have failed.

A SECOND PROPERTY, ADDED 2026-09-09 19:52Z ON cc'S SELF-REPORT: "PROVED APPLIED" AND "CLEANED UP ON REFUSAL" ARE DIFFERENT, AND THE SECOND FAILED. This row pins that an injection proves it applied before its result is read. cc's applier did that correctly and still left the tree INJECTED: injection M's anchor collided with an unrelated match arm, so the post-check refused -- **after the write, with the revert on the SUCCESS PATH ONLY**. The tree sat with `iso()` not validating dates at all while the harness printed REFUSED.

**THE WORD "REFUSED" READS EXACTLY LIKE NOTHING HAPPENED**, which is why it is worse than a harness that never refuses: a refusal message is reassuring, and reassurance is what stops the next look. cc caught it only because the next thing they did was read the function.

**THE BLAST RADIUS IS NOT ONE PROOF.** A red-proof harness that can leave a tree injected makes every SUBSEQUENT measurement in that session suspect -- gates, counts, binary sizes, any `strings` census -- and none of them would look wrong. This row governs every red-proof in this thread, so the property belongs here rather than in a new row: **the revert goes in a `finally`, so it runs on the refusal path as well as the success path.** Fixed by cc in the same commit; recorded here because prose does not fail and both nodes use this pattern.

NOT MINTED AS A SEPARATE ROW, on hv's two tests: it is real and demonstrated, and the fix is one `finally` that is already in. What it needed was a home on the contract, not a new line item. -- satisfied: yes
- AC-1.16 (non-test) EVERY cargo gate reaches every member of the workspace it claims to gate, and the population comes from the manifest rather than from the command's default or from a hand-written list. AC-1.9 stated this correctly for cargo test and scoped it to cargo test, which is the enumeration error one level up: the property is about GATES, and test is one of several. THE ROW THEN MADE THE SAME ERROR ONE LEVEL DOWN -- it named two homes and said "all three gates", and the estate has SIX SITES IN THREE FILES, measured 2026-09-09 by vc and cc independently at pickup. cargo test: --workspace, fixed in WP-01. cargo clippy in CI: had no --workspace, so a member crate's TEST code was never linted and one real lint sat behind it -- found and fixed by cc in 8dea619. THE OTHER FIVE ARE LIVE. (1) CI's per-crate release build in tests.yml, which is the line DIRECTLY ABOVE the comment explaining why --workspace is required -- the comment is attached to the cargo test call beneath it and reads "--workspace or every crate but the root package is silently skipped", describing the call above it exactly. (2) The shim's build in prez_ensure_built. (3) acceptance.sh's AT12 clippy arm, the check labelled "clippy warnings and errors", which the suite's own comment calls the load-bearing gate: it is 8dea619's defect still live, and artifact and showreel are unlinted by it today. (4) AT01's release build, legitimately prez-scoped since AT01 asserts prez's own binary and its 8 MB ceiling -- correct now, listed because a later AT needing the showreel binary makes it wrong silently. (5) The unconditional pre-build between AT01 and AT02, which every later AT depends on. AND THE SIXTH IS A DIFFERENT MECHANISM WITH NO FLAG TO FIX IT, cc's find, verified from source by vc: prez_is_stale's find -newer walk enumerates src, themes, assets, the manifest and Cargo.lock, and NOT crates -- so an edit to any member crate leaves the shim reporting the binary current and it never rebuilds. Silent: no message, no failure, an old binary. Its comment names this exact class, saying a watcher over src alone would report a stale binary as current, which is why themes and assets were added; crates joined the workspace later and the walk was not extended. SO THE POPULATION HAS TWO MECHANISMS AND ONE FIX DOES NOT COVER BOTH -- five take their population from cargo's default and one flag reaches them; the sixth takes it from a typed directory list and no flag reaches it. A sweep that adds --workspace everywhere and stops leaves the silent one, which is the worse of the two. TWICE NOW THE KNOWLEDGE WAS PRESENT AT THE SITE AND DID NOT PREVENT THE DEFECT: CI carries the explanation one line from the violation, and the shim carries the class description directly above the walk that commits it. That is the argument for taking the population from the manifest rather than restating the rule in a comment. THE MECHANISM RULING, vc 2026-09-09, after cc showed the row's own conclusion did not reach their instance: "from the manifest" has no route into a find, which takes no --workspace. THE GENERAL PROPERTY IS THAT THE POPULATION IS DERIVED, NOT TYPED -- from the manifest where the tool reads one, from the TREE where it does not. So cc's option (ii) is ruled: walk $CRATE_DIR minus target/ rather than adding crates to the hand list. THE ARGUMENT cc DID NOT MAKE IS THE ONE THAT SETTLES IT, and it is a direction argument rather than a correctness one. Adding crates to the list is correct today and fails IDENTICALLY the next time a directory is added, silently, because a walk that is too narrow reports a stale binary as current. Walking the tree over-includes instead -- an edit to the acceptance suite under $CRATE_DIR/test would trigger a rebuild nobody needed -- and over-inclusion fails LOUDLY, costing a rebuild, while under-inclusion fails greenly and ships an old binary. Trading a silent failure for a noisy one is the whole of it. Excluding target/ is then LOAD-BEARING in that walk rather than hygiene, since the binary's own directory is newer than the binary on every build and the check would otherwise always report stale. NOTE THAT THIS DOES NOT CARRY OVER TO AC-1.13, which is withdrawn: that row's target/ exclusion had a working type check behind it and an empty population, and the near-agreement between the two exclusions is exactly the kind that suppresses the check. DRIVEN 2026-09-09 BY vc FOR THE CLIPPY SITE, so one of the six is now a measurement rather than a source read, and it is the discharge standard this row names. Both forms run against the same manifest with --message-format=json, counting workspace targets linted: the UNSCOPED form -- what the acceptance suite's AT12 arm runs -- lints TWO, artifact's lib and prez's bin, artifact appearing only because prez depends on it. The --workspace form lints FOUR, adding showreel's lib and showreel's bin. SHOWREEL IS ENTIRELY ABSENT FROM THE GATE THE SUITE CALLS LOAD-BEARING. The forms disagree, driven, and the disagreement is the whole crate. Five sites remain read rather than driven. Discharged when every gate takes its population from a derivation rather than a list, by both mechanisms, driven and shown to disagree with the unscoped form.
AUDIT 2026-09-09 17:32Z BY vc, ALL SIX SITES READ AT HEAD AND THE BUILD GATE NOW DRIVEN. THREE OF SIX ARE FIXED OR CORRECT AND THREE ARE LIVE. FIXED: the shim's build (prez:215 carries --workspace); and prez_is_stale, which cc built as option (ii) and BETTER THAN THE RULING ASKED -- it walks $CRATE_DIR minus target/ AND takes the OLDER of the two binaries as its reference, because one `cargo build --workspace` relinks only what changed, so editing showreel alone leaves its sources newer than prez's binary and a prez-referenced check would report stale forever, rebuild nothing, and announce a rebuild each time. That second half was not in the ruling and is a real defect the ruling would not have caught. CORRECT BY SCOPE: AT01's release build at acceptance.sh:352, still legitimately prez-scoped. LIVE: (1) CI's release build at tests.yml:283, unchanged, and the comment explaining --workspace still sits ONE LINE BENEATH it attached to the cargo test call -- the knowledge is adjacent to the violation for the third recorded time; (3) AT12's clippy arm at acceptance.sh:947, still unscoped, still the gate the suite's own comment calls load-bearing; (5) the unconditional pre-build at acceptance.sh:375, still unscoped. NOW DRIVEN RATHER THAN READ, WHICH TAKES THE DRIVEN COUNT FROM ONE SITE TO THREE: sites 1 and 5 are the same command, `cargo build --release --manifest-path <root>`, and driving both forms with --message-format=json and counting workspace targets emitted shows the UNSCOPED form covers TWO -- artifact's lib and prez's bin, artifact appearing only because prez depends on it -- while --workspace covers FOUR, adding showreel's lib AND showreel's bin. THE DISAGREEMENT IS NAMED: showreel [lib] and showreel [bin]. AND SITE 5 HAS BECOME LOAD-BEARING SINCE THIS ROW WAS WRITTEN, WHICH THE ROW PREDICTED IN THE ABSTRACT AND IS NOW CONCRETE: `utilz prez showreel check` runs today, so WP-05's acceptance test needs the showreel BINARY, and the pre-build every later AT depends on does not build it. That AT will fail with a missing binary and read as a dispatch defect. The row's own words for site 4 were "listed because a later AT needing the showreel binary makes it wrong silently"; it is site 5 that got there first, and it is not silent so much as misattributed. STILL UNDISCHARGED: three live sites, all one flag each, none of them vc's to edit. -- evidence: Verified by vc 2026-09-09 17:56Z, all six sites read at HEAD and BOTH MECHANISMS DRIVEN.

MECHANISM ONE, cargo's own population, five sites, all now derived. tests.yml:289 CI release build --workspace (was the row's site 1, live at vc's 17:32Z audit and fixed by cc after it); tests.yml:290 test and :401 clippy already carried it; prez:215 shim build --workspace; acceptance.sh:380 the unconditional pre-build --workspace; acceptance.sh:956 AT12's clippy --workspace. AT01's build at acceptance.sh:352 remains deliberately prez-scoped, which is correct because AT01 asserts prez's own binary against its 8 MB ceiling.

DRIVEN, NOT READ, AND THE DISAGREEMENT NAMED. Both forms of the build through --message-format=json, counting workspace targets emitted: the UNSCOPED form covers TWO -- artifact [lib] and prez [bin], artifact appearing only because prez depends on it -- and --workspace covers FOUR, adding showreel [lib] and showreel [bin]. AT12's clippy was driven the same way earlier and disagrees identically, 2 against 4. cc independently drove site 5 end to end by removing the binary: old line, exit 0, binary still ABSENT; same line plus --workspace, exit 0, binary PRESENT.

MECHANISM TWO, THE FIND WALK, WHICH HAD NO FLAG AND IS THE ONE NOBODY HAD DRIVEN. prez_is_stale now walks $CRATE_DIR with target/ pruned. Driven by vc against a reference older than every file: the walk emits 68 paths of which 23 ARE UNDER crates/ and ZERO under target/. THE CONTROL IS THE OLD WALK ITSELF -- src, themes, assets, the manifest and Cargo.lock -- which emits 33 paths and reaches crates/ ZERO TIMES. The disagreement between the two walks IS the defect this row names, measured rather than argued, and target/ being pruned is load-bearing rather than hygiene since the binary's own directory is newer than the binary after every build.

AND cc BUILT MORE THAN THE RULING ASKED FOR, WHICH THE RULING WOULD NOT HAVE CAUGHT. vc ruled only that the population come from the tree. cc also made the freshness REFERENCE the OLDER of the two binaries, because one `cargo build --workspace` relinks only what changed, so editing showreel alone leaves its sources newer than prez's binary -- a prez-referenced check would then report stale forever, rebuild nothing, and announce a rebuild on every invocation. A ruling that fixed the population and not the reference would have shipped a loop.

BOTH MECHANISMS, EVERY SITE, DERIVED RATHER THAN TYPED: five from cargo's manifest via --workspace, one from the TREE where no manifest is read, and one scoped on purpose with its reason stated. The row's own argument holds at the end of it -- over-inclusion fails loudly and costs a rebuild, under-inclusion fails greenly and ships an old binary, and every site now fails in the loud direction. -- satisfied: yes
- AC-1.2 (non-test) WP-01 adds no third-party cost to prez. The comrak dependency line is byte-identical across WP-01, and prez's COMPILED closure -- `cargo tree -p prez --edges normal,build` -- carries the same 17 third-party packages before WP-01 and at HEAD. THE INSTRUMENT IS PREZ'S CLOSURE AND NOT THE WORKSPACE LOCKFILE, which is a correction rather than a preference: the lockfile answered for prez only while prez was the workspace's only package, and even then it counted 7 packages nothing compiles. The one addition is `artifact` -- first-party, path, std-only, taking no dependencies of its own -- signed off by hv 2026-09-09 as a one-off. -- evidence: Verified by vc 2026-09-09 17:02Z, red control re-run in the refusing form at 17:16Z. THREE INSTRUMENTS OVER TWO WINDOWS, AND THE DETECTOR WAS PROVED LIVE IN BOTH DIRECTIONS BEFORE THE ZERO WAS READ. Window: 3e39d5d (last commit before WP-01) to HEAD. WP-01's manifest work is c5afc33 (workspace + artifact path dep) and 47c2ebc (resolver pin); showreel's packages arrive later at 8dea619 in WP-03 and are outside this row.

(1) COMRAK LINE BYTE-IDENTICAL. `git show 3e39d5d:opt/prez/crate/Cargo.toml | grep '^comrak'` against the same grep at HEAD: 56 bytes each, `cmp` exit 0. Population control, exactly one `^comrak` line at each endpoint, 1 and 1, three mentions each (the other two are the comment explaining default-features). Independently `git log -S` puts the line's last change at 64d375b, ST0010/03, before ST0017 existed.

(2) PREZ'S COMPILED CLOSURE, ONE INSTRUMENT AT BOTH ENDPOINTS. A detached worktree at 3e39d5d and the working tree, both through one shell function with identical flags, so the two readings cannot differ by invocation. Closure including first-party 18 to 19. THIRD-PARTY ONLY, with the first-party names DERIVED from the workspace manifest rather than typed: 17 to 17, SYMMETRIC DIFFERENCE 0 over a named population of 17 -- caseless, comrak, entities, fastrand, finl_unicode, jetscii, phf, phf_codegen, phf_generator, phf_shared, rustc-hash, siphasher, smallvec, tinyvec, tinyvec_macros, typed-arena, unicode-normalization. Name-only dedup equals name+version dedup at both ends, so no two-versions-of-one-package hides inside the count.

(3) THE LOCKFILE, ON ITS OWN WINDOW, 3e39d5d to c5afc33: 25 to 26 entries, symmetric difference exactly {artifact}, third-party 24 to 24.

RED CONTROL BOTH DIRECTIONS. Removing one real package from the HEAD side (smallvec) takes the symmetric difference 0 to 1 and NAMES smallvec. Adding one (serde) takes it 0 to 1 and NAMES serde. The `comm -23` arm was separately observed firing at 7 in this session on the lockfile-versus-closure comparison. SO THE ZERO IS A DETECTOR ZERO OVER A FULL POPULATION -- neither a census zero nor a tautology.

AND THIS EVIDENCE WAS CORRECTED AGAINST AC-1.15 ON THE SAME CONTRACT, WHICH IS THE POINT OF A BINDING ROW. The first run of the red control PRINTED its application check -- "injection applied? removed 1 line(s), expected 1" -- and carried on regardless of the answer. AC-1.15 says the applying step REFUSES rather than warns, and a print is a warn: had the injection matched nothing, the line would have read "removed 0 line(s), expected 1" and the symmetric difference would have printed underneath it looking exactly like a result. Re-run at 17:16Z with an `apply_or_refuse` step that exits non-zero on a wrong delta; both injections applied (delta -1 and +1) and both named their package. THE REFUSING STEP IS ITSELF RED-PROVED: a no-op injection, the file copied unchanged, is REFUSED at exit 1 with "changed the file by 0 line(s), expected -1 -- injection did not apply". That is the arm the print form did not have.

THE ABSENCE ASSERTION IS PAIRED WITH A PRESENCE ASSERTION OVER ONE POPULATION COMPUTED ONCE. The 17 are NAMED, not merely counted, so deleting prez's dependency would fail this row rather than pass it.

CC'S FIGURE RECONCILES EXACTLY, AND THE GAP WAS A POPULATION RATHER THAN AN ERROR. cc measured prez at 17 on normal+build edges and 13 on normal; vc measured 19 and 15 counting prez and artifact themselves. 19-2=17 and 15-2=13. Both readings correct, one naming first-party out.

ARTIFACT IS STD-ONLY BY MEASUREMENT AND NOT BY ITS COMMENT. Its `[dependencies]` table is present and empty; it contributes ZERO packages to prez's closure, the whole delta being artifact itself; its source resolves only std, crate and super. The manifest is conclusive -- a crate cannot be reached without being declared -- and the closure count is the second instrument.

THE ROW WAS CORRECTED TO ITS INTENT BEFORE BEING SATISFIED, NOT WEAKENED TO FIT. As written it named the lockfile, and the workspace lockfile now stands at 79 entries: a later reader running the row's own instrument would read 24 to 76 and call the row false. The intent, that WP-01 adds no third-party cost to prez, held before the edit. The replacement instrument is STRICTER -- what compiles rather than what resolves -- and is measured at HEAD rather than at WP-01's commit, which is the harder window, since showreel has since joined the workspace and reaches prez through the shared crate. -- satisfied: yes
- AC-1.3 (non-test) No existing prez assertion weakened or altered. THE INSTRUMENT IS "NO ASSERTION WEAKENED, ACROSS BOTH CRATES" AND NOT A BYTE DIFF OF prez ALONE, which is a correction rather than a relaxation -- the diff was a proxy for the property and has now stopped answering for it twice, in two unrelated ways. prez's `src/` goes 135 -> 133 because three base64 tests MOVED to `artifact`, which is AC-1.1's whole point, so a diff of prez alone shows three deletions and cannot tell a move from a loss; and a shared-crate type gaining a field, or a function gaining a parameter, edits fixture bodies that assert nothing. Discharged by NAMING the delta rather than counting it: every test that left prez is found in `artifact` with a byte-identical body, every body that differs at both endpoints differs only mechanically, and every assertion INCLUDING ITS MESSAGE STRING is unchanged. Additions permitted and expected. -- evidence: Re-verified by vc 2026-09-09 17:20Z after cc reported at 17:15Z that the row's instrument no longer runs. cc's reconciliation reproduces exactly and their ACCOUNT OF WHAT CHANGED IS INCOMPLETE BY TWO BODIES, which is the part worth having.

THE COUNT, MEASURED AT BOTH ENDPOINTS THROUGH ONE INSTRUMENT. A detached worktree at 3e39d5d and the working tree: prez `src/` carries 135 `#[test]` at base and 133 at HEAD. NAMED rather than counted, which is what separates a move from a loss: LEFT prez -- every_output_length_is_a_multiple_of_four, handles_bytes_that_are_not_text, matches_the_rfc_4648_vectors. ARRIVED in prez -- the_default_is_simple_specifically, the permitted addition. 135 - 3 + 1 = 133, and the partition sums.

THE THREE THAT LEFT ARE IN `artifact` WITH BYTE-IDENTICAL BODIES, not merely present by name. Each body extracted from prez/base64.rs at base and from artifact/base64.rs at HEAD and compared with `cmp`: 6L/6L, 4L/4L, 10L/10L, all IDENTICAL. THE COMPARATOR IS RED-PROVED AND ITS APPLYING STEP REFUSES, per AC-1.15: deleting one line from a body takes it to DIFFERS, and the injection is gated on a delta of exactly 1 or the run aborts.

TEN SHARED BODIES DIFFER, NOT EIGHT, AND THE TWO EXTRA ARE A DIFFERENT CHANGE THAN cc ATTRIBUTED. Over the 132 tests present at both endpoints, 10 bodies differ. cc reported 8 and attributed all of them to `Theme.dir`. Measured: SEVEN gained the single line `dir: None,` -- that is cc's `Theme.dir`; THREE gained a `Grammar::Css` argument at a `refuse_external` call site, which is issue 0018's narrowing and not the `Theme.dir` change at all. cc's eighth is a `theme.rs` helper that is not a `#[test]` and so falls outside this population; the two accounts differ because they count different things and BOTH numbers are right about their own population, but the three Grammar bodies were in nobody's account until now.

AND THOSE THREE ARE THE ONES WHERE AN ASSERTION COULD HAVE BEEN WEAKENED. A call gaining an argument is exactly where an `unwrap_err()` quietly becomes an `is_err()`. It did not: the diffs are one line each and the assertion is byte-identical on both sides, message strings included -- `.expect("an attribution comment must not fail a build")` and `.unwrap_err()` unchanged.

THE NARROWING THAT MADE THEM DIFFER CARRIES ITS OWN POSITIVE COVERAGE, so the CSS test's continued pass is not doing the work alone. Issue 0018 made the comment exemption CSS-only, and `Grammar::Verbatim` is asserted directly at artifact/src/theme.rs:1024-1028 -- the bypass refuses, the control refuses, and the attribution comment refuses. An absence assertion (CSS still exempt) paired with a presence assertion (Verbatim does not), over the same mechanism.

SO THE PROPERTY HOLDS AND THE OLD WORDING DOES NOT. "All 135 original test bodies unchanged and verifiable by diff" is false on two independent counts at HEAD while "no assertion weakened or altered" is true and measured. The intent held before the edit; the replacement instrument is the stricter one, because a byte diff of prez alone cannot distinguish a test that moved to the shared crate from a test that was deleted, and this row exists to catch the second. -- satisfied: yes
- AC-1.4 (non-test) prez's release binary size is stated as a number against the budget, not as a distance. -- evidence: prez's release binary is 4,384,912 bytes against the 8,388,608 budget -- stated as a number with the budget beside it rather than as a distance, which is the row's requirement and is met by the form regardless of the value. RESTATED 2026-09-09 BECAUSE THE NUMBER MOVED FOR THE FIRST TIME, and the direction is the part worth recording. Earlier evidence cited cc's 4,384,864 from a build whose 32-byte difference from the on-disk 4,384,896 could not be resolved -- two builds, and no measurement could separate a stale figure from a transcription slip, so it was noted and closed. This move is different: it is DELIBERATE and it has a cause. R3 added refuse_external_target to artifact, artifact is SHARED, and prez linked sixteen bytes it will never call -- 4,384,896 to 4,384,912. cc reported it unprompted rather than letting it surface elsewhere. THE PRICE IS RIGHT HERE AND THE DIRECTION IS THE THING TO WATCH: the alternative was the scheme list in two places, which is the violation this crate exists to remove, so paying sixteen bytes to keep one rule is the trade the consolidation was for. But artifact being shared means every showreel-only addition costs prez something, and if showreel keeps growing artifact then prez keeps paying. NO NEW CONTROL IS NEEDED AND THAT WAS CHECKED: AT01 already asserts the binary against the 8 MB ceiling on every acceptance run, and 4,384,912 is 52 percent of it. The guard exists, it is driven, and this row's job is to keep the number stated rather than to gate it. -- satisfied: yes
- AC-1.5 (non-test) prez's and showreel's theme behaviours are diffed by two INDEPENDENT enumerations, each from source before seeing the other, before the shared crate takes either as its semantics. -- evidence: vc/inbox.cc.md entry 2026-09-09 08:16Z, committed 61206a5 (stamp repaired 20326b9). Two INDEPENDENT enumerations: snorkeltoast enumerated showreel from source before re-reading cc's table, cc enumerated prez before seeing theirs. Result: FOUR behaviours not five, one clean match, and the shipped defect that became issue 0014. The row existed because near-agreement suppresses the check, and the check disagreed. -- satisfied: yes
- AC-1.6 (non-test) The shared resolver keeps prez's newline-preserving comment stripping (R1) and its four provenance messages (R4). The shorter implementation is the worse one and a naive merge takes it. -- evidence: R4 verified by vc in the shared crate: artifact/src/theme.rs:270 and :275 are two templates parameterised over {mechanism} and {elsewhere}, giving four distinct messages across the shadowing and env-vs-flag axes, with prez's vocabulary (deck, --theme-path, PREZ_THEME_PATH) injected by the Registry rather than hardcoded. cc drove all four through the shim and each matches the pre-extraction wording. R1 red-proved by cc: injecting the naive strip fails stripping_a_comment_does_not_move_the_lines_after_it with 'the offender is on line 4 of the FILE; a comment-deleting strip reports line 2'; restored, sha matches, 12 pass. -- satisfied: yes
- AC-1.7 (non-test) crate/ is both workspace root and prez's package: every include_str! path, the shim and prez.bats's fixture unchanged, and [profile.release] proven IN EFFECT for the measured binary rather than read off the manifest. -- evidence: Verified by vc: crate/Cargo.toml carries [workspace] at :62 AND the [package], so every include_str! path, the shim and prez.bats's fixture are unchanged. [profile.release] proven IN EFFECT by DIFFERENCE, not read off the manifest -- cc built the same source with strip and lto disabled and measured 4,729,000 bytes / 2,534 nm entries against 4,384,832 / 108, so the profile is doing 344,168 bytes and 2,426 symbols of work. -- satisfied: yes
- AC-1.8 (non-test) prez pins which theme load(None,..) resolves to, by a property unique to simple rather than one the whole roster shares. The roster-reorder injection cannot fire until wiring and MUST be re-run then. -- evidence: The deferred half discharged by cc, and it is the half that could not fire this morning. Reordering BUILT_IN to put mono first now FAILS the_default_is_simple_specifically while no_theme_given_uses_the_embedded_default stays ok -- which is exactly the blindness the row was minted for, since all seven built-ins carry --gp-bg. Restored, sha matches. -- satisfied: yes
- AC-1.9 (non-test) Both cargo test call sites pass --workspace and --no-fail-fast, so the population comes from the manifest's members rather than a filesystem walk. An enumeration can fail to see a member; --workspace cannot. -- evidence: Verified by vc at e0dd357: common.sh:855 and tests.yml:284 both carry --workspace --no-fail-fast. Measured before the fix: the estate's own command built 1 executable where --workspace builds 2, so artifact's 12 tests would never have run. -- satisfied: yes

### WP-02 -- Fidelity harness, red-proved against population 1 (Python both sides) (status: WIP)

- AC-2.1 (non-test)  -- satisfied: no
- AC-2.10 (non-test) The harness's ability to grade CRAWL slides is contingent on the prefers-reduced-motion fix, and the contingency is stated wherever a crawl number is reported. Without it there is no working capture phase for a crawl at all: early in the dwell the text is off-frame, later it is mid-animation and non-deterministic. The fix collapses both by parking the text at its natural static position. Until hv rules, no crawl-slide number is a result. -- evidence: Contingency DISCHARGED, and RE-EVIDENCED 2026-09-09 after the instrument that produced the original figure was found to be anchored on the first capture. hv ratified the reduced-motion crawl fix; snorkeltoast applied it (3903937) and rebuilt as reel 008. The citable evidence is now snorkeltoast's FLOORS.md, generated from control.json rather than retyped, artifact sha256 daaa503ad7db, 13 captures, measured 2026-09-09T12:03:45Z: slide 20, the outro crawl, reads 1 render / floor 0.000000 / stability 0.000000, against 10.65 before the fix. WHY THE FIRST EVIDENCE WAS REPLACED RATHER THAN KEPT: it cited spread 10.831547 to 0.000000 across four render clusters to one, and snorkeltoast has since measured that spread was anchored on rep-0, so a slide whose odd capture happened to be first collapsed to a spread of zero -- two 13-capture runs over one artifact disagreed about slide 14 and that disagreement exposed it. The BEFORE figure is unaffected (the failure mode understates, it does not inflate) and the cluster count is hash-derived and independent of the anchor, but an AFTER figure of 0.000000 is precisely the failure mode's signature, so it is not the number to stand on. The corrected instrument agrees, which is why the conclusion is unchanged. CORRECTED CLAIM: the earlier evidence said the reel has no genuinely ungradeable slide left. FLOORS.md reads 19 gradeable, 2 UNDECIDED, 1 ungradeable of 22 -- slides 13 and 14 await a named magnitude (AC-2.17) and slide 1 awaits the brand literal (AC-3.7). Second confirmation, unasked-for and still standing: slide 1's presence figure rose 28.343 to 53.432 against an UNCHANGED stripped-render control. -- satisfied: yes
- AC-2.11 (non-test) The capture window has two real bounds and the contract states whether both can be met. VC'S EARLIER FRAMING WAS BACKWARDS AND IS CORRECTED HERE: ambient MITIGATES rather than compounds, because ease is capped at max_ease so the 1.4 multiplier never bites at the cap while dwell's 1.5 always does. Measured -- dwell 2500 ease 3000 gives a window of MINUS 500ms at default pace (the transition outlasts the slide) and PLUS 750ms under ambient. Default pace is the broken case, at any configured ease at or above 2500. vc computed only the ambient case and described it comparatively against a default case never computed: a population of one, reported as a comparison. Once AC-3.11's compile-time cap and refusal land, no config reaching the runtime can have ease >= dwell and this bound is closed at the source. AND THE ROW'S POPULATION IS THE HARNESS, NOT ONE COMMAND: measured 2026-09-09 by snorkeltoast, both bounds are computed and lo >= hi returns None in control and compare, with selftest covering a legal ease-3000 / dwell-2500 windowless config and both sides of the narrowest window (dwell 1301 gives (1100,1100); dwell 1300 gives None), so FLOORS.md's "0 windowless" is a RESULT and not a tautology. BUT cmd_capture and cmd_redproof pass no budget_for and capture at a flat 4000ms, computing no window at all -- two of four commands honour the bounds. Harmless on this reel, whose window is 1100..4520 so 4000 sits inside it, which is why redproof's numbers stand; on a legal dwell-2500 reel the same path photographs the NEXT slide while labelling it this one. FIXED AT snorkeltoast 28b4127 AND VERIFIED BY vc AT THE SOURCE, with one leg still unverified. The derivation is now ONE windows_for() at showreel-harness:381 with three consumers -- cmd_capture, cmd_control and cmd_redproof -- where control had it inline and capture and redproof had nothing; and capture() now DIES with "capture() was called with no per-slide budget" rather than defaulting. snorkeltoast's reasoning is the right one and is worth keeping: fixing it at the two call sites would have left the default sitting there for the next caller, and A DEFAULT THAT IS WRONG ON SOME INPUTS CANNOT BE FIXED BY REMEMBERING TO OVERRIDE IT. Re-measured against 008 at the corrected budgets, 1100ms on all 22 slides against 4000ms before: slide 6 red at 0.999752 unchanged, 13 at 0.001500 and 14 at 0.002196 at their control floors exactly -- the injury figure holding at a budget nearly four times earlier in the dwell, which is what reduced motion predicts and is now measured rather than assumed. THE UNVERIFIED LEG IS cmd_compare, which is not among windows_for's three call sites; vc has not established where it takes its budgets from, and grading a row on three of four commands after the row was minted about two of four would repeat the error in a smaller size. THE FOURTH LEG WAS A REAL GAP AND NOT A BOOKKEEPING ONE -- snorkeltoast established it after vc declined to satisfy the row on their summary, and their earlier statement that both bounds were computed "in control and compare" was wrong about compare. compare CAPTURES NOTHING: it reads two manifests and diffs the shots already in them, so the budgets are whatever the commands that wrote those directories recorded, and nothing compared them. LIVE UNTIL 28b4127: `capture` wrote 4000ms manifests while `control` wrote per-slide ones, so comparing a capture-made directory against a control rep graded 4000ms frames against 1100ms ones -- a real number measuring a PHASE difference and presenting it as a build difference, which is the pace=ambient defect of AC-2.13 arriving through a different door. Fixed at fab0924: compare refuses before the partition and before any pixel, under F2's existing rule that incomparable sides fail without a number. RED-PROVED BOTH WAYS, and the red case is the one worth reading -- the same shots with two budgets doctored IN THE MANIFEST ONLY, pixels BIT-IDENTICAL, giving "CAPTURE MISMATCH -- 2 slide(s) were photographed at different budgets. No pixel number is reported", exit 1, and grep -c RMSE returning 0. The old code would have printed RMSE 0.000000 down the whole reel and passed green. THE CHECK HAS TO FIRE ON THE PROVENANCE OF THE COMPARISON RATHER THAN ON ITS RESULT, BECAUSE THE RESULT IS PERFECT. Both paths now derive from windows_for, which means they agree because two code paths compute the same thing -- not a guarantee, and what an assertion is for. Discharged when the refusal is verified by vc at the source, the last leg vc has not read. -- evidence: THE LAST LEG IS READ, which was this row's whole discharge condition and was deliberately not taken on snorkeltoast's summary -- they had been wrong in this precise spot earlier the same day, having told vc that both bounds were computed "in control and compare" when compare computed nothing. VERIFIED BY vc AT THE SOURCE. load_manifest at showreel-harness:708 is five lines: it refuses a missing manifest.json and returns json.loads, validating nothing, exactly as reported -- so the budget agreement cannot live there and must live in the caller. It does. cmd_compare carries a CAPTURE block under the comment "the captures agree about WHEN, or there is no comparison", with two refusals: :1213 dies when a capture records no budgets, and :1222 dies with "CAPTURE MISMATCH -- N slide(s) were photographed at different budgets". BOTH SIT BEFORE THE PARTITION AND BEFORE THE PIXEL STAGE -- the only RMSE print in the function is :1309 -- and die() at :85 ends in sys.exit(1) with no try/except anywhere in cmd_compare, so the refusal is terminal rather than advisory. The derivation is one windows_for() at :381 with three consumers, and capture() at :648 now dies rather than defaulting to 4000. RE-MEASURED against 008 at the corrected budgets, 1100ms on all 22 slides against 4000ms before: slide 6 red at 0.999752 unchanged, 13 at 0.001500 and 14 at 0.002196 at their control floors exactly. THE CAVEAT SURVIVES SATISFACTION AND IS SNORKELTOAST'S OWN: both paths now derive from windows_for, so they agree BECAUSE TWO CODE PATHS COMPUTE THE SAME THING, which is not a guarantee and is what an assertion is for. A shared derivation is a much better position than two independent computations and it is not the same as a check. -- satisfied: yes
- AC-2.12 (non-test) Where the harness fixes a capture time, it states the machine and measurement it came from, because settling is a property of the ENVIRONMENT as much as the content and a value measured on one box is a lucky constant elsewhere. Converge-until-two-frames-agree is the preferred shape and is to be decided on that merit alone -- NOT as a fix for the crossfade misdiagnosis that prompted it, which AC-2.13 addresses instead. Its cost is named: convergence and the transition are the same phenomenon, so a converge loop settles correctly on a crossfade but on a CRAWL only after the animation ends, which is past the dwell. The bound therefore does real work and hitting it is common rather than exceptional. NOT SATISFIED, MEASURED 2026-09-09 by snorkeltoast against their own harness: capture times are DERIVED per slide in control and compare, so the row mostly does not bite -- but where a number is fixed, NO MACHINE IS NAMED ANYWHERE. SHOT_TIMEOUT_S 45 and SHOT_SETTLE_S 0.6 carry their measurement ("the shot lands in ~2s and the process sat for the full 90s timeout") and no host. The manifest records the Chrome version and the viewport and records no host. And the 4000ms fallback of AC-2.11's two unbounded commands is a fixed capture time with NO measurement behind it at all, which is the sharper half: a constant with neither a machine nor a reading is not a lucky constant, it is an unexamined one.
NARROWED 2026-09-09 17:50Z BY vc, MEASURED AT THE CURRENT HARNESS: THE SHARPER HALF IS FIXED AND ONE CONCRETE THING REMAINS. The 4000ms fallback is GONE. `capture` read `budget_for(...) if budget_for else 4000` and now takes no default at all, refusing without a per-slide budget, with the reasoning recorded at the site: "the 4000 is the exact latent bug capture_budgets() documents -- LIMITS permits min_dwell 2500, so on a legal reel a flat 4000ms photographs the wrong slide". Every remaining occurrence of 4000 in the harness was read rather than counted: two are a test fixture's ease value, one is a selftest case name, and the other six are comments recording the defect's history. NONE IS A LIVE FIXED CAPTURE TIME. So the half of this row that had a constant with neither a machine nor a reading behind it is discharged by the constant ceasing to exist, which is the better of the two available fixes. WHAT REMAINS IS EXACTLY ONE THING AND IT IS CHEAP. SHOT_TIMEOUT_S 45 and SHOT_SETTLE_S 0.6 still carry their measurement -- "the shot lands in ~2s and the process sat for the full 90s timeout" -- and still name NO HOST; and the manifest still records the Chrome version and the viewport and no host, verified by grepping the harness for platform, uname, hostname and node() and finding only an unrelated importlib.machinery import. THE MANIFEST ALREADY RECORDS A SET THIS BELONGS TO. Chrome version and viewport are there because they change what settles; the machine changes it for the same reason and is the one member missing. DISCHARGED WHEN the manifest records the host alongside the Chrome version and the viewport, and the two constants say which box their measurement came from. Converge-until-two-frames-agree remains the preferred shape and remains undecided on its own merits; it is not required by this row and must not be smuggled in by it.
CLOSED 2026-09-09 18:48 local at snorkeltoast's 7c53c60, AND THE FIELD IS A BETTER SHAPE THAN vc ASKED FOR. vc asked for a host. snorkeltoast took the invitation to say the field was wrong and said it was the right field in the wrong SHAPE: A HOSTNAME IS AN IDENTIFIER, NOT A PROPERTY -- it survives the hardware being replaced underneath it and it differs between two machines that would render identically -- so grading a floor against it would be the adjacency failure one field wide, the same shape as the compiler_sha256 key they renamed this morning. What lands is a BLOCK with the label beside the properties: node, system, release, machine, cpu_count. AND compare READS IT, which vc did not ask for and the row wants: "a field recorded and never read is the weaker half of recording it", which is the failure vc's -S'ease' zero and their own adjacency key both had. REPORT RATHER THAN REFUSE IS RULED CORRECT BY vc, WITH A REASON snorkeltoast DID NOT GIVE AND A CONDITION THAT WOULD FLIP IT. Their reason was that they could not name an input where continuing produces a wrong answer rather than a caveated one, and a refusal that cannot be justified is one somebody routes around. The stronger reason is the POPULATION: the third branch fires when either capture lacks the block, which is EVERY capture taken before today, including the two font captures compared this afternoon -- so a refusal there would enforce a rule against data that predates the rule, and the only route to compliance would be re-capturing everything. The second reason is that NOBODY HAS MEASURED whether per-slide floors survive a machine change, so a refusal would assert a fact nobody holds. FLIPS TO A REFUSAL IF that measurement ever lands and says floors do not survive the move; until then reporting states the fact and leaves the inference to whoever has the second machine, which is the honest position while the question is open. -- evidence: Verified by vc 2026-09-09 18:05Z at snorkeltoast's source and driven where driving was possible. Landed 7c53c60, +62 lines, one file.

THE MANIFEST CARRIES IT UNCONDITIONALLY, WHICH IS WHY READING IT IS CONCLUSIVE HERE. `"host": host_fingerprint()` is a literal key in capture's manifest dict with no branch and no condition, sitting directly alongside `chrome` and `viewport` -- the two members of the set the row says it belongs to. Every manifest capture writes carries it by construction, so there is no runtime path that omits it.

THE BLOCK'S SHAPE DRIVEN RATHER THAN QUOTED: running the same five primitives here returns {"node": "rhadamanth", "system": "Darwin", "release": "25.6.0", "machine": "arm64", "cpu_count": 16}, matching what snorkeltoast reported without vc taking it from their message.

vc ALSO SEARCHED FOR A REAL MANIFEST AND FOUND ZERO, AND THAT WAS vc'S POPULATION ERROR RATHER THAN A GAP. `find` over the whole Snorkeltoast tree returns NO manifest.json at all, because capture's out_dir is caller-supplied scratch that is rmtree'd and recreated per run and never lands in the repository. The search asked the tree for something that lives outside it. Named here because an empty find is exactly the reading vc's own board says not to take at face value.

BOTH CONSTANTS BOXED, AND THE EXEMPTION IS STATED RATHER THAN SILENT. SHOT_SETTLE_S 0.6 and the ~2s / 90s measurements now carry a paragraph saying they were measured on ONE MACHINE and that until today the file did not say which -- "a slower or more loaded machine moves the first, and a reader who inherits these numbers has no way to tell whether they were ever true where they are standing". SHOT_TIMEOUT_S 45 is DELIBERATELY LEFT ALONE with its reason at the site: it is a ceiling on a hang rather than an estimate of a shot, and a number chosen to be comfortably wrong in the safe direction should not be re-derived from a faster machine. That is the right call and is the difference between a measured constant and a bound.

COMPARE READS THE FIELD, THREE BRANCHES, ALL VERIFIED AT SOURCE, AND THE BLOCK SITS AHEAD OF THE AC-2.16 EXPECTATIONS AND THEREFORE AHEAD OF THE STRUCTURE CHECK. Properties differ: names which keys, prints reference against new for each, and says a floor carried across describes a machine that is not this one. Only `node` differs: says so, and points at the properties beside it as what a pixel difference should be read against, rather than treating a rename as a machine change. Neither side has the block: says the captures predate the host record, so whether they ran on one machine is NOT ESTABLISHED -- which is the honest third state rather than an assumed yes, and it has a real non-empty population arriving immediately, since every capture taken before today lacks the field.

THE ROW'S OTHER HALF WAS ALREADY DISCHARGED BY THE CONSTANT CEASING TO EXIST, measured separately: capture read `budget_for(...) if budget_for else 4000` and now takes no default at all. Every remaining 4000 in the harness was read rather than counted -- two a fixture's ease value, one a selftest case name, six comments recording the history -- and none is a live capture time.

CONVERGE-UNTIL-TWO-FRAMES-AGREE IS NOT SMUGGLED IN BY THIS CLOSURE. It remains undecided on its own merits, it is not in 7c53c60, and vc verified that. -- satisfied: yes
- AC-2.13 (non-test) The harness never passes a venue-override query parameter. Measured 2026-09-09: passing pace=ambient made player.html's dwellOf/easeOf pair -- the AMBIENT ? 1.5 : 1 and AMBIENT ? 1.4 : 1 tokens, cited by token because 3903937 moved them off the line this row named until 2026-09-09 -- scale runtime dwell by 1.5 and ease by 1.4, so a payload ease of 900ms became a runtime 1260ms and a budget computed as payload_ease+200 = 1100ms landed INSIDE the transition. Every first-budget capture was a crossfade blend read as slide motion. pace, speed, start and the rest exist so a venue can change a shipped reel without a rebuild; a fidelity harness that passes one is grading a configuration the reel was never built for, and then doing arithmetic against the payload's un-scaled numbers as though the override were absent. -- evidence: VERIFIED BY CENSUS OVER AN ENUMERABLE POPULATION, and the census is the right instrument here because the claim is about what the source contains. snorkeltoast measured it both ways at vc's request: `grep -n pace showreel-harness` returns only the comment block, and the capture URL is `?start=N&noloop&kiosk&nobug` and nothing else. The comment carries the measurement that made it a rule -- slide 3 at budgets 1100 and 4520: with pace=ambient RMSE 142.827361, without it 0.000000, and with it at 1400 (past the SCALED ease) 0.000000 again. That is the defect this row was minted from: pace=ambient scales runtime dwell by 1.5 and ease by 1.4, so a payload ease of 900ms became a runtime 1260ms and a budget computed as payload_ease+200 = 1100ms landed INSIDE the transition, and every first-budget capture was a crossfade blend read as slide motion. THE LIMIT IS STATED RATHER THAN HIDDEN: there is NO GUARD. Nothing fails if someone re-adds an override; a grep is the only instrument, and it is a census that must be re-run rather than a detector that fires. Satisfied on the census as it stands 2026-09-09 against the harness that produced daaa503ad7db, and honest that a later reader must re-run the grep rather than trust a green. -- satisfied: yes
- AC-2.14 (non-test) Where a defect in the artifact makes a gate unprovable, the harness NAMES it and counts the slide ungradeable rather than passing it. Measured 2026-09-09: the token add("Producer", REEL.producer || "Snorkeltoast") in player.html means blanking producer swaps a configured value for a literal rather than removing the row, so the presence control can never reach an empty render for a session crawl. CITED BY TOKEN AND NOT BY LINE, which is the correction: this row carried a line number until 2026-09-09 and 3903937 moved it. The dead number is deliberately not restated: to any scanner it is indistinguishable from a live one, which is the property that made the error invisible. Slide 1's presence figure of 28.34 PASSED the gate for the wrong reason -- both frames lack the session details and differ elsewhere. That 28.34 predates the crawl fix and FLOORS.md now reports 53.432 for the same slide; both are real, from different runs, and neither changes this row's point, but a reader reconciling them without this sentence could not. Session crawls therefore report presence UNPROVABLE with the player token as the reason, and when the literal goes the gate becomes sound with no harness change. AC-3.7 IS WITHDRAWN (hv, yak-shaving) SO THIS ROW'S INSTANCE IS PERMANENT, WHICH STRENGTHENS IT RATHER THAN WEAKENING IT. The producer literal is not going to be gated on, so slide 1 stays presence UNPROVABLE unless cc happens to drop the fallback at the template pull -- optional and ungated. That is exactly the situation this row exists for: a gate the artifact makes unprovable is NAMED and the slide is counted ungradeable, permanently, rather than passed. The reel reads 21 of 22 and says why. -- evidence: CODE AND RAN AND OBSERVED, and snorkeltoast calls it the strongest of the ten. The case is named by token -- player.html hardcodes add("Producer", REEL.producer || "Snorkeltoast") -- UNSTRIPPABLE is the harness's own name for it, verdict_for returns "presence UNPROVABLE", the slide is counted into ungradeable rather than passed, control.json records presence_unprovable, and compare refuses to grade it. Slide 1 in the run behind FLOORS.md, and covered twice in selftest. CORROBORATED AT snorkeltoast 28b4127, which is the citation to use: that run grades 20 slides and excludes exactly ONE by name, and the one is slide 1's unprovable presence. vc's first evidence cited an earlier red-proof reading "3 not-gradeable slide(s) were excluded", which is a weaker demonstration of this row -- two of those three were excluded as UNDECIDED under a control carrying no min_defect, for a reason that has nothing to do with an unprovable gate. AC-3.7 IS WITHDRAWN (hv, yak-shaving), SO THIS ROW'S INSTANCE IS PERMANENT AND THAT STRENGTHENS IT: nothing will ever gate on the producer literal, so slide 1 stays presence-unprovable unless cc happens to drop the fallback at the template pull, which is optional and ungated. The reel reads 21 of 22 and says why. Slide 1's earlier presence figure of 28.34 PASSED for the wrong reason -- both frames lack the session details and differ elsewhere -- and FLOORS.md now reports 53.432 for the same slide; both are real, from different runs, and a reader reconciling them needs this sentence to do it. -- satisfied: yes
- AC-2.15 (non-test) A per-slide floor is not derived from a single control run, because an artefact that MIGRATES between slides is attributed to whichever one showed it that day -- and the slide left at 0.000000 goes falsely red the next time it lands there. Measured 2026-09-09: the same sub-pixel artefact appears at x=145, one pixel wide, four pixels differing, max delta 2, on slide 14 under a 4000ms budget and on slide 13 under 1100ms. Both are left-aligned content templates and 145 is the left edge of the content block in each. Where an artefact has a NAMED CAUSE spanning a class of slides, the floor is a class floor at the largest observed value for that class, justified by the phenomenon rather than by which slide exhibited it. A class floor is not the global floor this contract rejects: a global floor is set by the worst slide in the reel and is wider than the signal, whereas a class floor is three orders of magnitude below the injected defect and is derived from a cause. -- WITHDRAWN: Minted on snorkeltoast's migration hypothesis, which their own test refuted. Six captures each of slides 13, 14 and 17, all fifteen pairs at a fixed 1100ms budget: slide 13 zero of 15 differ, slide 17 zero of 15, slide 14 FIVE of 15 and every one at exactly 0.002196. The artefact does not migrate -- slide 14 is BIMODAL, rendering one of two ways with no intermediate value, and slide 13's 0.001500 is a rarer instance of the same class rather than the same instance relocated. So per-slide floors stand as designed and the class-floor mechanism this row required is withdrawn with its proposer's agreement. The concern survives in AC-2.3 in a better form: a floor of 0.000000 recorded for a bimodal slide is a sampling artefact, not a measurement.
- AC-2.16 (non-test) The harness CONSULTS a recorded table of the port's predicted differences BEFORE it compares anything, so a difference the contract decided reads as a pass and an undecided one still fails loudly. THE PREDICTION MUST PREDATE THE BEHAVIOUR: a prediction written after the refusal is a rationalisation with a good filing system, and once both are on disk nothing tells them apart. THIS ROW NAMED TWO BEHAVIOURS AS ONE AND THAT WAS WRONG, CORRECTED BY snorkeltoast AGAINST vc'S BRIEF: AC-3.11's max_ease cap CLAMPS where the ease >= dwell pair REFUSES, and those are different KINDS -- a table filing the cap under refusal would wait for a refusal that never comes while the actual divergence walked past it as an unexplained pixel diff. So the row pins the property and not the list. Each entry carries its KIND (refusal, divergence, or blind -- blind being predicted to differ AND predicted to be invisible to this instrument, recorded so silence is not read as parity), predicates on the reel's INPUTS rather than on the port's error text, and records whether it has been OBSERVED or is only DESIGNED. The predicted set itself lives in design.md section 5 and in the harness's PORT_EXPECTATIONS; this row does not restate it, because an enumeration in a row drifts from the table it copies. -- evidence: Verified by vc 2026-09-09 17:09Z; ONE CITATION CORRECTED AT 17:26Z AFTER snorkeltoast REFUTED IT, AND THE CORRECTION IS RECORDED FIRST BECAUSE IT WAS vc'S ERROR IN THIS ROW'S OWN EVIDENCE. Built by snorkeltoast at 03a44ce and completed at 7ad5371, Snokeltoast tree.

THE CAP'S MARGIN WAS WRONG BY TWO HOURS AND TWENTY-TWO MINUTES, AND vc ASSERTED A COMMIT'S CONTENTS WITHOUT READING THEM. vc cited design commit 864a7f8 (10:31) as disposing of the max_ease cap. Tested: `max_ease`, `2400` and `MAX_EASE` occur ZERO times in that commit, with a control proving the instrument works on it -- `TN001` occurs 11 times. What actually happened, every stamp read off `git log -1 --format=%ci`: 930b31c 12:55 introduces limits.rs with MAX_EASE_MS = 3_000, the REFERENCE'S OWN NUMBER (max_ease 10 hits, 2400 zero, 3_000 three), so what lands there is that the compiler applies a cap AT ALL; 92ec041 14:46 records hv's ruling capping it at 2400 (2400, 8 hits); bdcd601 14:48 implements it (2400, 13 hits). THE CAP AT 2400 WAS DECIDED TWO MINUTES BEFORE IT WAS BUILT, NOT TWO HOURS AND TWENTY-FOUR. Decided-then-built and decided-and-built-in-one-breath are different states, and filing the second as the first, in the row whose entire subject is which came first, is the worst available place to be approximate.

HOW THE ERROR WAS MADE, BECAUSE THE MECHANISM IS THE REUSABLE PART: vc ran `git log -S'ease' -- design.md`, saw 864a7f8 in the output, and read "this commit touched design.md and matched 'ease'" as "this commit decided the cap". `-S'ease'` matches release, increase, please. A FOUR-CHARACTER SUBSTRING WAS USED AS A PROXY FOR A DECISION, and one member of a population vc never named was read as the answer -- which is the exact defect this contract has spent the day naming, committed by the node that holds the pen, inside the evidence for the row about predictions.

THE OTHER TWO CITATIONS SURVIVE THE SAME TEST. The ease >= dwell refusal: AC-3.11 minted 3438fd1 11:05 against limits.rs at 930b31c 12:55 -- the contract leads by 1h50m, exact. The theme refusals: design 151c5a7 17:47 for the .ttf, amended bbca403 18:01 for the missing font and the favicon, and NEITHER IS IMPLEMENTED, so for those the ordering is not merely provable but unarguable.

AND snorkeltoast CORRECTED THEIR OWN HEADER ON THE SAME POINT, unprompted: it claimed the table was built "while cc's build still refuses to build anything at all -- the only window in which it can be honest", which is true of three rows and not of the two `observed` ones, whose honesty rests on the CONTRACT deciding them first rather than on the table preceding the build. Every row now carries a `decided` field separate from `source`, and the selftest refuses a row without one (7ad5371) -- which was the one gap vc raised and it is closed.

THE MECHANISM IS WIRED INTO THE INSTRUMENT THAT WOULD OTHERWISE DISCOVER THE DIFFERENCE, NOT ONLY INTO A NEW VERB. `cmd_compare` reads the expectations and prints them BEFORE the structure check, and the code says why the position is the mechanism: "move this block below the structure check and it still prints the same words, and they stop being evidence of anything." A predicted structural difference prints as PREDICTED and names the entry that predicted it; an unpredicted one still reaches `die("STRUCTURAL MISMATCH -- N unpredicted difference(s)")`, whose remedy names the constraint -- add it to PORT_EXPECTATIONS "but only if it was decided before it was observed". The match is an EXACT STRING and the looser rule is refused with its reason: anything mentioning ease on a named slide would swallow a real ease regression on that slide.

THE TABLE PREDICATES ON INPUTS, NOT ON cc'S ERROR TEXT, so it does not stop matching the day somebody rewords a message -- and two of the five behaviours have no string to match because they are not written yet.

RUN AGAINST THE LIVE 45h REEL BY vc, 2026-09-09: 1 of 5 predictions fires, and it is the BLIND one. THAT IS THE GOOD OUTCOME AND THE LINE SAYS WHICH KIND OF ZERO IT IS -- report_expectations distinguishes "none of the 5 predictions apply to this reel's inputs" from a claim about the table, because "the two read identically unless the line says which". The four silent entries are detectors over CONFIGS and not over this reel; 45h authors no ease at or above its dwell, none above 2400, popupart ships favicon.svg, and all five fonts are .woff2. Two of the four fired on configs snorkeltoast constructed against the real binary, so those zeros are sound.

THE DETECTOR IS RED-PROVED WHERE IT CAN BE AND HONESTLY MARKED WHERE IT CANNOT. Two entries are `observed` with the observation quoted: dwell 3s / ease 4s drove cc's binary to exit 2 naming the segment, and authored ease 5s drove exit 0 with "timing 1 segment(s) clamped by the envelope" against Python's 5000. Two are `designed`, carry `evidence: None`, and carry `unobserved` saying why nothing could be observed. One is BLIND by construction. A selftest arm refuses the table if a row claims `observed` and cites nothing, or is `designed` and cites evidence, or is `designed` and does not say why -- AND THAT CHECK WAS ITSELF RED-PROVED BY INJECTION AFTER ITS FIRST VERSION WAS NOT: flipping a designed row to observed left it green, because both rows carried prose in `evidence` explaining that nothing had been observed, so the field meant "a paragraph" and the check tested that a paragraph was present.

WHAT THIS ROW DOES NOT CLAIM. It does not claim the port refuses a missing favicon or a .ttf today; those two entries are DESIGNED and flip to `observed` now that cc's `check` reads the theme, which is one field each. This row is discharged on the harness's mechanism and on the ordering, both of which are complete and both of which have now survived a refutation. -- satisfied: yes
- AC-2.17 (non-test) Gradeability is decided against a NAMED minimum defect, named by DESCRIPTION and computed, never taken from an injection's own figure and never a multiplier. A floor is acceptable iff it sits below it. hv RULED THE VALUE 2026-09-09: a one-level shift over 5 percent of the frame. That is RMSE 0.223607, because showreel-harness rmse() sums squared differences over RGB and divides by 3*W*H, so a one-level shift over area fraction f is exactly sqrt(f) -- INDEPENDENT OF FRAME SIZE, so the magnitude transfers to any reel at any resolution rather than being a fact about 1920x1080. At 1920x1080 that is 103,680 pixels, about 429x241. Both UNDECIDED slides clear it: slide 13's floor 0.001500 is 149x below and slide 14's 0.002196 is 102x below, so naming this magnitude makes 21 of 22 gradeable and leaves only slide 1, which waits on AC-3.7. THE ROW CITED 6.29 UNTIL 2026-09-09 AND IT WAS NEVER THIS INJECTION'S VALUE. snorkeltoast re-measured at 0.999752; 6.29 needs an average shift of 6.29 levels, which a one-level darkening cannot produce on any capture, so the arithmetic rules it out without snorkeltoast's pace=ambient contamination hypothesis, which is now unnecessary rather than merely unproven. TWO FURTHER CLAIMS OF THIS ROW ARE WITHDRAWN, BOTH vc's. First, the 0.998249 to 2.280042 spread was NOT the injection varying by picture content: snorkeltoast ran the control the script never had -- re-encode with no injection -- and the pure injection spans 0.4 percent, not 2.3x. The range was a JPEG generation the harness introduced by decoding and re-encoding, and on slide 0, a PNG logo on black, 2.24 of the 2.28 is re-encode alone. Second, vc explained the readings above 1.0 as pixels moving more than one level where the darken clips. Clipping can only LOWER the figure, never raise it: vc verified that sqrt(1 - clipped_fraction) predicts every pure value to six decimals -- 0.718 percent clipped gives 0.996404, 0.003 percent gives 0.999984 -- so the pure injection is near-exact and its only deviation is the pixels that physically cannot darken. WHAT SURVIVES IS THE SQRT-AREA LEG, WHICH WAS ALWAYS THE LOAD-BEARING ONE AND IS NOW EXACT: a full-frame one-level defect measures 1.000000 and a four-pixel one measures 0.001389, a factor of 720, so any constant taken from an injection is blind to defects of smaller extent by exactly the ratio of their extents. AND A STATED LIMIT OF THE RED-PROOF, snorkeltoast's, flagged because this row's earlier wording implied otherwise: redproof darkens AND re-encodes at q86, so its reported figure is not a pure one-level injection. Both plausible targets on this reel are JPEG so today's result stands, but a reel of PNG sources would red-proof against a mostly-different defect. THIS IS WHY hv NAMED A DESCRIPTION AND THE NUMBER IS COMPUTED FROM IT. NOT YET SATISFIED: the magnitude is named and nobody has run the harness with --min-defect 0.223607, so the buckets are predicted and not measured. -- evidence: MEASURED, not predicted. snorkeltoast ran the harness with --min-defect 0.223607 against artifact daaa503ad7db at 13 captures a slide: POPULATION 21 gradeable, 0 undecided, 1 ungradeable, 0 windowless, of 22. Slide 13 gradeable at 149x under the magnitude, slide 14 at 102x, slide 1 presence UNPROVABLE and waiting on AC-3.7's brand literal rather than on any magnitude. hv named the defect by DESCRIPTION -- a one-level shift over 5 percent of the frame -- and the threshold 0.223607 is computed from it as sqrt(0.05), which snorkeltoast re-derived independently rather than accepting: n = f*W*H pixels moving one level over three channels gives sum(d^2) = 3*f*W*H against a divisor of 3*W*H, so W and H cancel and the magnitude is a property of the DEFECT rather than of 1920x1080. It transfers to any reel at any resolution. snorkeltoast could have re-derived this bucket line exactly before the run -- verdict_for is pure and control.json holds every input -- and declined to circulate it, on the grounds that an exact re-derivation carries the authority of arithmetic while describing a run that has not happened. The line above is the run's own. LIMIT ON THIS EVIDENCE, ADDED 2026-09-09 AFTER IT WAS FIRST WRITTEN: the artifact named by that sha is UNTRACKED -- 23 files, 15 MB, zero under git -- so the hash identifies the build and does not let anyone obtain it. The measurement happened and the numbers are real; what the sha cannot do is what a sha is for. AC-2.18 carries the property and the decomposition; this row is satisfied on its own terms, which concern the METHOD, and a reader should not take the sha as an offer of verification it cannot honour. AND THE FOUR FIGURES IN THAT POPULATION LINE ARE NOT WORTH THE SAME, which is snorkeltoast's catch on their own table and it lands on this evidence. 21 gradeable is a MEASUREMENT -- each slide's floor tested against 0.223607. 1 ungradeable is a MEASUREMENT -- slide 1 returning at verdict_for's unprovable_reason branch. 0 windowless is a DETECTOR ZERO and a meaningful one: snorkeltoast proved capture_budgets() can return None in five cases including the legal ease 3000 / dwell 2500 that LIMITS permits and this reel never contains. BUT 0 undecided IS A TAUTOLOGY AND IS NOT EVIDENCE ABOUT THIS RUN. Verified by vc in the source: verdict_for tests unprovable_reason, then presence <= floor, then 'if min_defect is not None' which returns unconditionally, and the 'renders > 1' UNDECIDED branch sits BELOW it. Naming any magnitude whatever makes that count zero before a pixel is captured. What naming 0.223607 actually decided is that slides 13 and 14 came out GRADEABLE rather than ungradeable, which their floors at 149x and 102x under it settle -- name 0.001 instead and the undecided count is still zero and both slides fail. The load-bearing figure is 21, and the row rests on it alone. -- satisfied: yes
- AC-2.18 (non-test) Fidelity evidence names an artifact a later reader can OBTAIN or REBUILD, or states plainly that it cannot and why. MEASURED 2026-09-09 by snorkeltoast and verified by vc: the 45h reel is entirely untracked -- 23 files, 15 MB, ZERO under git, and matched by no ignore rule, so it is unadded rather than deliberately excluded, in a parent directory that does carry 3 tracked files. FLOORS.md therefore cites sha256 daaa503ad7db for a build existing on one machine: the hash identifies the artifact precisely and makes it no easier to obtain, and the sha's only function was to let a reader verify. A PROVENANCE LINE THAT CANNOT BE FOLLOWED IS MOST OF A PROVENANCE LINE MISSING. THE 15 MB IS NOT ONE DECISION AND MUST NOT BE PUT TO hv AS ONE, which is vc's measurement: showreel.yaml is 8 KB in 1 file and is the reel's DESCRIPTION; assets/ is 5.7 MB in 16 files and is the source; _out/ is 9.2 MB in 3 files and is DERIVED, regenerable from the other two and never a thing to commit; plus .raster/ at 256 KB of intermediate cache and a 12 KB .DS_Store, both of which want an ignore rule rather than a decision. So the repository question is 5.7 MB of source images, the config costs nothing, and 9.2 MB of the total is an answer nobody should store. AND OBTAINABLE IS NOT THE SAME AS BYTE-REPRODUCIBLE -- THOUGH THE MEASURED CAUSE IS STRONGER THAN THIS ROW ORIGINALLY GAVE, AND THE CONCLUSION IS UNCHANGED. This row said a rebuild differs because the encoder's bytes depend on its VERSION. MEASURED 2026-09-09 by snorkeltoast, five ways: the TTF-to-WOFF2 conversion in the reference's font_face() is NONDETERMINISTIC ACROSS PROCESSES AT FIXED VERSION -- same font, same interpreter, same fontTools 4.61.1, same brotli 1.2.0, five separate processes giving two distinct shas, and six probes spanning 36896 to 37028 bytes. Stable WITHIN a process, unstable across them, which is why nobody caught it: nothing ever converted the same font twice in one run. PYTHONHASHSEED pinned to 0 leaves 3 of 5 fonts still differing, so it is not hash randomisation. So version drift was a true cause and not the operative one, and committing config and assets makes the reel REBUILDABLE, not identical, because the build contained a fresh random draw every time. **THE SHA WAS NEVER A RECIPE, ONLY A NAME** -- snorkeltoast's distinction, and it is the correction to vc, who reasoned from *the conversion is nondeterministic* to *the sha is not a stable identifier* and skipped the step that the nondeterminism is in the PRODUCTION of the file and never in the HASHING of it. A sha is always a faithful function of bytes. daaa503ad7db identifies the bytes that were captured, exactly as before, and FLOORS.md's floors remain measurements of one specific artifact. What is newly known to be false is a claim this contract never made: that rebuilding 008's config would yield 008. **AND THE FIX MAKES IT REPRODUCIBLE FOR THE FIRST TIME** -- committing the WOFF2 bytes removes the only nondeterministic step, and two full builds of the whole reel now come out BYTE-IDENTICAL at ae90d9e88b0fcdba, where five builds of one font previously gave two results. So this clause is now true only of builds made BEFORE that change -- which is the same fact that makes WP-02 necessary for the Rust port at all. A rebuild that yields a different sha is not evidence of a defect and not evidence of its absence; only the harness can tell those apart. So the sha identifies, the inputs make it rebuildable, and the harness makes a rebuild gradeable, and provenance is followable only with all three. DISCHARGED, AND NOT BY TRACKING ANYTHING. hv RULED 2026-09-09 that the reel's inputs stay in Dropbox: the reel lives in a Dropbox worktree rather than a git one, its inputs can be arbitrarily large, and Utilz itself is fully tracked, which is what the hoist actually depends on. THIS ROW IS NOT AN ACTION ITEM AND MUST NOT READ AS ONE -- snorkeltoast's point when the ruling landed. The limit is permanent and TRUE: a later reader cannot resolve sha256 daaa503ad7db, and nobody intends to change that. What the row still requires is the honest half -- FLOORS.md states at the point the sha is read what a reader can and cannot do with it, which it does. The three-legged reading holds and is now the whole of it: the sha identifies, the inputs are unobtainable so nothing makes the reel rebuildable by a third party, and the harness is what makes any rebuild gradeable for whoever does hold the inputs.
THE ROW'S OWN PRINCIPLE HAS A SECOND INSTANCE AND NO ROW COVERS IT. Found by snorkeltoast 2026-09-09 20:36Z, verified at source by vc before recording. FLOORS.md's provenance says **"Every number below is generated from `control.json`, never retyped"** -- and `control.json` EXISTS NOWHERE. Measured: three TEXT mentions across the Snorkeltoast tree (FLOORS.md, HARNESS-STATE.md, showreel-harness) and ZERO files of that name. It was written to a temp dir and the temp dir is gone.
**THIS ROW ALREADY CONTAINS THE SENTENCE THAT CONDEMNS IT: "A PROVENANCE LINE THAT CANNOT BE FOLLOWED IS MOST OF A PROVENANCE LINE MISSING."** And the control-data case is STRONGER than the artifact case the row measured: the artifact at least exists on one machine and its sha identifies it precisely, whereas `control.json` exists on none. The floors can therefore only ever be RE-MEASURED, never verified against the source they claim.
NOT REOPENED, AND THE LINE IS DELIBERATE. This row's subject is "an ARTIFACT a later reader can obtain or rebuild", and it is satisfied on that subject by the second branch -- it states plainly that the artifact cannot be obtained, and why, at length. **`control.json` is control DATA, not the artifact, and stretching the row's noun to reach it would grade the row against something it does not say** -- which is the error this contract has corrected three times in the other direction. Recorded here so the row's silence about provenance-of-the-numbers is not later read as coverage of it.
OWNED AND NOT BLOCKING: snorkeltoast holds it as a WP-02 item for the next control run -- retain `control.json` somewhere durable and cite the retained path. Nothing is blocked, because hv's event rebuild changes the reference artifact and forces a re-measurement anyway. **It is adjacent to AC-3.6's citation problem and dies from a different cause: that one expires when a fix lands, this one died from tidying.** -- evidence: SATISFIED ON THE ROW'S SURVIVING REQUIREMENT, WHICH WAS ALWAYS THE HONEST STATEMENT AND NEVER THE REEL'S LOCATION -- snorkeltoast's words on hv's ruling landing, and they are right. hv RULED 2026-09-09 that the reel's inputs stay in Dropbox: the reel lives in a Dropbox worktree rather than a git one, its inputs can be arbitrarily large, and Utilz itself is fully tracked, which is what the hoist actually depends on. So the limit is PERMANENT and TRUE -- a later reader cannot resolve sha256 daaa503ad7db, and nobody intends to change that -- and the row requires the harness to say so where the number is produced, which FLOORS.md does. LEAVING THIS UNSATISFIED WAS ITSELF THE DEFECT: a row in state unsatisfied reads as an open action item to every later reader, and this row's own text says it must not read as one. The record is what gets fixed, not the reel. The three-legged reading stands and is now the whole of it: the sha IDENTIFIES the artifact precisely, the inputs are unobtainable so nothing makes the reel rebuildable by a third party, and the harness is what makes any rebuild gradeable for whoever does hold the inputs. Obtainable is not byte-reproducible in any case -- the encoder's bytes depend on its version, which is the same fact that makes WP-02 necessary for the port at all -- so a rebuild yielding a different sha is neither evidence of a defect nor of its absence, and only the harness separates those. The caveat sits where the number is produced rather than where it is reported, which is the only arrangement that survives the document being quoted without it. -- satisfied: yes
- AC-2.2 (non-test) A structural mismatch is asserted before any pixel comparison, and fails without reporting a pixel number at all. GRADED ON A SPLIT, snorkeltoast's own framing after they corrected themselves upward: the DETECTOR is red-proved on the real artifact in the run behind FLOORS.md -- cmd_redproof assertion 2 removes a slide from the payload and requires compare_structure to fail, observed at snorkeltoast 28b4127 as "79 structural difference(s), first: slide COUNT differs: reference 22, new 21". CITE THAT RUN AND NOT THE EARLIER redproof2.log, which reports the identical figure from a strictly weaker setup: it ran against a control carrying no min_defect, so slides 13 and 14 were excluded as UNDECIDED and only 18 slides were graded, where the 28b4127 run uses ctl10 -- the control FLOORS.md is generated from -- and grades 20 with one exclusion. THE CALLER'S ORDERING IS INSPECTION ONLY and redproof is candid about it in its own output, which asserts the ordering by pointing at cmd_compare's STRUCTURE block rather than observing cmd_compare refuse. So the mechanism that finds a mismatch has been made to fire against a real injury; the mechanism that guarantees no pixel number is printed first has been read, not run. THE ROW'S WORD "ids" IS CORRECTED RATHER THAN FAILED: slides carry no id -- collect_segment does not put a segment's `id:` into the payload -- and identity is position plus every rendering parameter except pixels, which is strictly STRONGER than an id would give. The absence is not a gap and must not be read as one. Discharged when cmd_compare's die-before-any-pixel-number is observed rather than inspected. -- evidence: BOTH LEGS NOW HOLD, and the ordering leg closed by reading rather than by report -- vc's own condition, because snorkeltoast had been wrong about compare once already today. THE DETECTOR IS RED-PROVED against a real structural injury on the real artifact, in the run behind FLOORS.md: cmd_redproof assertion 2 removes a slide from the payload and requires compare_structure to fail, observed at snorkeltoast 28b4127 as "79 structural difference(s), first: slide COUNT differs: reference 22, new 21". THE ORDERING IS NOW OBSERVED BY DOMINATION AND NO LONGER INSPECTION-ONLY, which is the upgrade. Read by vc at the source: die() ends in sys.exit(1); cmd_compare carries NO try/except anywhere in its body, so the path is linear; the STRUCTURAL MISMATCH die is at showreel-harness:1184 and the ONLY RMSE print in the function is at :1309, 125 lines later, past the partition and the pixel stage. And snorkeltoast's fab0924 red-proof OBSERVED an exit with grep -c RMSE returning 0 at the CAPTURE MISMATCH die on :1222 -- which is STRICTLY LATER in the same linear function using the same primitive, so a refusal at :1184 reaches pixels only if one at :1222 does, and that one is measured not to. An observation at a later die point dominates every earlier one on a linear path. THE ROW'S WORD "ids" IS CORRECTED RATHER THAN FAILED: slides carry no id -- collect_segment does not put a segment's id: into the payload -- and identity is position plus every rendering parameter except pixels, which is strictly STRONGER than an id would give. The absence is not a gap. -- satisfied: yes
- AC-2.3 (non-test) Stability of the noise, not presence of it, decides gradeability; the repeat count is FLAT across every slide; and where a floor's structure is known it is a derived CEILING rather than a largest-observed value. MEASURED 2026-09-09, thirty captures of slide 14 by hash: THREE renders -- A=21 (0.700), B=8 (0.267), C=1 (0.033) -- so P(miss)=sum(p_i^n) is 56.2 percent at n=2, 16.9 at n=5, under 5 percent first at n=9 and under 1 percent at n=13. Thirteen captures per slide. AN ADAPTIVE PER-SLIDE COUNT IS REFUSED and this row said the opposite until it was measured: deciding a slide is stable requires the same evidence as proving it is, so escalating only the slides that look unstable under-detects exactly those that need it. THE TAIL IS CLOSED STRUCTURALLY, NOT SAMPLED: all three renders differ only at the two ENDPOINTS of one 59-pixel edge at x=145, so the renders are (top,bottom) with each end rounding independently -- A=(0,0), C=(0,1), B=(1,1), and a predicted D=(1,0). The maximum possible difference is both endpoints flipping, which IS A-to-B, so 0.002196 is a ceiling no unobserved mode can exceed. Confirmed independently by magnitude as well as geometry: RMSE(A,B)/RMSE(A,C) = 1.41404 against sqrt(2) = 1.41421, which is what 4 differing pixels against 2 of equal delta predicts. -- evidence: SATISFIED on the 2026-09-09 run against artifact daaa503ad7db. FLAT COUNT: 13 captures a slide, every slide, plus one late-budget frame and one content-stripped control, no adaptive escalation. STABILITY IS A SEPARATE COLUMN from the floor that decides gradeability. THE CEILING IS TESTED RATHER THAN ARGUED, and this row was itself the prediction that tested it: two endpoints rounding INDEPENDENTLY predicts FOUR combinations, not the two it was written from. The run saw a THIRD render of slide 14 where ctl9 saw two, at exactly the distance independence requires -- one endpoint is 15 squared-difference units over rmse()'s divisor of 3*W*H and two are 30, so C-A and C-B are both sqrt(15/6220800) = 0.001553 and A-B is sqrt(30/6220800) = 0.002196, additive to the unit. That reproduces this row's independently measured RMSE(A,B)/RMSE(A,C) = 1.41404 from absolute counts rather than from a ratio. THE PREDICTIVE ARM ON SLIDE 13 IS STRUCTURAL AND NOT ARITHMETIC, which is snorkeltoast's correction to vc and it stands: the model, derived from a 59-pixel edge, predicts the SHAPE of slide 13's difference -- exactly four pixels, two per endpoint, at the two ends of a single vertical edge -- and counting on its 74-pixel edge confirmed it. vc had also offered sqrt(14/6220800) = 0.001500 against slide 13's reported floor as a second and stronger arm. IT IS NOT AN ARM AT ALL: rmse() IS sqrt(sum(d^2)/N), so that recomputes the reported figure from the same measured pixels and confirms the pixel COUNT rather than the model, which was never consulted on either side. Worth having as a consistency check that would catch a miscount; not evidence for the mechanism, and it is a correct number carrying more authority than it earned. THE CEILING IS NOW FALSIFIABLE AND THE UNUSED PREDICTIONS ARE ON THE RECORD IN FLOORS.md BEFORE ANY RUN TESTS THEM: slide 14's fourth state must land at 0.001553 or 0.002196; slide 13 has shown only two of four renders and a third must land at sqrt(7/6220800) = 0.001061 or at 0.001500. Any other distance falsifies the account, on slide 13 falsifying it on the slide the model was not built from. THE CEILING DOES NOT GENERALISE: any other multi-render slide is a lower bound until someone counts its pixels, because the mechanism must be identified before the argument is available at all. LIMIT ON THIS EVIDENCE, ADDED 2026-09-09 AFTER IT WAS FIRST WRITTEN: the artifact named by that sha is UNTRACKED -- 23 files, 15 MB, zero under git -- so the hash identifies the build and does not let anyone obtain it. The measurement happened and the numbers are real; what the sha cannot do is what a sha is for. AC-2.18 carries the property and the decomposition; this row is satisfied on its own terms, which concern the METHOD, and a reader should not take the sha as an offer of verification it cannot honour. -- satisfied: yes
- AC-2.4 (non-test) An injected known change shows red before the harness grades any Rust build.
NOT SATISFIED, AND THE REASON IS A STALE BINDING RATHER THAN A MISSING RED-PROOF. Measured by vc 2026-09-09 17:47Z. The red-proof exists and is recorded: FLOORS.md says `redproof` "currently passes: the injected pixel defect goes red on exactly one slide at 0.999752, and the injected structural defect fails before any pixel number is reported". Two injections, one discriminating on a single slide and one refusing at the right stage -- exactly what this row asks for. THE PROBLEM IS THE WORD "CURRENTLY". FLOORS.md was written at 14:21 local and the harness has been committed to SIX TIMES since. Measured by reading each commit's HUNK CONTEXT rather than its diff text -- which functions the changed lines actually fall inside: cmd_redproof's call graph is capture, compare_structure, load_compiler, rmse and signature, and since 14:21 the commits touched cmd_redproof ITSELF (28b4127), capture (28b4127), compare_structure (03a44ce and 7ad5371), load_compiler (03a44ce), and shoot twice (fab0924, 186af24), which capture calls. FOUR OF FIVE CALL-GRAPH MEMBERS PLUS THE FUNCTION ITSELF. So "it currently passes" is not a slightly stale record; it is a record about a different program, and this row must not be satisfied on it. AND VC'S FIRST INSTRUMENT RETURNED THE OPPOSITE ANSWER, CLEANLY, WHICH IS THE PART WORTH KEEPING. A grep for added or removed lines matching `def cmd_redproof|def rmse|def capture|def shoot|PAYLOAD_RE|def compare_structure` returned ZERO for all six commits -- a plausible, well-formed zero that would have licensed satisfying this row on the 14:21 record. It is a TAUTOLOGY: the pattern can only fire when a DEFINITION LINE changes, and none of these commits changed a signature, so it could not have fired however much the bodies moved. The detector was named "touches redproof machinery" and measured "changes a def line of redproof machinery", and THE NAME CONCEALED THE NARROWING -- a detector whose population is a strict subset of the thing its name claims, which passes a "has it ever fired?" check because it fires happily on a commit that adds a function. DISCHARGED WHEN `redproof` is re-run against the current harness and both injections are shown red again, with the run dated after bc6c3fe.
RE-RUN 2026-09-09 18:42 LOCAL AGAINST THE HARNESS AT bc6c3fe, BOTH INJECTIONS RED, EXIT 0. Artifact 008 (daaa503ad7db), reel008 reconstruction, ctl10 floors. PIXEL: slide 6 alone at RMSE 0.999752, the other twenty graded slides at their control floors, and one not-gradeable slide EXCLUDED BY NAME -- slide 1, the unprovable brand literal -- rather than silently tolerated. STRUCTURE: 79 differences, the first being "slide COUNT differs: reference 22, new 21", and compare exits there before the pixel stage, which is the ordering property this row wants as much as the redness. -- evidence: Verified by vc 2026-09-09 17:58Z, on a run snorkeltoast dated 2026-09-09 18:42 local against the harness at bc6c3fe -- AFTER the six commits that made the previous record a record about a different program.

BOTH INJECTIONS RED. Pixel: slide 6 alone at RMSE 0.999752, the other twenty graded slides at their control floors, one not-gradeable slide EXCLUDED BY NAME (slide 1, the unprovable brand literal). The injection is DISCRIMINATING rather than a blanket failure, which is what makes it a proof of the instrument and not merely of its ability to fail. Structural: 79 differences, first "slide COUNT differs: reference 22, new 21", with compare exiting there BEFORE the pixel stage -- the ordering property this row wants as much as the redness, since a pixel number computed across a structural mismatch is meaningless.

"BEFORE THE HARNESS GRADES ANY RUST BUILD" HOLDS BY CONSTRUCTION AND NOT BY LUCK: there is no `build` verb yet, so no Rust artifact has been graded by anything.

THE CAVEAT TRAVELS WITH THE EVIDENCE, AND snorkeltoast RAISED IT AGAINST THEMSELVES. The RMSE is unchanged from the 14:21 run, and that is NOT an invariance result across 28b4127's budget change from 4000ms to 1100ms. It is the same number because it is the same FRAME -- both reference PNGs of slide 6 hash to bac557ab0fc4, byte-identical across the two runs. VERIFIED BY vc AT THE MECHANISM: player.html:389 carries `.slide img{animation:none!important}` inside the reduced-motion block, and the harness shoots under --force-prefers-reduced-motion, so an image slide is static and every budget inside its window photographs the same pixels. Slide 6 is `motion: kenburns` with `fit: cover` in the live config, so it LOOKS like the slide where a budget change must show and is precisely the slide where it cannot. SO THIS RUN DOES NOT RE-PROVE 28b4127'S BUDGET FIX. That fix remains load-bearing -- it decides which frame of a crawl or a transition is taken -- and exercising it needs a crawl or transition target, which nobody has built and which this row does not require.

WHAT ELSE MOVED WAS THE CONTROL, NOT THE HARNESS: the 13:06 run graded 18 with 3 excluded, this one grades 20 with 1, because ctl10 replaced an earlier control. Recorded so the improvement is not read as an effect of the harness changes.

FLOORS.md NOW CARRIES ITS OWN SUBJECT (e7b532f): the run date, the harness commit and the full caveat, replacing the bare "it currently passes" that this row refused to be satisfied on. The general form snorkeltoast wrote into the file is the reusable half -- A PASS WITH NO DATE IS A PASS WITH NO SUBJECT. -- satisfied: yes
- AC-2.5 (non-test) The exemption list is NAMED and DERIVED, and the harness refuses when exemptions plus covered do not sum to the slide total, or when a slide outside the named list is exempted. THE DIRECTION REQUIREMENT IS REMOVED, deliberately and with its reasoning on the record, and this is a correction to the row's intent rather than a weakening to fit what was built -- the distinction vc holds itself to, so the argument is stated rather than asserted. The row originally also required each exempt slide's expected difference to be stated as a DIRECTION. THAT REQUIREMENT WAS WRITTEN AGAINST A MODEL IN WHICH EXEMPT SLIDES RECEIVE A WEAKER GRADE. The model actually built gives them NO grade: exempt slides are not graded at all, so a stated direction would be consumed by no instrument, checked by nothing, and read by nobody. Prose serving no instrument is the exact shape hv cut four items for on 2026-09-09. SNORKELTOAST'S TEST IS WHAT DECIDES IT AND IT IS THE SAME TEST THAT KEPT AC-2.6, WHICH IS WHY IT IS TRUSTWORTHY HERE: AC-2.6's naming was worth writing because A READER WAS OBSERVED MAKING THE ERROR -- vc, working carefully, and cc next, reading the same lines to port capture in WP-03. AC-2.5's direction has no such reader and no such consumer. A measured reader making a measured error is the scope test passing; an absent reader is it failing; and the two rows were decided by the same test in opposite directions on the same afternoon, which is what makes it a test rather than a preference. WHAT SURVIVES IS THE WHOLE OF IT AND IT IS WHAT THE ROW WAS MINTED TO PROTECT. The list is DERIVED from the compiler's own plan() rather than hand-written, which is the property that stops a slide being quietly exempted; the run printed "covered: 19 exempt: 3 total: 22 of 22" and named all three by bucket; and partition() carries all three refusals -- overlap, sum, and out-of-range. NONE OF THE THREE HAS BEEN OBSERVED TO FIRE, AND THAT IS A SOUND DETECTOR ZERO RATHER THAN A TAUTOLOGY: a wrongly decomposed partition reaches every one of them, so they could fire. Selftest prints that limit on every run, which is the caveat sitting where the number is produced rather than where it is reported. -- evidence: The two surviving legs are measured and the removed one carries its reasoning in the row: list DERIVED from plan() not hand-written, run printed 'covered: 19 exempt: 3 total: 22 of 22' naming all three by bucket, and partition() carries overlap, sum and out-of-range refusals -- unfired, reachable by a wrongly decomposed partition, with selftest printing that limit on every run. DIRECTION removed on snorkeltoast's reader test, the same test that KEPT AC-2.6 the same afternoon. -- satisfied: yes
- AC-2.6 (non-test) Every determinism mechanism is named singly, and the mechanisms are named CORRECTLY for the artifact being graded. THIS ROW ASSERTED A FALSEHOOD UNTIL 2026-09-09 and the correction is snorkeltoast's, verified at the artifact by vc: the row said crawl determinism comes from --virtual-time-budget alone and that --force-prefers-reduced-motion does nothing to the crawl. THAT IS BACKWARDS FOR BUILD 008. player.html's reduced-motion block carries the rule `.crawl .text{animation:none!important}` -- cited by token -- and that rule IS the crawl fix. Its own comment records the measurement: the outro crawl went from RMSE 4.99 / 8.28 / 10.65 between captures to 0.000000 three times, and the comment states that the block previously claimed to drop all motion while naming .crawl zero times. Virtual time alone did NOT make the crawl deterministic; that is where slide 20's 12.45 came from. THE ROW WAS TRUE OF BUILD 007 AND THE FIX FALSIFIED IT, which is the hazard being recorded: a criterion describing a mechanism goes stale when the mechanism is repaired, and it goes stale SILENTLY because a row is prose and prose does not fail. The single-point-of-failure concern the row was minted for survives unchanged and is now the whole of it -- one mechanism described as two reads as covered when it is not. Discharged when the harness names each mechanism singly AND the naming is checked against the artifact it grades rather than against the build the row was written from. -- evidence: FIXED AT snorkeltoast 186af24 AND VERIFIED BY vc AT THE SOURCE -- one paragraph, ten inserted lines, one file, no code change, which is the right size for a row about naming. shoot()'s docstring previously named both flags and left which-does-what to inference. It now states the division outright: VIRTUAL TIME FIXES WHEN THE FRAME IS TAKEN, REDUCED MOTION FIXES WHETHER ANYTHING IS STILL MOVING WHEN IT IS, and neither is redundant. It carries slide 20's 4.99 / 8.28 / 10.65 / 12.45 as the evidence that they are not interchangeable, and it records that until the crawl fix the player's reduced-motion block named .crawl zero times, so a rising crawl kept animating and virtual time alone did not settle it. THE DOCSTRING NAMES THE ERROR A READER WOULD MAKE FROM ITS OWN PREVIOUS TEXT, in those words -- caveat at production rather than at report, applied to a docstring. AND THE REASON THIS ROW WAS WORTH BUILDING IS MEASURED RATHER THAN ASSUMED, which is snorkeltoast's test and the best form of it produced in this thread: A READER WAS OBSERVED MAKING THE ERROR. That reader was vc, working carefully, which is what put the falsehood into this row in the first place; and cc will read the same lines to port capture in WP-03, so the next reader is named and imminent. A measured reader making a measured error is the scope test PASSING. Contrast AC-2.12, where the reader who would use a recorded host machine was measured NOT to exist, and the same test fails. Same test, opposite answers, and the difference is a reader rather than a judgement about cost. -- satisfied: yes
- AC-2.7 (non-test) A deviation attributed to a slide is separated from a deviation attributable to its capture POSITION before any floor is set. Only reordering separates them; no number of repeats at one order can. SATISFIED on the reverse-order run, whose CONCLUSION stands: with the capture budget held constant, the deviation followed the slide across both orders and the untouched slides were identical in both. Its NUMBERS are superseded -- that run predates 6c278a2 and carried pace=ambient. And a THIRD variable was held constant and is now known to matter: the capture MOMENT. The artefact migrates between slides as the budget changes, so this row rules out position and does not rule out budget. -- evidence: Snorkeltoast e2380f5, verified by vc 2026-09-09. Full reverse-order capture against the forward control, compared per slide: slide 14 reads 0.002196 captured 14th forward AND 7th reverse; slide 20 reads 12.453773 captured 20th forward and 1st reverse; the twenty untouched slides are 0.000000 in both orders. The deviation follows the SLIDE, not the capture position, so cc's positional hypothesis is ruled out and no run-order floor sits under every slide. Discharged before any floor was set, which was the point. -- satisfied: yes
- AC-2.8 (non-test) A per-slide zero is not evidence unless the frame contained the slide's content. Measured 2026-09-09: against the unmodified player, slide 1's captured frame holds the sunburst ground and wordmark chrome and NOTHING ELSE -- no venue, city, date or action -- because the crawl text has not entered frame at that capture phase. The harness returned a clean zero having seen none of the slide that carries the session details, which is the most venue-specific content in the reel and the thing 'one build serves every venue' depends on. A green over an empty frame is the failure this row exists to make impossible. -- evidence: CODE AND RAN AND OBSERVED, answered from harness source by snorkeltoast at vc's request rather than inferred from FLOORS.md's output -- the distinction the row turns on, since a clean table cannot show whether a guard exists. control_blocked() folds absent, unprovable, undecided and ungradeable into one blocked set, and grade_per_slide never grades a blocked slide. In the run behind FLOORS.md, slide 1 printed "b slide 1 RMSE 0.000000" and was EXCLUDED, under a heading naming "3 covered slide(s) are NOT gradeable" and a closing note that a green here does not cover them and that the reasons are not all the same reason. So the exact artefact this row exists to refuse -- a per-slide zero over a frame that did not contain the slide's content -- was produced and refused as evidence in the same run, by name. Slide 1's clean zero is the measured instance: its captured frame holds the sunburst ground and wordmark chrome and none of the venue, city, date or action, because the crawl text has not entered frame at that capture phase. CORROBORATED AT snorkeltoast 28b4127, AND THE EARLIER CITATION IS DELIBERATELY REPLACED RATHER THAN KEPT. vc first cited redproof2.log's "3 not-gradeable slide(s) were excluded by name rather than tolerated" as corroboration. THAT CITATION DID NOT CORROBORATE WHAT IT WAS QUOTED FOR: that run used a control with no min_defect, so slides 13 and 14 were excluded as UNDECIDED -- a reason unrelated to this row's presence-and-content mechanism -- and only one of the three exclusions was on point. The 28b4127 run uses ctl10, the control FLOORS.md is generated from, where a magnitude is named and 13 and 14 are gradeable: 20 graded, 1 excluded, and that one is slide 1's unprovable presence, which is this row's mechanism exactly. A count of three that happened to include the case is weaker evidence than a count of one that IS the case, and vc quoted the three. -- satisfied: yes
- AC-2.9 (non-test) Frame STABILITY and frame PRESENCE are separate assertions and the harness makes both. Capturing at two virtual-time budgets and requiring identical frames establishes stability only: two identical EMPTY frames pass it, so a slide whose content never arrives within either budget is graded green while invisible. The presence half must be differential rather than thresholded -- the blankness detector it replaces read 55.75 percent 'has content' on the actual empty frame it was built to catch. The chosen budgets are themselves a parameter and are stated with their justification. -- evidence: CODE AND RAN, answered from harness source by snorkeltoast at vc's request. Three separate assertions reported in three columns, not one conflated measure: REPEAT (reproducibility), STABILITY (two budgets spanning the dwell), PRESENCE (the slide graded against a render of itself with its content stripped). THE PRESENCE HALF IS DIFFERENTIAL RATHER THAN THRESHOLDED, which is the row's load-bearing requirement: an empty frame cannot pass a comparison against a stripped render of itself, whereas the blankness detector this replaces read 55.75 percent "has content" on the actual empty frame it was built to catch. Two identical empty frames still pass stability, so stability alone grades an invisible slide green -- which is why the two assertions are separate rather than combined. THE BUDGETS ARE STATED WITH THEIR JUSTIFICATION as the row requires, in capture_budgets' docstring: lower bound ease + 200 so the transition has completed, upper bound dwell - 200 so the reel has not advanced, and the second sample at 60 percent of the remainder rather than a fixed offset because two samples 200ms apart would call a slow crawl stable. -- satisfied: yes

### WP-03 -- Rust build path: YAML, admission (C1), normalisation policy (C2), data-URI (status: WIP)

- AC-3.1 (non-test) An unknown key in showreel.yaml is refused, named, with the valid set. -- evidence: VERIFIED BY vc INDEPENDENTLY, AND THE CASE BUILT TO BREAK IT LOST. cc evidenced 8 deny_unknown_fields attributes against 8 structs. vc counted differently on purpose: 8 attribute LINES matching #[serde(...deny_unknown_fields...)], and 9 lines matching #[derive(...Deserialize...)] -- a nine-against-eight that would mean one deserialisable type accepts unknown keys. The ninth is segment.rs's `struct Just` inside #[cfg(test)] mod tests, a deliberate partial extractor pulling only `segments` out of the whole 45h fixture, where deny_unknown_fields would be WRONG because it would refuse the reel's other top-level keys and break the test. So coverage is complete over the PRODUCTION population -- Schema, Artist, Session, Bug, Social, OutroRow, Defaults, Reel, all eight carrying the attribute on the line after their derive. The row's exact property is tested by name in an_unknown_key_is_refused_named_and_with_the_valid_set, with a parse-whole control beside it, and the valid set is deliberately the union of compiler-read and player-read keys so the compiler does not refuse a key the player consumes. NOTED AND NOT CHARGED: cc's "8 structs" is the production population and vc's 9 is the file, and neither statement named its population -- which is a fourth instance of the class cc diagnosed in the same message, in cc's own evidence, and it is confirmation of their thesis rather than a fault. Nothing turns on it here because the two populations reconcile exactly and the difference is a test helper. -- satisfied: yes
- AC-3.10 (non-test) The third-party package count for the approved set is measured as a UNION, over a NAMED population, and stated. THE ROW'S OWN FIGURES WERE REFUTED 2026-09-09 BY THE NODE THAT MEASURED THEM, and the refutation is the row's own requirement turned on itself: it demanded that populations be named while stating a union that was not one. It claimed 59 packages against prez's 26, with 69 dismissed as a non-quotable sum overstating by 10 through shared transitive deps. MEASURED BY cc AND REPRODUCED INDEPENDENTLY BY vc, all seven figures agreeing: lockfile [[package]] entries 79 minus 3 first-party = 76; cargo tree --edges normal gives prez 13 and showreel 51; the union under normal edges is 64; under normal,build it is 69, with prez alone at 17. 59 REPRODUCES UNDER NO POPULATION EITHER NODE CAN CONSTRUCT, and neither does prez's 26. 69 DOES reproduce -- as a genuine union under normal+build edges, which is precisely the figure the row called the overstating sum, so THE ROLES OF ITS TWO NUMBERS WERE INVERTED. And the overlap the overstatement was attributed to is ZERO: comm over the two normal-edge trees returns no shared package at all, so there was nothing to overstate through. THE POPULATION IS HEREBY NAMED AS normal+build EDGES, union 69 with prez at 17, on cc's recommendation and vc's agreement: the lockfile's 76 counts resolved versions that nothing may build, normal edges undercount by hiding build-dependencies, and normal+build is what actually compiles, so it is the only one of the three that is a COST. VC'S OWN INSTRUMENT PRODUCED 65 AND 70 FOR THE TWO UNIONS BEFORE THE ERROR WAS FOUND, one above cc each time, and the near-agreement almost became an accusation that cc had computed a sum rather than a measurement. It was a BLANK LINE: cargo tree --workspace --prefix none emits a separator between member trees, the filter kept it and wc -l counted it as a package. Caught only because the symmetric difference came back empty while the counts differed, which is arithmetically impossible; with the blanks stripped the two sets are byte-identical at 64. A ONE-APART DISAGREEMENT IS THE SHAPE THAT HIDES, and here it would have hidden an error in the instrument rather than in the subject. Discharged when the stated figure and its edge population are quoted together and reproduce.
RESOLVED 2026-09-09 17:30Z, AND THE EDGE SET WAS NOT ENOUGH -- THE DEDUP KEY IS PART OF THE POPULATION TOO. vc measured the union on the agreed normal+build edges and got 71 against cc's 69, and prez at 17 against cc's 17. TWO APART, IN showreel ONLY, AT THE SAME TREE: measured at cc's own commit bdcd601 and at HEAD, showreel's third-party closure is 54 both times, so nothing moved underneath. THE CAUSE IS THE DEDUP KEY AND BOTH FIGURES ARE CORRECT ABOUT THEIR OWN POPULATION. cc counted distinct package NAMES; vc counted distinct name+version pairs. Two names occur at two versions each -- miniz_oxide at 0.8.9 and 0.9.1, syn at 2.0.119 and 3.0.4 -- and name-dedup reproduces ALL SIX of cc's figures exactly, predicted before it was run: showreel normal 51, showreel normal+build 52, prez normal 13, prez normal+build 17, union normal 64, union normal+build 69. THE STATED FIGURE IS 71, DEDUPED BY NAME+VERSION, WITH prez AT 17, because the budget is denominated in third-party code compiled and linked, and two versions of one crate are two compilations and two copies -- a name count makes exactly that invisible, which is the thing a dependency budget exists to see. Cross-checked on a second instrument: a workspace-wide walk returns the same 71 with a symmetric difference of ZERO against the per-package union. Build-only packages named rather than counted: showreel adds autocfg; prez adds entities, fastrand, phf_codegen, phf_generator. Overlap between prez and showreel remains ZERO, so the union equals the arithmetic sum here and the row's original "overstates by 10 through shared transitive deps" stays refuted. AND THE ROW'S OWN LESSON LANDED ON IT TWICE: it demanded that populations be named, was refuted for stating a union that was not one, and then the named edge set STILL under-determined the answer by two. -- evidence: Verified by vc 2026-09-09 17:30Z, reconciling against cc's refutation of the row's original figures.

THE FIGURE IS 71 THIRD-PARTY PACKAGES ON normal+build EDGES, DEDUPED BY NAME+VERSION, prez AT 17. Measured with `cargo tree -p <pkg> --edges normal,build --prefix none --offline`, blank separator lines dropped explicitly (the failure that produced vc's earlier 65/70), `(*)` repeat markers stripped, first-party names removed by DERIVING them from the workspace manifest rather than typing them.

TWO INSTRUMENTS, SYMMETRIC DIFFERENCE ZERO. Per-package union of prez and showreel gives 71; a workspace-wide walk gives 71; `comm -3` over the two sets returns nothing. That is the control the earlier blank-line error failed -- differing counts alongside an empty symmetric difference is arithmetically impossible, and it is what caught the error then.

THE DISAGREEMENT WITH cc WAS TWO PACKAGES AND IT IS NOT AN ERROR ON EITHER SIDE. vc 71, cc 69; prez agrees exactly at 17 and at 13, so the whole gap sits in showreel. IT IS NOT THE TREE MOVING: showreel's third-party closure measured in a detached worktree at cc's own commit bdcd601 is 54, and at HEAD is 54, with nothing arrived and nothing left. THE DEDUP KEY IS THE POPULATION DIFFERENCE. Predicted before measuring -- exactly two names appear at two versions -- and confirmed: miniz_oxide 0.8.9 and 0.9.1, syn 2.0.119 and 3.0.4. Name-only dedup then reproduces ALL SIX of cc's published figures without exception: showreel normal 51, showreel normal+build 52, prez normal 13, prez normal+build 17, union normal 64, union normal+build 69. Six of six is not a coincidence that needs arguing about.

WHY NAME+VERSION IS THE COST AND NAME IS NOT. AC02's budget is denominated in third-party code taken on. Two versions of `syn` are two crates fetched, two compiled, two linked; the same for miniz_oxide. A name count reports one where the estate carries two, and it does so silently -- which is precisely what a dependency budget exists to make visible. The lockfile's 76 remains the wrong instrument for the opposite reason, counting resolved versions nothing may build, and normal-only edges remain wrong for hiding build-dependencies.

BUILD-ONLY PACKAGES NAMED RATHER THAN COUNTED, so the edge-set choice is auditable: showreel gains `autocfg v1.5.1`; prez gains `entities`, `fastrand`, `phf_codegen`, `phf_generator`. Overlap between prez and showreel is ZERO by `comm -12`, so the union equals the arithmetic sum of 17 + 54 + 0 = 71, and the row's original claim that 69 "overstates by 10 through shared transitive deps" stays refuted -- there is still no overlap to overstate through.

THE ROW IS DISCHARGED ON ITS OWN TERMS: the stated figure and its population are quoted together and reproduce. The population is now stated in full -- edge set AND dedup key -- because naming the edge set alone left the answer under-determined by two, which is this row's own lesson arriving a second time, on the row itself. -- satisfied: yes
- AC-3.11 (non-test) max_ease is applied by the COMPILER, not only by the runtime, and a config whose ease is greater than or equal to its dwell is refused by name. THE DEFECT IS SPLIT ENFORCEMENT, NOT AN UNLUCKY PAIR OF CONSTANTS -- cc's correction, verified from source f593de8: showreel:799-800 applies min_dwell and min_ease and NEVER applies max_ease, while the cap lives only in player.html's easeOf, at the Math.min(..., LIM.max_ease) token -- cited by token because 3903937 moved it off the line this row named until 2026-09-09, whereas the two showreel citations here were swept the same day and are correct, and showreel:988 comments 'the runtime enforces the same floors' when it enforces one the compiler does not have. One limit, two homes, disagreeing, and a comment asserting a parity that does not hold -- this thread's own dominant class, in the code about to be ported. README section 7's claim that compiler and player both enforce the floors is false for max_ease. hv RULED 2026-09-09: max_ease is capped at 2400. That is ONE constant and it closes the whole crossing space STRUCTURALLY rather than by refusal -- runtime ease <= 2400 < 2500 <= runtime dwell, for every config, every speed and both pace modes. vc verified the envelope in source before putting it up: cc was right that no compile-time enforcement closes it, because at ?speed=16 a config with dwell 20000 and ease 2600 crosses while authored ease sits eight times under authored dwell, so the compiler cannot see it. What the measurement added is that BOTH defects -- the constants crossing at speed=1, and the dwellOf/easeOf asymmetry crossing at any speed -- reduce to one condition, runtime ease exceeding the 2500 dwell floor. Capping below that floor makes it unreachable rather than refused, which is the difference between a guard and a rule. Cost: no reel may crossfade slower than 2.4s; 45h's worst authored ease is 900, so nothing real moves. Raising min_dwell instead would also work and was not taken, because it changes pacing for every existing slide. -- evidence: VERIFIED BY vc AT THE SOURCE. The three constants read from limits.rs: MIN_DWELL_MS 2500, MIN_EASE_MS 600, MAX_EASE_MS 2400 -- hv's cap, against the reference's 3000 which exceeded min_dwell and was the second defect. timing() returns Timing { dwell_ms: dwell_ms.max(MIN_DWELL_MS), ease_ms: ease_ms.clamp(MIN_EASE_MS, MAX_EASE_MS) }, so THE COMPILER APPLIES max_ease WHERE THE REFERENCE NEVER DID -- the reference applied min_dwell and min_ease and left the cap to player.html's easeOf alone, which is the split enforcement this row was minted about. The ease >= dwell case returns a Failure naming the segment and BOTH durations, with a remedy explaining that ease is the crossfade inside the dwell rather than an addition to it. AND THE SUBTLE HALF IS BUILT AND PROVED: the refusal reads the AUTHORED values rather than the clamped ones, and clamping_can_never_manufacture_the_violation proves by cases that a pair which is sound before clamping cannot become unsound after it -- so the two mechanisms cannot disagree, which is the property that makes checking the authored values safe rather than merely convenient. -- satisfied: yes
- AC-3.12 (non-test) The offline scan never ends in silence: a theme the scanner cannot read to the end is REFUSED by name, not scanned in part. The instance is an unterminated /*, which a browser comments out to end of file and the old scanner truncated at, so anything after it reached the artifact unexamined. It is a SEPARATE row from AC-3.5 because it refuses a population AC-3.5 has no interest in -- a malformed theme carrying no external reference at all -- and a user can hit it with a theme that was building yesterday. Two arms, both red-proved: the refusal fires and names the line the comment opens on; AND a stray quote does not switch comment stripping off for the rest of the file, because a fix whose failure mode is file-wide would reopen 0015 one character wide. Raised by cc in its own commit rather than slipped in, and ruled by vc 2026-09-09. -- evidence: cd0412d: artifact/theme.rs, two tests, both arms and both discriminating. an_unterminated_comment_is_refused_rather_than_ending_the_scan_in_silence asserts 'never closed' AND 'line 2'; an_unclosed_string_does_not_swallow_the_rest_of_the_theme puts an unclosed string on line 1, a real comment on line 2 and the reference on line 3, and asserts 'line 3', which holds only if the line-2 comment was still stripped. Verified by vc 2026-09-09: both pass under cargo test --release -p artifact (16 passed, 0 failed). Neither is trivially true -- against the replaced implementation the scan returned early with NO error, so unwrap_err panics in both, which is the same discrimination cc's red-proof measured by running it (E8 rc=0 to rc=2, injection application proved with grep -c protocol_relative_site = 0). -- satisfied: yes
- AC-3.13 (non-test) The scan's grammar matches every surface it is applied to. refuse_external runs on THREE surfaces -- theme.css, theme.js and layout.html (artifact/theme.rs:201-206) -- and strip_comments implements CSS's comment and string grammar, which JS and HTML do not share. MEASURED 2026-09-09 by vc at the artifact, both arms, and the mismatch runs in BOTH directions where the row was minted believing it ran in one. OVER-refusal, loud: a theme.js attribution in a // comment and an HTML attribution in <!-- --> are both refused, the harm the comment exemption exists to prevent. UNDER-refusal, silent: a live href in layout.html between a stray /* and a later */ is stripped before the scan and SHIPS -- issue 0018, the class of 0014, 0015 and 0017 on the surface none of them looked at. RULED BY VC IN TWO PARTS, SIZED TO WHICH HALF IS SILENT. NOW: the comment exemption becomes CSS-only, and a non-CSS surface is scanned with no comment stripping at all. That closes the silent half outright -- nothing can be hidden behind markers the language does not have -- and costs a flag on one call. LATER, and only once a real theme.js or layout.html exists in the estate: a comment and string grammar per surface, which is cc's shape and the right one at that point. What remains after the first part is over-refusal on every comment in JS and HTML, and it fails LOUDLY against a population of zero -- nine theme.css files estate-wide, no theme.js, no layout.html -- which is what makes it deferrable where the bypass was not. NOT dischargeable by writing down that the CSS scanner is adequate, because it is not. -- evidence: FIXED AT cc 538e7fe AND VERIFIED BY vc AT THE ARTIFACT, four arms, exit codes read directly rather than through a pipe. The grammar is now chosen per call site: Grammar::Css strips comments as before, Grammar::Verbatim scans every byte, and artifact/theme.rs passes Css for theme.css, Verbatim for theme.js and Verbatim for layout.html. No per-surface JS or HTML comment parsers were built, per hv -- the population is nine theme.css, zero theme.js, zero layout.html. FOUR FIXTURES, BUILT AND RUN AGAINST THE REAL BINARY, each fixture's content proved present before its result was read: (A) a clean layout.html BUILDS, exit 0, artifact written -- the control, without which a refusal for an unrelated reason is indistinguishable from the fix; (B) issue 0018's bypass fixture verbatim, the live href between a stray /* on line 1 and a */ on line 3, REFUSES at exit 2 with no artifact, naming layout.html, the http:// scheme, the offending line and LINE 2 -- and line 2 can only be reported if the line-1 marker stripped nothing, so the message itself carries the proof; (C) a /* */ attribution in theme.js REFUSES at exit 2, which is the COST and is loud, where it built yesterday; (D) the same attribution in theme.css BUILDS at exit 0, so the CSS exemption is preserved and the narrowing did not over-correct. THE 2x2 IS THE POINT: the silent bypass closed, the loud cost accepted, the CSS half unchanged, and a clean control proving the harness path works at all. cc also pinned the defect in their suite -- the bypass under Grammar::Css is asserted to STILL BUILD -- so a later per-surface parser must come and delete that assertion deliberately rather than meet it as a failure. -- satisfied: yes
- AC-3.2 (non-test) All six admission sites route through one function: a bad input refuses with a remedy, and a dropped segment input is reported at the segment's altitude, extending report_unused rather than duplicating it.
NOT SATISFIED BY C1'S LANDING (1d5f320), AND cc REFUSED TO OFFER IT, WHICH IS THE RIGHT CALL. The classifier exists; the six SITES route through it only when `collect_segment` lands. **A row reading "all six sites route through one function" is satisfiable by machinery nothing calls**, and grading it on the function's existence would be AC-3.5's error committed a second time on the same contract. Discharged at `collect_segment`, not before.

TWO LIMITS RECORDED HERE 2026-09-09 SO THEY ARE NOT DISCOVERED AT GRADING TIME.

(1) ONE OF THE SIX SITES IS EXERCISED BY NO REAL CONFIG IN THIS ESTATE -- **CORRECTED FROM "TWO" BY vc 2026-09-09 19:35Z, AND THE ERROR WAS vc'S OWN.** The six admission sites are enumerated at source and are `qr:`, `mark:`, `image:`, `file:`, `from:` and `files:` -- six `admit::named`/`admit::scanned` call sites in `slide.rs`, over ONE private `classify()` with two public call shapes. **`bug:` and `exclude:` are NOT admission sites**: `exclude:` filters files already gathered (`slide.rs:431`) rather than admitting a path, and `bug.file` is joined unclassified in `plan.rs:143`. The original note listed eight FIELDS cc had counted in the config and called them sites, so it reported two unexercised when the answer is ONE, `files:`. **A count of fields is not a count of call sites, and the row is graded on call sites.** Measured by cc against 45h: `from:` 1, `file:` 1, `mark:` 2, `image:` 1, `bug:` 1, `qr:` 3, and **`files:` 0, `exclude:` 0**. The test population is the only thing standing behind those two, which is the same shape as segment.rs's shape-table note -- the live reel exercises all twelve shapes but not every FIELD of them. **This is a stated limit and NOT fixture work**: authoring a config that uses `files:` and `exclude:` purely so the count reads six would be inventing data to satisfy an enumeration, which is the failure this contract keeps naming rather than a cure for it. When the row is graded, "six sites route through one function" must not be read as "six sites exercised by real data".

(2) ONE RED-PROOF'S DISCRIMINATING POWER BELONGS TO THE FILESYSTEM RATHER THAN TO THE TEST, cc's own catch. The sort-removed injection fires 1 of 40 on APFS because `read_dir` returns roughly creation order and the fixture writes c, a, b deliberately out of order; on a filesystem that hands back sorted entries the same test PASSES WITH THE DEFECT LIVE -- a test asserting nothing while looking fine. cc recorded the limit at the test so a future green is not read as proof, which is the load-bearing half. **vc rules it NOT a row, on a ground cc did not state: the property has a downstream detector.** An unsorted slide list changes the structure signature, and `compare` reports a structural mismatch before any pixel number, so the estate is not relying on this unit test alone. The principled fix is PFIC rather than a new control -- extract the ordering as a pure function over a Vec, test that deterministically, and the filesystem drops out of the question -- and it is worth taking when cc is next in that file rather than as a special trip.

THE POPULATION report_unused READS IS NAMED HERE 2026-09-09 18:50Z, BECAUSE THE CLAUSE ABOVE NAMED THE FUNCTION AND NEVER NAMED ITS INPUT. cc measured the reference's own `plan()` against the live 45h config: it yields FOURTEEN used assets, splitting into TEN per-slide and FOUR reel-level, and the ten match `Slide::assets()` by name, file for file. So the slide projection is correct on its own population AND ITS POPULATION IS NOT THE BUILD'S -- the four reel-level entries are social QRs, and no arm of that match could return one, nor the bug. A `report_unused` computed from the slide walk would name four files the build embeds and instruct the operator to delete them, which is a confident wrong instruction to destroy live inputs rather than a missing warning. THE BUILD'S USED SET IS THEREFORE A PROJECTION OF THE PLAN, NOT OF THE SLIDE LIST, and that is what this clause requires. cc read the finding as already inside this clause; it is inside this ROW and was not inside any sentence of it, which is the difference between a judgement someone holds and one the contract can fail on.
AND THE LIVE REEL CANNOT TEST THE ARM, cc's catch against their own arithmetic: 45h's `bug.file` IS the logo segment's file, so the bug enters `used` through the slide walk anyway and a build that dropped the bug entirely still counts fourteen. The total agrees FOR THE WRONG REASON -- two measurements sharing a population error -- so the arm is red-proved on a fixture whose bug file is distinct from every slide asset, and a green on 45h is not evidence for it.
AC-3.8 IS NOT REFUTED BY THIS AND ITS EVIDENCE PARTLY IS. That row says the slide list and the asset list come from one walk, and they do; what vc's evidence added was that "the recycler that decides an asset is safe to delete reads exactly what the build read", and THAT sentence is false for the build until the used set projects from the plan. The row's letter survives, the over-claim in its evidence does not, and the evidence is corrected rather than the row reopened.
RULED 2026-09-09 20:25Z ON cc'S QUESTION: AN `exclude:`-FILTERED FILE IS NOT A DROPPED SEGMENT INPUT. cc asked and said NO, inviting refutation. The answer is NO and cc's reason is the correct one: an `exclude:` drop is an author's instruction OBEYED, an admission drop is an accident NOBODY WAS TOLD ABOUT, and `Scan::report`'s own comment names what reporting both would cost -- "a report that fires on every build is one nobody reads, and then the one that matters is invisible too."
vc TRIED TO BREAK IT TWO WAYS AND BOTH ATTEMPTS FAILED, WHICH IS WHY THIS IS A RULING AND NOT AN AGREEMENT. (a) TEXTUALLY, cc IS WRONG: an excluded file IS a segment input and IS dropped, so the row's words plainly cover the case. That attack succeeds against the TEXT and not against the property -- which is why the text is what changes. (b) A WRONG `exclude:` -- a pattern dropping more than its author meant -- is an instruction obeyed but not as intended, and nothing reports it. That hazard is REAL and is NOT this row's: folding it in would put an accident-detector and an instruction-log behind one switch, which is the merge the ruling refuses.
SO THE ROW'S WORDS NOW CARRY THE DISTINCTION: "a dropped segment input" means a segment input dropped by ADMISSION -- a path that did not classify -- and NOT one removed by `exclude:`, which is the author's own instruction and is deliberately silent. WITHOUT THIS SENTENCE THE NEXT READER GRADES cc'S CORRECT IMPLEMENTATION AS A MISS, because the text covered the case and cc's reasoning lived in an inbox. That is precisely the failure recorded one paragraph above, where a finding was "inside this ROW and was not inside any sentence of it".
A STATED LIMIT SO ITS SILENCE IS NOT LATER READ AS COVERAGE: nothing reports an `exclude:` that drops more than its author intended. The slide count the build prints is the only signal, and a count is not a name. Recorded, not scheduled.
AND THE REMAINING CLAUSE IS SHARPER THAN "UNBUILT" -- cc's measurement 2026-09-09 20:25Z. `admit::Scan::report` HAS a production caller, `slide.rs:425`, and its output is read ONLY inside the refusal branch (`if files.is_empty()`). On the SUCCESS path `dropped` is computed and falls out of scope unread. Its doc comment says "for the caller to print at the segment" and NO CALLER PRINTS IT: the method that exists to report at the segment's altitude is reachable only from the path where there is no segment left to report on. THIS IS THE "MACHINERY NOTHING CALLS" CLASS ONE LEVEL DOWN, AND vc'S OWN SWEEP MISSED IT -- a sweep for functions with no production caller passes `report` cleanly, because BEING CALLED and HAVING ITS RESULT READ are different properties and only the first was measured. -- evidence: SATISFIED 2026-09-09 20:42Z BY vc, EVERY CLAIM RE-VERIFIED AT SOURCE RATHER THAN TAKEN FROM cc'S REPORT. Landed at ec3b211 (source only -- cc touched no contract file).
THE SIX SITES, COUNTED AT SOURCE AND NOT RELAYED: `qr:` slide.rs:284, `mark:` :298, `image:` :321, `file:` :350, `from:` :444, `files:` :449 -- six `admit::named`/`admit::scanned` calls, over ONE private `classify()` at admit.rs:122 with exactly two public shapes, `named` :166 and `scanned` :193. The row required these to ROUTE rather than exist, which is why it was refused at C1's landing; `slide::collect` is live and is the crate's most-referenced function, so the machinery is called.
THE DROP NOW SURVIVES A SEGMENT THAT SUCCEEDED, WHICH IS THE CLAUSE THAT WAS UNBUILT. `Collected { slides, dropped }` at slide.rs:208; the gallery arm is the only one that scans and it returns EARLY carrying the list, while every other arm returns `dropped: Vec::new()` at :364 -- structural, so no arm has to remember it has nothing to say. Before this, `dropped` was computed at :442 and read ONLY inside the refusal branch.
EXTENDING report_unused RATHER THAN DUPLICATING IT, WHICH IS THE ROW'S EXACT WORDING: `plan::report` at plan.rs:204 starts `out` as `dropped.to_vec()` -- one function emitting two altitudes -- and `main.rs:155` is the production caller. THE SEGMENT LINES ARE NOT BEHIND THE `spare.is_empty()` EARLY RETURN, which cc reports as the trap their first draft walked into: behind it, a drop would be invisible on exactly the tidy reels where it is the only thing worth saying.
TWO TESTS CHECKED FOR NON-VACUITY BY vc RATHER THAN COUNTED. `the_segment_altitude_is_reported_before_the_reel_wide_sweep` (plan.rs:373) builds a reel carrying BOTH a segment drop and reel-wide exhaust, and both `.expect()` calls mean a missing line fails the test rather than passing it -- so `seg < sweep` cannot hold vacuously. `an_excluded_file_is_not_reported_because_the_author_asked_for_it` (slide.rs:663) is vc's own ruling asserted, AND IT CARRIES ITS OWN CONTROL: `slides.len() == 1` proves the exclude actually fired, so the empty `dropped` is not a pass on a reel where nothing was excluded.
cc'S RED-PROOF, FOUR INJECTIONS, EVERY REVERT IN A finally AND BOTH FILES DIGEST-VERIFIED AFTER (slide.rs 12c6fa7106a847d6, plan.rs 5c802c1ab08a5c64): drops not travelling out of the gallery RED; the restored early return RED; the drops moved after the sweep RED on ordering; `exclude:` pushing a drop line RED against vc's ruling.
THE LIVE REEL IS SILENT AND vc RAN IT INDEPENDENTLY rather than reading cc's figure: `check` against the 45h reel gives 15 segments validated, 23 slides, 14 assets read, exit 0, and NO line at either altitude. 45h drops no input and carries no exhaust, so its silence is the correct output and NOT evidence for the reporting arms -- those are carried by the tests above. The row's two recorded limits stand unchanged: `files:` is exercised by no real config in this estate, and 45h exercises `exclude:` not at all, though the tests now do. -- satisfied: yes
- AC-3.3 (non-test) QR absence remains valid and is carried as Option -- a type, not a policy -- so flattening it is a compile error. -- evidence: Verified by vc 2026-09-09 18:30Z, and the row's COMPILE-ERROR half is DRIVEN rather than argued -- which mattered, because cc's own red-proof covers the other half and not this one.

THE TYPE: `qr: Option<Qr>` at crates/showreel/src/slide.rs:106, matched as `Kind::Atwork { qr: Some(q), .. }` at :138 and constructed at :270.

DRIVEN BY vc: flattening `Option<Qr>` to `Qr` -- which is exactly what the row forbids -- makes the compiler REFUSE with TWO errors, `error[E0308]: mismatched types: expected Qr, found Option<_>` at slide.rs:138 and `expected Qr, found Option<Qr>` at :278, and `cargo check` exits 101. So "flattening it is a compile error" is a measured fact about this code rather than a property asserted of it. THE INJECTION WAS APPLIED THROUGH A REFUSING STEP per AC-1.15 -- the anchor count was required to be exactly 1 or the run aborted -- and REVERTED IMMEDIATELY, with the file's sha256 identical before and after and the working tree clean on both sides, because the tree belongs to cc.

BOTH HALVES KEPT APART, WHICH IS THE WHOLE ROW. Absent is `None` and VALID, asserted at :536. A `qr:` NAMING a file that is not there REFUSES, via `admit::named(reel, &rel, site("qr:"), admit::Requires::AnyFile)?` at :270. THE REFERENCE RETURNED `""` FOR BOTH, which is precisely what made them look like one thing and is why the row says a type rather than a policy.

cc'S RED-PROOF COVERS THE SECOND HALF: collapsing the qr refusal into `None` fires 1 of 61, with the injection recorded verbatim in cec1645's message so it can be re-applied. The two proofs are of different halves and neither substitutes for the other -- a compile error cannot be shown by a failing test, and a wrong runtime disposition cannot be shown by the compiler. -- satisfied: yes
- AC-3.4 (non-test) One normalisation policy governs both image passes. No opaque RGBA ships un-collapsed.
THIS ROW SPANS TWO WORK PACKAGES BY CONSTRUCTION AND CANNOT CLOSE INSIDE WP-03. Recorded by vc 2026-09-09 18:12Z, WHILE cc IS BUILDING C2, so it is known in advance rather than discovered at grading. design.md:1177 names the two passes and they do not live together: `normalise_image` (showreel:271) is **INIT ONLY** and writes 2560px masters, and `data_uri` (showreel:417) is **BUILD**. Init is WP-04. So a row that says "governs BOTH image passes" has half its population in a work package WP-03 does not touch, and grading it when C2 lands would be AC-3.5's error a third time -- verifying the built half thoroughly and never asking where the other one lives. THE ROW IS NOT SPLIT AND MUST NOT BE. The whole property is that ONE policy governs both; two rows, one per pass, would permit exactly the divergence the row exists to forbid, and this contract's rule is that a criterion pins properties rather than enumerations. WHAT LANDS WHEN IS THEREFORE: the POLICY itself is one module and lands inside showreel with C2 in WP-03, where it can be read and red-proved; its APPLICATION at the second site lands with init in WP-04; and the artifact-level half -- no opaque RGBA ships un-collapsed -- needs a built artifact and a real reel, so it is later still. The asymmetry being retired is concrete and is what the row is protecting against: `normalise_image` carries an alpha-collapse heuristic at showreel:283-285 demoting a fully-opaque RGBA to RGB/JPEG, `data_uri` has no equivalent, and its PNG branch at showreel:426 is the DEFAULT path for hand-placed brand marks -- so under the reference a fully-opaque RGBA PNG ships full-size where init would have made it a JPEG. DISCHARGED AT WP-04, NOT AT C2.

THE ARTIFACT-LEVEL CLAUSE IS A TAUTOLOGY ON 45h, AND THAT IS THE THIRD KIND OF ZERO RATHER THAN THE CENSUS IT LOOKS LIKE. Measured 2026-09-09 18:59Z by TWO nodes with TWO instruments, neither redundant. cc (5853f33) read the ALPHA VALUES of every raster asset: both brand PNGs genuinely transparent, so the collapse does not fire on them in either implementation, and the six art files plus the one location file carry no alpha at all. vc verified with a DIFFERENT instrument -- the DECLARED colour type at the PNG IHDR's byte 25 -- with the members printed rather than counted, because a count whose members you cannot see is a claim you cannot check. NINE raster assets: six `assets/art/*.jpg` which cannot carry alpha BY FORMAT; one `assets/location/forbiddenplanet-01.png` at colour type 2, RGB, NO ALPHA CHANNEL; and two `assets/brand/*.png` at colour type 6, RGBA, alpha channel PRESENT.
THE TWO INSTRUMENTS COMPOSE AND NEITHER COULD HAVE DONE THE OTHER'S HALF. The header settles SEVEN of nine STRUCTURALLY and without any pixel scan -- a file with no alpha channel cannot be an opaque RGBA, full stop -- and cc's scan settles the remaining two, which no header can answer, since colour type 6 says an alpha channel EXISTS and says nothing about whether it is used. This is the rule about two nodes agreeing on an instrument wasting one of them, applied deliberately for once rather than after the fact.
SO THE OPAQUE-RGBA POPULATION ON 45h IS EMPTY, FOR TWO DIFFERENT REASONS, AND THE CLAUSE "no opaque RGBA ships un-collapsed" IS TRIVIALLY TRUE ON THIS REEL. A build over 45h satisfies that sentence perfectly while exercising the mechanism ZERO times. **That is a tautology -- the branch is never reached -- and it passes a has-it-fired check, because the detector DOES fire, in cc's three synthetic tests.** Reachability is a property of the RUN, and on this run the branch is unreachable.
WHAT THIS DOES NOT DO: IT DOES NOT WEAKEN THE ROW OR THE RULING. One policy governing both passes is still the whole property; cc's ruling stands and its reasoning never leaned on the 45h instance; and the code IS covered -- three synthetic tests drive the arm, one of them carrying the insight in its own name, `one_transparent_pixel_in_a_million_stops_the_demotion`. **The code is covered while the REEL is not, and those are different facts.** What can no longer be claimed is that 45h demonstrates anything about this arm.
DISCHARGE CONDITION UNCHANGED, WITH ONE THING ADDED. Still WP-04, because `normalise_image` is init-only. AND the artifact-level half must be graded on a CONSTRUCTED reel carrying a genuinely opaque RGBA, or explicitly stated as ungradeable on 45h -- **a green from 45h is not evidence for this arm**, exactly as it is not evidence for the bug embed or for the sort ordering. Three arms now, one shape. -- satisfied: no
- AC-3.5 (non-test) No theme surface puts an external reference into the artifact: an absolute scheme anywhere in live text, or a protocol-relative target at a reference site, is a build error wherever a theme can express one. THE SURFACES ARE THE INSTANCES AND NOT THE CRITERION -- the previous wording said 'in CSS and in theme.yaml', which is an enumeration, and the code was already broader than it (see AC-3.13). Implements R2 and R3; CLOSES issues 0014, 0015 and 0017. The CSS half landed 2026-09-09 (cd0412d), reading the sites as TOKENS rather than as spellings, after cc measured a needle list deciding 8 of 13 references wrongly with every one of the 8 shipping its reference into the artifact. The theme.yaml half is R3 and is NOT blocked on the manifest existing -- themes/popupart/theme.yaml exists today, is read at showreel:198, and carries favicon: and fonts[].file, which are exactly the reference-bearing fields -- it is blocked on WP-03's YAML loader. The row therefore stays unsatisfied with half of it built, and is NOT split by surface: one property, one row.

STATUS 2026-09-09, CORRECTED BY vc AGAINST vc: R2 IS BUILT AND VERIFIED, R3 IS NOT, AND THIS ROW WAS WRONGLY MARKED SATISFIED FOR ROUGHLY AN HOUR. R2 landed at cc 538e7fe and vc verified it at the artifact in four arms with exit codes read out of band: a clean layout.html builds at exit 0; issue 0018's bypass fixture -- a live href between a stray /* on line 1 and a */ on line 3 -- REFUSES at exit 2 with no artifact, naming layout.html, the http:// scheme and LINE 2, which can only be reported if the line-1 marker stripped nothing; a /* */ attribution in theme.js refuses at exit 2, the loud cost; the same in theme.css builds at exit 0, so the CSS exemption survives. That verification stands and is kept here rather than lost with the evidence. R3 IS UNBUILT: theme.yaml is a FOURTH surface, refuse_external has THREE call sites, and nothing scans the theme manifest at all. A remote URL there resolves as tdir / "https://...", does not exist, and the loader warns and continues -- so THE OFFLINE GUARANTEE HOLDS BY ACCIDENT OF PATH RESOLUTION RATHER THAN BY THE CHECK THAT IS SUPPOSED TO HOLD IT, which is IN-AG-NO-SILENT-001. The live example exists and is not hypothetical: popupart/theme.yaml is read at showreel:198 and carries favicon: and fonts[].file, precisely the reference-bearing fields. HOW vc GOT IT WRONG, BECAUSE THE TELL IS IN THIS ROW'S OWN TEXT: the row says it implements R2 AND R3, and vc verified R2's three surfaces exhaustively and never asked what R3 was. The four arms were complete for the code's reach and answered nothing about the row's. THIS ROW ALREADY CARRIED THE SENTENCE 'THE SURFACES ARE THE INSTANCES AND NOT THE CRITERION', WRITTEN BY vc THIS MORNING, AND vc THEN GRADED IT AGAINST THE INSTANCES. design.md also states the disposition plainly -- that the right outcome for a half-built row is that it stays unsatisfied, which is what cc did rather than asking for a mark -- so the correct answer was written down in two places before the error was made. UNBLOCKED NOW: R3 waited on WP-03's own YAML loader with a live example to test against, and cc reports that loader exists, with the loader living in showreel so artifact stays at zero dependencies and AC02's sign-off is untouched. Discharged when theme.yaml joins the scanned set and a reference-bearing field in it is refused by name. -- evidence: R3 LANDED AT cc 151c5a7 AND vc VERIFIED THE STRUCTURE AT THE SOURCE, INCLUDING A CASE BUILT TO BREAK cc'S ACCOUNT, WHICH LOST. R2 was verified earlier at the artifact in four arms (clean layout builds exit 0; issue 0018's bypass fixture refuses exit 2 naming layout.html, http:// and LINE 2; a /* */ attribution refuses in theme.js and builds in theme.css) and that verification stands. VC'S OWN PRESCRIPTION WAS NEARLY RIGHT AND HAD A HOLE, FOUND BY A FAILING TEST RATHER THAN BY READING: vc told cc that R3 lands as a fourth CALL SITE and not a fourth grammar. refuse_external could not express it. A protocol-relative fonts[].file -- //cdn.example.com/x.woff2 -- BUILT, because the absolute net wants a scheme and finds none, and the protocol-relative arm is keyed on CSS's two reference SITES, url() and @import, while a theme.yaml field has no site: THE WHOLE VALUE IS THE TARGET. cc did not treat that as a scanner defect and was right not to -- refusing protocol-relative in arbitrary string content was the OTHER implementation's false positive, content: "//" being a shape people write, which AC-3.5's own history records. SO IT IS A SECOND ENTRY POINT AND NOT A SECOND RULE, AND vc CHECKED THAT CLAIM RATHER THAN TAKING IT. One const ABSOLUTE at theme.rs:512 and one protocol_relative_target at :625; refuse_external_target reads both directly; refuse_external reads ABSOLUTE and calls protocol_relative_site, WHICH ITSELF DELEGATES TO protocol_relative_target at each of url( and @import. One rule, two adapters -- so the Highlander position holds and vc's suspicion that there were two protocol-relative rules was wrong. THE SPLIT IS PINNED AS A DECISION RATHER THAN MET AS A BUG: a_bare_reference_target_needs_its_own_entry_point asserts the document form PASSES //cdn/x.woff2 (correct for a document) while the target form refuses it, then asserts the two AGREE across https://, HTTP:// uppercase, a relative path and the empty string, with the message "the split is a second rule rather than a second input shape" if they ever diverge. THE SCAN IS OVER FIELDS, NOT THE FILE, and that is issue 0017's correction one surface along -- read the SITE, not the spelling. theme.yaml is not CSS so Verbatim would apply and there is no comment form to exempt, meaning a whole-file scan would refuse a provenance URL in a # comment, which popupart's own header carries. Sites are favicon and fonts[].file, each scanned separately so the diagnostic names WHICH field and, for a font, its FAMILY -- a five-face theme should not send its author counting from zero. Red-proved by cc: removing the scan fails exactly the two tests that assert it and leaves 21 of 23 green, so the proofs are identified rather than assumed. THE LOADER LIVES IN showreel AND NOT artifact, checked rather than assumed: artifact's [dependencies] is empty, so AC02's zero-dependency sign-off keeps its whole basis. -- satisfied: yes
- AC-3.6 (non-test) The safety floors hold in compiler and runtime, and ?speed= cannot cross them. hv RULED 2026-09-09: max_ease is capped at 2400. That is ONE constant and it closes the whole crossing space STRUCTURALLY rather than by refusal -- runtime ease <= 2400 < 2500 <= runtime dwell, for every config, every speed and both pace modes. vc verified the envelope in source before putting it up: cc was right that no compile-time enforcement closes it, because at ?speed=16 a config with dwell 20000 and ease 2600 crosses while authored ease sits eight times under authored dwell, so the compiler cannot see it. What the measurement added is that BOTH defects -- the constants crossing at speed=1, and the dwellOf/easeOf asymmetry crossing at any speed -- reduce to one condition, runtime ease exceeding the 2500 dwell floor. Capping below that floor makes it unreachable rather than refused, which is the difference between a guard and a rule. Cost: no reel may crossfade slower than 2.4s; 45h's worst authored ease is 900, so nothing real moves. Raising min_dwell instead would also work and was not taken, because it changes pacing for every existing slide.

THE RUNTIME LEG'S MECHANISM, SPECIFIED 2026-09-09 18:55Z BEFORE cc PULLS THE TEMPLATE, BECAUSE AFTER IT LANDS THE SAME SENTENCE IS A REVIEW COMMENT. Measured at the reference: `player.html:561` is `const LIM = REEL.limits`, so the cap is READ OFF THE PAYLOAD and is not a JS literal -- the player's own comment gives the reason, "a safety limit that exists twice, in two languages, is one limit and one liability." THE LEG THEREFORE REDUCES TO TWO SOURCE CHECKS AND NEEDS NO PIXEL: the emitted payload's `limits.max_ease` DERIVES from `limits::MAX_EASE_MS`, and the pulled template still reads `LIM = REEL.limits`. Red-control that can fire: flip `MAX_EASE_MS` to 3000, rebuild, and the payload must follow; if it still reads 2400 there is a second home, which is the one-limit-two-homes defect this module was written to fix, re-created by the commit that ports the fix.
THE NEGATIVE HALF IS CITED AS TWO STRUCTURAL FACTS RATHER THAN AS A HARNESS ROW, AND THAT IS snorkeltoast'S CORRECTION OF vc, VERIFIED AT SOURCE BEFORE BEING TAKEN. (i) `signature()` at `showreel-harness:232` walks `payload.get("slides", [])` only, so NO top-level block -- `limits` included -- is structural to that instrument. (ii) the capture URL at `showreel-harness:1098` is `?start={index}&noloop&kiosk&nobug`, carrying no `pace=`, with a boxed comment above it recording that `&pace=ambient` was there and was the single worst defect in that harness. Both stay true whatever `max_ease` says on either side.
WHY NOT THE ROW, AND vc WOULD HAVE BEEN THE PROXIMATE CAUSE OF ITS OWN DANGLING CITATION. vc first cited snorkeltoast's `shipped-max-ease` expectation, kind BLIND. `_shipped_max_ease` returns `[]` the moment `compiler.LIMITS["max_ease"] == PORT_MAX_EASE_MS` -- its docstring says it "retires itself the day the Python side is capped" -- and capping Python is hv item 2, WHICH vc IS CARRYING TO hv. So the citation was contingent on a defect vc is asking hv to fix. **Cite the durable fact, not the contingent conclusion resting on it**; that is "cite the token, not the line" one level up, where what expires is not a line number but the finding.
NO AMBIENT CAPTURE ARM, AND THE EXPERIMENT IS ALREADY ON DISK. `showreel-harness:1088-1092` records it: slide 3, budgets 1100 vs 4520 -- with `pace=ambient` RMSE 142.827361, without it 0.000000, and with `pace=ambient` at 1400 (past the SCALED ease) 0.000000. The 142.8 was a capture-WINDOW artefact, budgets computed `ease + 200` from the payload's 900 while the runtime ran 1260, and NOT a `max_ease` divergence. So an arm on 45h has two possible outcomes and both are measured. The independent leg agrees: 900 x 1.4 = 1260, which is 1140ms short of the cap.
AND THE TWO CROSSING NUMBERS ARE TWO PREDICATES, RECORDED AS A PAIR SO THEY STOP LOOKING LIKE ONE NUMBER MEASURED TWICE. Divergence from the cap needs authored ease x 1.4 > 2400, so above 1714.29 -- 1715ms, vc's. Ease reaching the dwell floor needs 2500 / 1.4, so above 1785.71 -- 1786ms, snorkeltoast's, and it is `_ease_meets_dwell`'s predicate rather than this one. Both correct, neither superseding the other. A two-number discrepancy on one subject is exactly what gets filed as explained and never looked up. -- satisfied: no
- AC-3.7 (non-test) player.html carries no brand token, and the check covers STRING LITERALS in JS as well as CSS custom properties. Red-proved against the known live instance, cited BY TOKEN rather than by line: add("Producer", REEL.producer || "Snorkeltoast"), a hardcoded brand name in the shell README section 28 says knows about no brand. This row carried a line number until 2026-09-09 and 3903937 moved it, so the line belonged to the commit and the token belongs to the row. The dead number is deliberately not restated: to any scanner it is indistinguishable from a live one. HOIST.md section 4's proposed guard, grep for '--pop-', returns ZERO hits against it -- verified 2026-09-09 -- because it was written from the one instance that had been found, a CSS custom property, and so catches that instance rather than the class. The check applies to player.html and showreel and NOT to themes/, whose brand strings are legitimate. THIS ROW IS NOT HYGIENE AND MUST NOT BE SCHEDULED AS HYGIENE -- cc measured 2026-09-09 and vc verified: add is (k, v) => { if (v) rows.push(...) }, so it already drops a falsy value and removing the fallback renders an IDENTICAL row for any config that sets producer, which 45h does. One line, parity-neutral on the artifact, and it takes the reel from 19 gradeable to 20 by removing the only permanent hole in the instrument WP-03 is graded by -- FLOORS.md has slide 1 as the sole presence-UNPROVABLE of 22 and names this literal as the reason. It is also the sole outlier in its own block: every neighbouring add falls back to CONFIG, not to a literal. Take it early. -- WITHDRAWN: hv 2026-09-09: brand-free testing is dropped as yak-shaving. 'Why are we testing for this? It's just yak-shaving' -- and on being asked directly, 'I have no idea what that is other than I told snorkeltoast to stop yak-shaving and move on.' Nothing gates on the producer literal and no row carries it. DROPPING THE FALLBACK REMAINS FINE AND IS NOT REQUIRED: cc may remove '|| "Snorkeltoast"' when WP-03 pulls the template, ungated, and if they do slide 1 becomes gradeable and the reel reads 22 of 22 instead of 21. THE ROW WAS SOUND AND THE SCOPE CALL IS STILL RIGHT. Its red-proof held -- HOIST section 4's proposed grep for '--pop-' returns zero hits against a live brand literal, so the guard as designed catches the instance it was written from rather than the class. That is a true finding about a check nobody needs. vc applied 'is it real' and never applied 'is it worth building', which is the same error snorkeltoast named in their own afternoon and it cost more here.
- AC-3.8 (non-test) The slide list and the asset list come from one walk. -- evidence: Verified by vc 2026-09-09 18:30Z at source. THE PROPERTY IS STRUCTURAL RATHER THAN DISCIPLINARY, which is the strongest form this row could take.

ONE WALK, AND THE ASSET LIST IS A PROJECTION OF THE SLIDE LIST. crates/showreel/src/main.rs:123 reads `slides.iter().flat_map(showreel::slide::Slide::assets).collect()`. There is no second traversal of the config: `Slide::assets()` at slide.rs:132 matches on the slide's OWN kind and returns the paths that slide reads -- an image's path, a strapline's mark, a venue's image, an atwork's qr, and an empty vector for the shapes that read no file. SO THE TWO CANNOT DISAGREE BECAUSE THERE IS ONLY ONE OF THEM, and the recycler and the build read one list.

THE REFERENCE'S `dry` FLAG HAS NO COUNTERPART HERE AND THAT IS THE REASON, which is cc's observation and is the part that makes this a design property rather than a coincidence: `dry` existed because the Python's `collect_segment` both PLANNED and EMBEDDED, so a caller wanting the plan without the bytes needed a flag. Separating the plan from the embedding removes the flag and removes the second walk with it.

cc'S RED-PROOF: dropping a strapline's mark from the declared assets fires 1 of 61, with the injection recorded verbatim in cec1645's message so it can be re-applied. That is the discriminating direction -- it proves `assets()` actually reports what a slide reads, rather than proving only that the two lists have the same length.

EVIDENCE CORRECTED BY vc 2026-09-09 18:50Z, ON cc'S MEASUREMENT, AND THE CORRECTION IS TO THE EVIDENCE RATHER THAN TO THE ROW. The sentence above originally read "the recycler that decides an asset is safe to delete reads exactly what the build read", and THAT IS FALSE FOR THE BUILD. cc ran the reference's own `plan()` against the live 45h config: FOURTEEN used assets, TEN per-slide and FOUR reel-level, and the ten match `Slide::assets()` by name. So the slide projection is complete on its own population and is a STRICT SUBSET of the build's -- the four reel-level entries are social QRs, which no arm of that match could return, nor the bug.

THE ROW'S PROPERTY IS UNTOUCHED AND THE OVER-CLAIM WAS MINE. "The slide list and the asset list come from one walk" is true and remains structurally true: `main.rs:123` is a single traversal and there is no second one to disagree with it. What the evidence added was a claim about a DIFFERENT list -- the build's used set -- which is not what the row pins and which the code does not yet compute. A satisfied row whose evidence carries a false sentence is worse than an open row, so the sentence goes and the row stays.

THIS IS THE SUBSET-DUPLICATE SHAPE ONE LEVEL UP, AND IT IS WHY IT SURVIVED: a sweep asking whether the two lists agree finds that they do, because the entries the build adds are not there to disagree. AC-3.2 now names `report_unused`'s population so the correct version is a thing the contract can fail on rather than a judgement in an inbox. RE-VERIFY THIS ROW AT THE DELIVERY SLICE: once the used set projects from the plan, "one walk" should hold BETTER rather than merely still -- the plan becomes the single source and both the embedding and the used set project from it. -- satisfied: yes
- AC-3.9 (non-test) showreel's manifest carries EXACTLY the budget hv approved 2026-09-09 and nothing else: image with features jpeg,png,webp,gif,tiff (matching showreel:55's RASTER_EXT, +35 where jpeg+png alone is +20); serde with derive (+7); serde_yaml (+14, published as 0.9.34+deprecated, accepted knowingly); kamadak-exif (+2); qrcode with svg only, NOT default (+1 against +8); walkdir (+5); regex (+5). fast_image_resize is REFUSED -- image carries FilterType::Lanczos3 and nothing has measured a need. Any addition needs hv's sign-off named in the commit, as AC02's precedent requires.
NOTHING GATES THIS ROW AND ITS WORDS ARE A CONTINUOUS RULE, NOT A MEASUREMENT. Found by vc 2026-09-09 20:36Z while closing a stated coverage gap. THE ROW IS FACTUALLY TRUE RIGHT NOW AND vc RE-READ THE MANIFEST TO SAY SO: `crates/showreel/Cargo.toml` carries exactly the eight approved entries, `serde_json` appears in no manifest and in no lock. THE PROBLEM IS WHAT HOLDS IT. Measured: NO test reads `Cargo.toml` or asserts the dependency set; the only `manifest.sha256` in the tree is `bin/.devbin/manifest.sha256`, devbin's, unrelated to the crate; no bats or shell gate names any of the approved crates. **SO THE MOMENT A CRATE IS ADDED -- WITH OR WITHOUT hv'S SIGN-OFF -- THIS ROW'S CLAIM BECOMES FALSE AND NOTHING GOES RED.** It stays `satisfied` while saying something untrue.
THE ROW IS NOT REOPENED, AND THE REASON MATTERS. Its satisfaction was an honest reading of the manifest and the manifest still reads that way; reopening would punish an accurate measurement for the absence of a gate the row never promised. **What is wrong is that its words -- "carries EXACTLY the budget hv approved and nothing else", "Any addition needs hv's sign-off named in the commit" -- are a RULE, and a rule with no enforcement is held by discipline.** That distinction is the same one this contract draws between a snapshot and a property, and FLOORS.md's own header draws it about itself.
THE DISCIPLINE IS WORKING AND THAT IS EVIDENCE, NOT A GATE. cc refused to add `serde_json` twice on 2026-09-09, the second time against hv's general "rock on as needed", on the ground that a general go-ahead names nobody. **A rule obeyed by the one party who could break it is not the same as a rule that cannot be broken**, and the difference is invisible until the careful party is replaced.
LIVE CONSEQUENCE FOR hv'S PENDING DECISION, WHICH IS WHY THIS IS RECORDED NOW: whichever way hv rules on `serde_json`, **this row must be amended in the same commit or it becomes false silently.** A yes changes the approved set; a no leaves it and should say so. The natural home for an actual gate is issue 0016, already scheduled by hv as a WP-05 rider. -- evidence: VERIFIED BY vc AT THE MANIFEST, read rather than relayed. crates/showreel/Cargo.toml's [dependencies] carries exactly eight entries and they are hv's approved set: artifact by path (first-party, the one addition AC02 is signed off for), image 0.25 with default-features = false and features jpeg, png, webp, gif, tiff, kamadak-exif 0.6, qrcode 0.14 with default-features = false and svg only, regex 1, serde 1 with derive, serde_yaml 0.9, walkdir 2. Seven third-party plus one first-party, and no others. fast_image_resize IS REFUSED AND STAYS REFUSED: it appears once in the manifest as prose recording the refusal and ZERO times in Cargo.lock, so it is named but not linked -- image carries FilterType::Lanczos3 and nothing has measured a need. This row is INDEPENDENT OF AC-3.10'S ARITHMETIC and that separation is the point: the budget is named correctly here even though the count of what the budget pulls was recorded under an unnamed population there. A dependency set can be exactly the approved one while every figure about its transitive cost is wrong, and conflating the two is what let the wrong figures sit unchallenged. -- satisfied: yes

### WP-04 -- Rust init and qr paths, graded against population 3 (status: Not Started)

- AC-4.1 (non-test) init's normalisation is graded against population 3 (Rust init + Rust build), labelled as such, never against population 2.
THIS ROW ASSUMES A CAPABILITY THAT DOES NOT EXIST AND THAT AC-2.1'S DISCHARGE WILL NOT CREATE. Found by vc 2026-09-09 18:20Z, before either row was graded and before cc's first build. THE TRAP IS ALREADY ARMED: design.md 4.1 calls population (2) "the one that happens by accident -- the masters are on disk and nobody re-runs `init`", and the 45h reel's assets ARE those masters, measured at 2560 pixels wide, which is exactly what `normalise_image` writes. So cc's first Rust build over 45h is population (2) BY CONSTRUCTION, not (3). WHAT THE HARNESS CAN AND CANNOT DERIVE, READ AT SOURCE: `population_source` is `"stamp" if stamp else "adjacency (UNVERIFIED)"` where `stamp = producer_stamp(artifact)`, so it answers WHICH COMPILER BUILT THE ARTIFACT. `label` is `args.label or "unlabelled"` -- CALLER-SUPPLIED, which is precisely the "flag someone passed" design.md 4.1 forbids deriving a population from. No other manifest key touches init: label, artifact, artifact_sha256, population_source, producer, compiler_adjacent, compiler_adjacent_sha256, chrome, viewport, host, slide_count, captured, budgets, retries, signature, captured_at. SO THE DERIVATION COVERS THE BUILD HALF AND NOTHING COVERS THE INIT HALF -- and the init half is the ENTIRE difference between (2) and (3). The mechanism built to stop "a run of (2) reported as (3)" does not reach the distinction it exists for. AC-2.1 DOES NOT CLOSE THIS AND IT IS WORTH SAYING WHY: its three legs are artifact-level population, producer-level population, and the refusal, and all three are about the artifact and its builder. Even once WP-03 stamps the build and leg 2 becomes a derivation, the harness still will not know whether the masters came from Python init or Rust init. THE FIX SHAPE, NOT PRESCRIBED: Rust `init` stamps its masters the way the Rust build stamps its artifact, at which point (3) is provable and the ABSENCE of that stamp derives (2) -- exactly as the absence of a producer stamp already derives "adjacency (UNVERIFIED)". That keeps it a derivation rather than a flag. THIS IS THE SAME SHAPE AS AC-2.1'S LEG 2 ONE WORK PACKAGE LATER: that row records a real WP-02-on-WP-03 dependency so it is not rediscovered as a surprise, and this is a WP-02-on-WP-04 dependency recorded for the same reason. Until Rust init exists, EVERY run is population (2) and this row cannot be satisfied by any of them.
vc'S DIMENSION EVIDENCE WAS SAMPLED RATHER THAN MEASURED, CORRECTED BY snorkeltoast 2026-09-09, AND THE CORRECTION STRENGTHENS THE CONCLUSION. vc ran `find | head -4`, saw four assets at 2560, and wrote "the reel's assets ARE those masters" -- FOUR MEMBERS CHARACTERISING A SET OF NINE, which is the rule cc's own board leads with. Re-measured over the whole population with the partition printed: NINE raster assets, SIX at the 2560 cap and THREE below it (1024, 1433, 1920), and the partition sums. AND THE INFERENCE RUNS ONLY ONE WAY, which is snorkeltoast's point: `normalise_image` resizes only `if max(w, h) > max_edge`, so a file AT the cap was resized to reach it and IS evidence, while a file BELOW the cap was either normalised and already small or never touched, and those two are indistinguishable. So 6-and-3 is not a ratio and must not be read as one. A REFINEMENT THE DIRECTORY GIVES AND THE DIMENSION DOES NOT: all six at the cap are in `art/`, and all three below are in `brand/` and `location/` -- buckets init NEVER CREATES, since design.md 4.1 records that init writes exactly `titles`, `art`, `photos`, `cards`. So in this reel the three are not ambiguous evidence, they are a different INPUT CLASS, and the six that are init's own bucket are six of six at the cap. THE SHARPER POINT, AND IT IS WHY THE STAMP IS THE ONLY MECHANISM RATHER THAN THE NEATEST ONE: a Rust init will cap at the SAME 2560, so the dimension is identical under either init and cannot separate (2) from (3) EVEN IN PRINCIPLE. Disk can establish THAT normalisation happened and can never establish WHO did it. That closes off reading the masters more cleverly, which was the alternative snorkeltoast says they would otherwise have proposed, and leaves the stamp standing alone. WHAT LANDED WITHOUT WAITING ON cc (snorkeltoast 7f8454c): compare's POPULATION block used to print a source line covering the build half while implying nothing about init, which READS AS COMPLETENESS. It now derives what disk supports and refuses the rest BY NAME -- reporting the master counts, stating that an init has run, and then saying that WHICH init is not established and cannot be from disk, naming this as design.md 4.1's population (2). So the first compare after cc's build will say in the run that it does not know, rather than being labelled in a way nobody can defend.
THE BUCKET SPLIT LANDED AT b5e4365 AND snorkeltoast FOUND A FAULT IN THEIR OWN LINE WHILE BUILDING IT. `INIT_BUCKETS = {"titles","art","photos","cards"}` is ONE definition with a second READER rather than a second list -- the master derivation and `partition()` both read it -- and the split happens before anything is counted, because a dimension is only evidence about a file init would have written. THE FAULT WAS A CAVEAT PRINTED UNCONDITIONALLY: "a file below the cap is not evidence either way" fired on every run, and on this reel there are ZERO files below the cap inside init's buckets, so it was a warning about a population that does not exist here. snorkeltoast's own words for why that matters: **a caveat nobody reads is worse than no caveat, because it still looks like diligence.** Now guarded on `if norm["below"]`, and verified by vc at source: on 45h it correctly does not print, while the line that IS true of every reel -- which init is not established and cannot be from disk -- correctly still does. RED-PROVED ON CONSTRUCTED REELS BECAUSE 45h CAN NO LONGER REACH EITHER BRANCH, which snorkeltoast names as the same problem as grading a detector on the one config everybody runs: a mixed reel gives at_cap 1 / below 1 / outside 1, and a reel whose only master is under the cap gives at_cap 0, where the harness says there is NO evidence init ran rather than claiming it did -- the branch most worth seeing fire, since a wrong answer there would be an invented population rather than a missing caveat.
THE FIX SHAPE ABOVE CITES A REFUSAL-TO-DERIVE AS THE MODEL FOR A DERIVATION, AND THE ANALOGY POINTS THE OTHER WAY FROM THE CLAIM IT SUPPORTS. Caught by snorkeltoast 2026-09-09 20:31Z against vc's quotation of this row, not against the row -- they had not read it and asked vc to spend ten seconds on it. The sentence says "the ABSENCE of that stamp derives (2) -- exactly as the absence of a producer stamp already derives adjacency (UNVERIFIED)". **"adjacency (UNVERIFIED)" IS A REFUSAL TO DERIVE, NOT A DERIVATION.** So the model does not support the claim; it contradicts it.
CORRECTED: A RUST INIT STAMP MAKES (3) PROVABLE, AND ABSENCE MUST REFUSE TO DERIVE RATHER THAN ESTABLISH (2). "No stamp implies Python init" is sound only while a Python init is the sole unstamped producer, and it STOPS BEING SOUND WITHOUT ANY SIGNAL: a Rust init predating the stamp, a stamp dropped by a later edit, a hand-placed master, a truncated file. Every one of those maps to (2), confidently, and NO RUN CAN EVER GO RED OVER IT. Absence is the cheapest identifier there is, and reading it as a positive answer is the same substitution this row already refuses when it rejects `args.label`.
AND vc SPENT THE BAD VERSION BEFORE IT WAS CAUGHT, WHICH IS THE PART WORTH RECORDING. At 20:18Z vc quoted this sentence to cc as the justification for recommending a Rust-only stamp -- "absence then derives Python, which AC-4.1 already establishes as the shape". THE MECHANISM RECOMMENDATION SURVIVES AND ITS JUSTIFICATION DID NOT: cc stamps, Python does not, and a missing stamp goes on meaning UNVERIFIED. No code changes; the reasoning does. A row's own analogy is load-bearing the moment somebody cites the row.
SEQUENCING FOR THE PYTHON SIDE, snorkeltoast's, recorded so it is not re-litigated: Python SHOULD stamp eventually and it is pixel-neutral -- a meta in head renders nothing so RMSE is unaffected, and `signature()` walks `slides` only so structure is unaffected; `artifact_sha256` changes, which is correct, because the file did change. But it waits until after hv's event window, because touching player.html is touching the runtime shell days out from a live reel and the benefit does not expire. Until then absence keeps saying UNVERIFIED, which is TRUE. -- satisfied: no
- AC-4.2 (non-test) A QR generated for an address the config has since moved on from is reported at build.

AUDITED 2026-09-09 19:03Z BY vc, BEFORE WP-04 EXISTS, BECAUSE THIS WAS THE ONE BARE ROW ON THE CONTRACT. Every other open row carries paragraphs; this one was a single sentence, and a single sentence is what a later reader grades against whatever got built. The mechanism is measured at the reference rather than guessed: `QR_STAMP = "showreel-qr:"` has ONE definition (`showreel:707`), ONE write (`cmd_qr`, `:742`) and ONE read (`build_socials`, `:786`). Census control: `qr_path` returns six sites and `venue_qr_path` three, so the instrument does find multi-site tokens and the three is a real three.

FIVE FINDINGS, ALL MEASURED, EACH CHANGING WHAT WP-04 SHOULD BUILD.

(1) THE DETECTOR CANNOT FIRE ON AN UNSTAMPED QR, AND EMBEDS IT ANYWAY. `if m and m.group(1) != row["url"]` short-circuits when `re.match` returns None, and `row["qr"] = svg` then runs regardless. A QR made by hand, copied from another reel, or produced by any tool other than `showreel qr` carries no stamp -- and that is precisely the population most likely to be stale. **The detector is blind to its own worst case and silently ships it.**

(2) `cmd_qr` KEEPS AN EXISTING FILE WITHOUT `--force` (`:735`, "kept ... (--force to regenerate)"). So the two behaviours compose: `qr` preserves the stale file, `build` warns about it, and the warning names `--force` as the remedy. Coherent, and it means **stale is the STEADY STATE rather than an accident.**

(3) THE SINGLE READ COVERS `socials:` ALONE, AND NOT THE VERB PEOPLE RUN MOST. Segment-level `qr:` loads through `load_qr` (`:746`), which never reads the stamp; the venue QR is written by `cmd_qr` and never read back. And `build_socials` is called only `if not dry` (`:577`), so `check` never warns at all. **One of three QR populations, in one of two verbs.**

(4) ON 45h THE DETECTOR CANNOT FIRE, MEASURED WITH THE MEMBERS PRINTED. Five QR files, all five stamped, and every stamp equals its config value exactly: `instagram.svg`, `tiktok.svg`, `linkedin.svg` and the Showreel social against `socials[].url`, and `venue.svg` against `session.venue_url`. So this row joins AC-3.4's artifact half and the bug embed: **its evidence on the only real reel is a census over an empty population, and a green from 45h is not evidence for it.** cc's observation, verified independently here rather than relayed.

(5) THE DETECTOR COMPARES QR AGAINST CONFIG AND NEVER CONFIG AGAINST THE WORLD, WHICH IS A STATED LIMIT RATHER THAN A DEFECT. The row's own wording -- "an address the config has since moved on from" -- is faithful to that. But the HARM the reference cites for it is "a wrong QR is invisible until somebody scans it in a shop", and a QR that agrees with a config which is itself stale is equally wrong in the shop and equally invisible. **Naming the boundary is the point: this row is not, and should not silently become, a claim that the encoded address is CORRECT.**

THE ROW IS CORRECTED TO ITS INTENT AND NOT WEAKENED, AND THE TEST IS THAT THE INTENT HELD BEFORE THE EDIT: "reported at build" plainly meant every QR the build embeds, not one of three populations in one of two verbs. WHAT DISCHARGE REQUIRES: every QR the build embeds is checked against the config's current value; an UNSTAMPED QR is reported as UNCHECKABLE rather than passing silently; the population -- socials, segment `qr:`, venue -- is named in whatever the port builds; and the arm is red-proved on a CONSTRUCTED reel, because 45h cannot reach it.

AND THIS ROW SPANS WORK PACKAGES THE WAY AC-3.4 DOES, RECORDED NOW RATHER THAN DISCOVERED AT GRADING. The group digit puts it in WP-04, and the STAMP is written by the `qr` verb which is init-adjacent -- but the READ is at BUILD, which is WP-03's half of the pipeline. Not split, for AC-3.4's reason: one policy over write-and-read is the whole property, and two rows would permit exactly the divergence the row exists to forbid. -- satisfied: no

### WP-05 -- Command surface: shim dispatch, manifest, help, doctor, prez help amendment (status: Not Started)

- AC-5.1 (non-test) prez showreel build <dir> builds the 45h reel from its real config, from BOTH invocation forms. THE THEME PATH IS PART OF THE FIXTURE AND NOT PART OF THE DEFECT, RECORDED BEFORE THE ACCEPTANCE TEST IS WRITTEN RATHER THAN AFTER IT FIRES. Raised by cc 2026-09-09 and taken. 45h's config names `theme: popupart`, and popupart can NEVER be a built-in here because no built-in is ever a brand (H3, and AC-6.1 measures it), so a bare invocation with SHOWREEL_THEME_PATH unset REFUSES where the Python built. Driven by vc 2026-09-09 against the live reel, both invocation forms: unset gives exit 2 with "no theme 'popupart'", stating that the variable is unset and NO directories were searched, listing the one built-in and naming three remedies; set gives exit 0. THAT REFUSAL IS H3 WORKING AND MUST NOT BE GRADED AS A PORT REGRESSION. It is the same class as AC-2.16's predicted differences, and it is written down here for the same reason: a difference recorded before it fires is a prediction, and the identical sentence written afterwards is a rationalisation. Whatever shape this row's acceptance test takes, SHOWREEL_THEME_PATH belongs in the fixture setup. -- satisfied: no
- AC-5.2 (non-test) help/prez.md's 'there is no player, and there never will be' is amended in the SAME commit that adds the subcommand. -- evidence: VERIFIED BY vc AT THE COMMIT, which is what this row asks -- the amendment and the subcommand in ONE commit, not two. cc b8dc9f1 carries help/prez.md (+26), opt/prez/prez (+46) and opt/prez/test/prez.bats (+54) together, so the anti-requirement could not have gone stale between them even briefly. THE AMENDMENT KEPT THE TEETH AND LOST ONE WORD, which is the right edit: "there is no server, no player, no watch-me-render mode, and there never will be" became "there is no server, no watch-me-render mode, and there never will be". "No player" was false the moment a showreel artifact carries one INSIDE the single file prez writes; "no server, no watch-me-render mode" is what the sentence ever meant. cc added the reconciliation in the file rather than leaving it inferable: prez still writes a file and stops, and what is new is that one of the files it writes happens to animate once opened -- a player that ships inside the artifact being the OPPOSITE of a server, since it is what lets the output need nothing at all. VERIFIED SEPARATELY THAT THE TWO USER-FACING INVOCATION FORMS AGREE, which is ST0016's property and the thing this amendment could have broken: bin/prez --help and bin/utilz prez --help are BYTE-IDENTICAL by md5 over 427 lines. The 36-line terse usage from opt/prez/prez --help is the raw utility script invoked by path, which is not a user path and is not a divergence -- checked because it looked like one. -- satisfied: yes
- AC-5.3 (non-test) utilz doctor reports showreel's external dependencies as optional lines, present because the tool is present. THE POPULATION IS NAMED, BECAUSE AN UNNAMED ONE HERE CAN BE SATISFIED BY REPORTING NOTHING. Measured by vc 2026-09-09 17:59Z. The reference shells out TWICE -- `pdftoppm` at showreel:288 and `qrencode` at showreel:680 -- and THE PORT'S POPULATION IS ONE, NOT TWO, because `qrencode` was absorbed into the approved crate budget as `qrcode 0.14` with default-features off and the svg feature on. So the external dependency this row is about is `pdftoppm`, and design.md rules why it STAYS external: every Rust PDF renderer is bindings to PDFium or MuPDF, which breaks the self-contained claim motivating the port, and MuPDF drags AGPL in. It runs only in `init` and never in `build`, so the hot path is pure. WITHOUT THE NAME THIS ROW IS A CENSUS OVER A POPULATION NOBODY STATED: showreel is a self-contained Rust binary, so "doctor reports its external dependencies" is satisfied identically by doctor reporting `pdftoppm` and by doctor knowing nothing about showreel at all, and the two outputs are indistinguishable. Note also that prez's own manifest declares `dependencies: []` for exactly the reasons in its comment -- cargo is a BUILD-time need deliberately undeclared, and a browser is needed only by verbs that already refuse well -- so showreel's manifest must not copy prez's empty list by symmetry. Discharged when `utilz doctor` reports `pdftoppm` as an OPTIONAL line, present because showreel is present and absent when it is not, and the line is shown to appear and disappear rather than read from source. -- satisfied: no

### WP-06 -- Snorkeltoast side: point the prototype at the hoisted tool, move the house theme (status: Not Started)

- AC-6.1 (non-test) No Utilz-tree file contains the popupart theme; it arrives over SHOWREEL_THEME_PATH and a build resolving it warns that it came from off the built-ins.
AUDITED 2026-09-09 17:38Z BY vc. TWO LEGS VERIFIED AND DRIVEN, ONE HALF-BUILT, SO THE ROW STAYS UNSATISFIED. LEG 1 -- THE THEME'S SUBSTANCE IS ABSENT AND THE WORDING IS CORRECTED TO SAY WHAT WAS MEASURED. Measured across the whole tracked tree: ZERO .woff2 files, ZERO favicon files, and no popupart theme.css -- the only shipped theme is crates/showreel/themes/default/theme.css. So popupart cannot be rendered from this tree, which is the property. THE ABSOLUTE WORDING WAS FALSE, THOUGH, AND A FALSE ROW IS WORSE THAN AN OPEN ONE: crates/showreel/fixtures/popupart.theme.yaml is a Utilz-tree file that contains popupart's theme MANIFEST, and cc's phrase "a manifest, not a theme" understates it -- the manifest carries the brand description "POP^UP^ART by Snorkeltoast. Comic four-up palette, keyline black.", the token source path, and five font FAMILY names. That is the theme's identity without its bytes. The exemption is stated and MEASURED rather than argued: the fixture is #[cfg(test)] and does not reach the release binary. Driven by vc independently of cc and over a wider token set -- popupart, POP^UP^ART, Snorkeltoast, "Luckiest Guy", "keyline black" and "Ash Sinclair" all return ZERO from `strings` on target/release/showreel, WITH A CONTROL PROVING THE INSTRUMENT WORKS ON THAT BINARY: showreel 11, theme.yaml 4, woff2 2. cc measured the first three tokens; the last three are vc's, and the brand DESCRIPTION and a font family were the ones worth adding because they are what a manifest carries and a name does not. LEG 2 -- DRIVEN, BOTH DIRECTIONS, BOTH INVOCATION FORMS. Without SHOWREEL_THEME_PATH both `prez showreel check` and `utilz prez showreel check` exit 2 with "no theme 'popupart'", stating that the variable is unset and NO directories were searched, listing the one built-in, and naming three remedies. With it, both exit 0 and resolve to the path. LEG 3 IS HALF-BUILT AND THAT IS WHY THE ROW IS NOT SATISFIED. The row says a BUILD resolving it warns, and there is no build verb yet. The WARNING ITSELF EXISTS, is driven, and is better than the row asked: "theme 'popupart' came from <dir> (on SHOWREEL_THEME_PATH), not from the built-ins. Elsewhere this reel refuses to build until that directory is on the path." The second sentence names the consequence on a DIFFERENT machine, which is exactly the H1 trap cc flagged separately, so the warning pre-empts the confusion rather than merely reporting provenance. It fires at theme RESOLUTION, which build will share -- but sharing a mechanism is not the same as the verb existing, and this contract has already paid once for grading a two-leg row on the leg that was built. DISCHARGED WHEN `prez showreel build` exists and the same warning is driven through it. -- satisfied: no
- AC-6.2 (non-test) The prototype is committed in the Snorkeltoast repo BEFORE WP-06 pulls it, so the hoist cites a real source commit on both sides. It was untracked there -- showreel, player.html, themes/, README.md and HOIST.md all outside git -- so a pull without this creates a provenance gap rather than inheriting one. -- evidence: Snorkeltoast repo commit f593de8, verified by vc 2026-09-09: carries showreel, player.html, themes/default, themes/popupart (fonts + favicon), README.md, HOIST.md. player.html confirmed UNMODIFIED -- the reduced-motion block at :388 holds only .slide img and .wipe, .crawl appears zero times in it, so the defect is present and deliberately unfixed. The hoist now cites a real source commit at both ends. -- satisfied: yes

### Group AT02

_(no criteria in this group)_

## Acceptance Tests

### WP-01 -- Workspace + artifact crate: the C3 consolidation, zero behaviour change to prez (status: Done)

_(no tests in this group)_

### WP-02 -- Fidelity harness, red-proved against population 1 (Python both sides) (status: WIP)

_(no tests in this group)_

### WP-03 -- Rust build path: YAML, admission (C1), normalisation policy (C2), data-URI (status: WIP)

_(no tests in this group)_

### WP-04 -- Rust init and qr paths, graded against population 3 (status: Not Started)

_(no tests in this group)_

### WP-05 -- Command surface: shim dispatch, manifest, help, doctor, prez help amendment (status: Not Started)

_(no tests in this group)_

### WP-06 -- Snorkeltoast side: point the prototype at the hoisted tool, move the house theme (status: Not Started)

_(no tests in this group)_

### Group AT02

- AT02 `opt/prez/crate/test/acceptance.sh` -- covers AC-1.2 -- status: green

---

_Generated by Intent v3.0.0 from `thread.json`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

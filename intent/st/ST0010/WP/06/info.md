---
wp_id: WP-06
title: Theme addressing split + --theme-path (hv's CLI asks)
scope: S
status: Not Started
---

# WP-06: Theme addressing split + --theme-path (hv's CLI asks)

## Objective

hv's CLI asks: --theme-path (a CLI door prepending to PREZ_THEME_PATH -- the resolver already speaks split_paths), and the addressing split (--theme names-only, --theme-file paths-only, front matter theme:/theme-file:, split refusals, --theme=./x.css refused). AT14 goes red against the pinned binary before any code. Sequenced after WP-04 so the carried contract is proven before the surface moves; announce-on-resolve (AC14) lands here, where theme.rs is already open.

## Acceptance

Acceptance Criteria for this work package are RENDERED into `ST0010/acceptance.md`, under the `WP-06` heading. THAT FILE IS A GENERATED VIEW -- a row authored there is discarded by the next sync. The contract is canon in the thread's model: change a state with the `intent ac` / `intent at` verbs, and mint or reword a row in `.canon/st/ST0010.json`, then `intent sync --to-store`. This cover never restates them.

---

_Generated by Intent v3.0.0 from `the thread canon`. Do not edit this file -- it is rendered from the model, and `intent doctor` reports any hand-edit as skew._

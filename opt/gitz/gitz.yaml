name: gitz
version_file: VERSION
utilz_version: "^2.0.0"
description: Git multi-repository operations
author: Matthew Sinclair
dependencies:
  - name: git
    required: true
    install: brew install git
    purpose: Repository status inspection; gitz calls require_command git

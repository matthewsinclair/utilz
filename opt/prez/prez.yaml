name: prez
version: 1.0.0
utilz_version: "^2.5.0"
description: Markdown to a self-contained HTML presentation -- a pipeline, not a viewer
author: Matthew Sinclair
website: https://matthewsinclair.com
help_file: ../../help/prez.md

# NO RUNTIME DEPENDENCIES. The built binary is self-contained: comrak is the
# only crate, and the seven themes plus mermaid.min.js are embedded at compile
# time with include_str!. Nothing is resolved from the filesystem at run time.
#
# cargo is a BUILD-time need rather than a runtime one, so it is deliberately
# NOT declared here -- doctor's declared-dependency walk would report it as
# missing on every machine that has already built the binary. The shim refuses
# by name when cargo is absent, and doctor carries a separate conditional check
# that fires only when a crate is present.
#
# A browser is needed only by the `pdf` and `present` verbs. The tool probes
# for one and refuses with every path it tried, so doctor does not duplicate a
# refusal the tool already makes well.
dependencies: []

optional_dependencies: []

# Supported input modes
modes:
  - file # a markdown deck on disk
  - stdin # a deck on standard input

# Main features
features:
  - Markdown to a single self-contained HTML file
  - Slide splitting, front matter, speaker notes
  - Seven brand-free built-in themes, plus external themes over PREZ_THEME_PATH
  - Images inlined as data URIs -- the artifact opens offline
  - Optional mermaid diagrams, vendored rather than fetched
  - PDF export and a local present mode, both via a browser

# INTEGRATION BLOCK ADDED 2026-09-03, hv's ruling, issue 0009. This file used
# to explain at length why there was none. Both of its reasons are retired:
#
#   1. ST0010's design deferred the binding rather than refusing it -- "the
#      Emacs bridge can bind prez later". Later is now.
#   2. It warned that a block here would make bridge.bats' hardcoded row count
#      wrong and redden a correct test. Issue 0006 fixed that test to DERIVE
#      the count by walking the yaml corpus, so a new block no longer reddens
#      anything. Verified in bridge.bats before this was added, because the
#      warning was specific enough to be worth checking rather than assuming.
#
# The old comment was also right that the GENERATOR's scaffolded block would
# have been wrong twice over -- `stdin -> buffer` advertises a round-trip prez
# does not do, since it writes a FILE. That is why this one is `file ->
# message`: the bridge echoes the "wrote deck.html" line rather than pretending
# to hand back a document.
#
# `flags: [build]` supplies the verb. Without it the bridge builds
# `utilz prez <file>` and the dispatcher refuses with "Unknown command" -- the
# exact defect issue 0009 found in four other utilities, all of which declared
# `flags: []` and none of which could ever have worked.
integration:
  input: file
  output: message
  flags: [build]
